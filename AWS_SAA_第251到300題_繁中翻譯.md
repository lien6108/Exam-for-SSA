# AWS SAA 第 251～300 題繁中翻譯

僅保留：題號、題目、選項、答案。

## Question #251

**題目**  
一個 Amazon EC2 執行個體位於新建 VPC 的私有子網路中。此子網路沒有對外網際網路存取能力，但該 EC2 執行個體需要能夠從外部廠商下載每月的安全性更新。解決方案架構師應該如何滿足這些需求？

**選項**
- A. 建立 internet gateway，並將其附加到 VPC。設定私有子網路的路由表，將 internet gateway 作為預設路由
- B. 建立 NAT gateway，並將其放置在公有子網路中。設定私有子網路的路由表，將 NAT gateway 作為預設路由
- C. 建立 NAT instance，並將其放置在 EC2 執行個體所在的相同子網路中。設定私有子網路的路由表以使用 NAT instance
- D. 建立 internet gateway，並將其附加到 VPC。再建立 NAT instance，並將其放置在 EC2 執行個體所在的相同子網路中

**答案**  
B

## Question #252

**題目**  
某位解決方案架構師需要設計一套用來儲存客戶案件檔案的系統。這些檔案是公司核心且重要的資產，檔案數量也會隨時間持續成長。這些檔案必須可由多台執行於 Amazon EC2 執行個體上的應用程式伺服器同時存取，且解決方案必須具備內建備援。哪一個解決方案符合這些需求？

**選項**
- A. Amazon Elastic File System (Amazon EFS)
- B. Amazon Elastic Block Store (Amazon EBS)
- C. Amazon S3 Glacier Deep Archive
- D. AWS Backup

**答案**  
A

## Question #253

**題目**  
某位解決方案架構師建立了兩個 IAM policy：Policy1 與 Policy2，且兩個 policy 都已附加到某個 IAM 群組。一位雲端工程師被加入為該 IAM 群組中的 IAM 使用者。這位雲端工程師將能執行下列哪個動作？

**選項**
- A. 刪除 IAM 使用者
- B. 刪除目錄服務目錄
- C. 刪除 Amazon EC2 執行個體
- D. 刪除 Amazon CloudWatch Logs 中的日誌

**答案**  
C

## Question #254

**題目**  
某公司正在檢視最近將一個三層式應用程式遷移至 VPC 的結果。安全團隊發現，在各應用層之間的 Amazon EC2 security group 進出站規則中，並未落實最小權限原則。解決方案架構師應該如何修正這個問題？

**選項**
- A. 使用 instance ID 作為來源或目的地來建立 security group 規則
- B. 使用 security group ID 作為來源或目的地來建立 security group 規則
- C. 使用 VPC CIDR 區塊作為來源或目的地來建立 security group 規則
- D. 使用子網路 CIDR 區塊作為來源或目的地來建立 security group 規則

**答案**  
B

## Question #255

**題目**  
某公司有一個電商結帳流程，會先將訂單寫入資料庫，再呼叫一個服務來處理付款。使用者在結帳過程中經常遇到逾時問題。當使用者重新送出結帳表單時，會為同一筆想要完成的交易建立多筆不同的訂單。解決方案架構師應該如何重構此流程，以避免產生多筆訂單？

**選項**
- A. 將訂單寫入 Amazon SQS FIFO queue。使用另一個元件讀取佇列並處理付款，再寫入資料庫
- B. 在寫入資料庫前，先將訂單資訊儲存在使用者的瀏覽器 cookie 中
- C. 讓應用程式在付款服務逾時後自動重試，直到付款完成
- D. 在資料庫中為交易建立唯一識別碼，並讓流程具備冪等性（idempotency）

**答案**  
D

## Question #256

**題目**  
某位解決方案架構師正在實作一套文件審閱應用程式，使用 Amazon S3 bucket 作為儲存。此解決方案必須防止文件被意外刪除，並確保所有版本的文件都可用。使用者必須能下載、修改並上傳文件。哪個解決方案符合這些需求？

**選項**
- A. 啟用 S3 Versioning
- B. 啟用 S3 Object Lock compliance mode
- C. 啟用 S3 Replication
- D. 啟用 S3 Glacier Deep Archive

**答案**  
A

## Question #257

**題目**  
某公司正在建置一個解決方案，用於回報 AWS 帳戶中所有應用程式的 Amazon EC2 Auto Scaling 事件。公司需要使用無伺服器方案，將 EC2 Auto Scaling 狀態資料儲存到 Amazon S3，之後再透過 Amazon Athena 進行分析。哪個解決方案最符合需求？

**選項**
- A. 使用 Amazon EventBridge 擷取 Auto Scaling 事件，觸發 AWS Lambda，將事件寫入 Amazon S3
- B. 使用 Amazon CloudWatch Logs 收集事件，再以手動方式匯出至 Amazon S3
- C. 使用 Amazon EC2 執行個體輪詢 Auto Scaling API，並將結果寫入 Amazon S3
- D. 使用 Amazon SNS 訂閱所有 Auto Scaling 事件，並由使用者手動下載資料到 Amazon S3

**答案**  
A

## Question #258

**題目**  
某公司每小時都會將數百個 .csv 檔放入 Amazon S3 bucket，每個檔案大小為 1 GB。每當檔案上傳完成後，公司都需要將檔案轉換成 Apache Parquet 格式，並將輸出檔放到另一個 S3 bucket。哪個解決方案最符合需求？

**選項**
- A. 使用 S3 事件通知觸發 AWS Lambda 進行轉換
- B. 使用 S3 事件通知觸發 AWS Glue job 進行轉換
- C. 使用 Amazon EC2 執行個體持續監控 S3 bucket，並進行轉換
- D. 使用 Amazon Athena 查詢 .csv，並輸出為 Parquet

**答案**  
B

## Question #259

**題目**  
某公司正在為所有執行於 Amazon RDS DB instance 上的資料庫實施新的資料保留政策。公司必須保留每日備份至少 2 年，且備份必須一致且可還原。解決方案架構師應該建議哪個方案？

**選項**
- A. 將自動備份保留期設定為 2 年
- B. 使用 AWS Backup 來排程與保留 RDS 備份
- C. 每天手動建立 DB snapshot，並自行管理保留週期
- D. 匯出資料到 Amazon S3，並用 Lifecycle 保留 2 年

**答案**  
B

## Question #260

**題目**  
某公司的法遵團隊需要將檔案分享移轉到 AWS。這些分享目前執行在 Windows Server SMB file share 上，由自管的內部 Active Directory 控制檔案與資料夾的存取權限。公司希望將 Amazon FSx for Windows File Server 納入解決方案，且盡量減少管理負擔。哪個方案最符合需求？

**選項**
- A. 建立 Amazon FSx for Windows File Server，並與自管 Active Directory 整合
- B. 建立 Amazon EFS，並在 Windows Server 上掛載
- C. 建立 Amazon S3 bucket，並透過 SMB 直接提供檔案分享
- D. 使用 Amazon EC2 Windows 執行個體自行架設 SMB 檔案伺服器

**答案**  
A

## Question #261

**題目**  
某公司最近將其零售網站部署給全球使用者。網站執行在多台 Amazon EC2 執行個體上，位於 Elastic Load Balancer 後方，並由跨多個 Availability Zone 的 Auto Scaling group 管理。公司希望根據客戶用來存取網站的裝置，提供不同版本的內容。解決方案架構師應採取哪兩個動作來滿足這些需求？（選兩項）

**選項**
- A. 設定 Amazon CloudFront 快取多個版本的內容
- B. 在 Network Load Balancer 設定 host header，將流量轉送到不同執行個體
- C. 設定 Lambda@Edge function，根據 User-Agent header 將特定物件傳送給使用者
- D. 設定 AWS Global Accelerator，將請求轉送到 Network Load Balancer (NLB)，並在 NLB 設定 host-based routing
- E. 設定 AWS Global Accelerator，將請求轉送到 Network Load Balancer (NLB)，並在 NLB 設定 path-based routing

**答案**  
AC

## Question #262

**題目**  
某公司計畫為其多層式 Web 應用程式使用 Amazon ElastiCache。解決方案架構師建立了一個 Cache VPC 用於 ElastiCache 叢集，以及一個 App VPC 用於應用程式的 Amazon EC2 執行個體。兩個 VPC 都位於 us-east-1 Region。解決方案架構師必須以最具成本效益的方式，讓應用程式的 EC2 執行個體能夠存取 ElastiCache 叢集。哪個解決方案符合需求？

**選項**
- A. 在兩個 VPC 之間建立 VPC peering connection，並在雙方 VPC 的 route table 加入對應路由
- B. 建立 Transit VPC，並更新 Cache VPC 與 App VPC 的 route table，讓流量經過 Transit VPC
- C. 在兩個 VPC 之間建立 VPC peering connection，並額外設定 inbound-only 規則
- D. 建立 Transit VPC，更新路由後再額外設定網路設備轉送

**答案**  
A

## Question #263

**題目**  
某公司正在建置一個由多個微服務組成的應用程式，並決定使用容器技術部署到 AWS。公司需要一個能將持續性維護與擴展工作降到最低的方案，且不能自行管理額外基礎設施。解決方案架構師應採取哪兩個動作來滿足需求？（選兩項）

**選項**
- A. 部署 Amazon Elastic Container Service (Amazon ECS) 叢集
- B. 在跨多個 Availability Zone 的 Amazon EC2 執行個體上部署 Kubernetes control plane
- C. 部署使用 Amazon EC2 launch type 的 Amazon ECS service，並指定所需 task 數量
- D. 部署使用 Fargate launch type 的 Amazon ECS service，並指定所需 task 數量
- E. 在跨多個 Availability Zone 的 Amazon EC2 執行個體上部署 Kubernetes worker nodes，並建立 deployment

**答案**  
AD

## Question #264

**題目**  
某公司有一個 Web 應用程式，部署在超過 10 台 Amazon EC2 執行個體上，並由 Amazon Route 53 導流。公司偶爾在瀏覽應用程式時發生逾時錯誤。網路團隊發現，某些 DNS 查詢回傳的是狀態不健康的 EC2 執行個體 IP 位址。解決方案架構師應該如何修正這個問題？

**選項**
- A. 為每個 EC2 執行個體建立 Route 53 health check，並使用 multivalue answer routing policy
- B. 為每個 EC2 執行個體建立 weighted routing policy
- C. 在每台 EC2 上安裝代理程式，主動更新 Route 53 記錄
- D. 將所有 EC2 執行個體移到單一 Availability Zone，降低 DNS 回應複雜度

**答案**  
A

## Question #265

**題目**  
某位解決方案架構師需要設計一個高可用性的應用程式，包含 Web、應用程式與資料庫三層。HTTPS 內容傳遞應該盡可能接近邊緣節點，並以最短時間送達，同時必須具備最佳安全性。哪個解決方案最符合這些需求？

**選項**
- A. 使用 Amazon CloudFront 搭配 AWS WAF，來源為 Application Load Balancer，資料庫使用 Multi-AZ 部署
- B. 直接使用 Network Load Balancer 提供 HTTPS 內容傳遞，並將資料庫部署於單一 AZ
- C. 使用 Amazon S3 Static Website Hosting 搭配公有資料庫
- D. 使用 Route 53 geolocation routing 導向單一 EC2 執行個體

**答案**  
A

## Question #266

**題目**  
某公司在 AWS 上執行一個熱門遊戲平台。此應用程式對延遲非常敏感，因為延遲會影響使用者體驗，並可能讓部分玩家獲得不公平優勢。該應用程式部署在每個 AWS Region 中，執行於 Amazon EC2 執行個體上，並使用 UDP 流量。公司希望提供最低延遲、就近導流以及靜態 IP。哪個解決方案最符合需求？

**選項**
- A. 使用 Amazon CloudFront 搭配 Application Load Balancer
- B. 使用 AWS Global Accelerator 搭配 Network Load Balancer
- C. 使用 Route 53 latency-based routing 搭配 NAT gateway
- D. 使用 API Gateway 搭配 Lambda

**答案**  
B

## Question #267

**題目**  
某公司有一百萬名使用者在使用其行動 App。公司必須近乎即時分析資料使用情況，也必須近乎即時加密資料，並以 Apache Parquet 格式集中儲存於單一位置，以供後續處理。哪個解決方案最符合需求？

**選項**
- A. 使用 Amazon Kinesis Data Streams、AWS Lambda 與 Amazon S3
- B. 使用 Amazon Kinesis Data Firehose 將資料轉換後寫入 Amazon S3，並使用 AWS Glue Data Catalog
- C. 使用 Amazon SQS 搭配 EC2 進行加密與轉換
- D. 使用 Amazon EMR 直接從行動裝置串流進行處理

**答案**  
B

## Question #268

**題目**  
某遊戲公司有一個顯示分數的 Web 應用程式，執行於 Application Load Balancer 後方的 Amazon EC2 執行個體上，並將資料儲存在 Amazon RDS for MySQL 資料庫中。使用者開始出現明顯延遲與中斷，原因是讀取查詢變多。哪個解決方案最適合改善此問題？

**選項**
- A. 使用 Amazon ElastiCache 快取常用分數資料
- B. 將資料庫改成單一大型 EC2 執行個體
- C. 使用 Amazon S3 儲存所有分數
- D. 將 Application Load Balancer 換成 Network Load Balancer

**答案**  
A

## Question #269

**題目**  
某電商公司注意到其以 Amazon RDS 為基礎的 Web 應用程式出現效能下降。問題來自商業分析人員觸發的大量唯讀 SQL 查詢。解決方案架構師需要在不影響主要交易流量的情況下解決此問題。應該怎麼做？

**選項**
- A. 建立 read replica，並將唯讀查詢導向 read replica
- B. 將所有查詢導向 primary DB instance
- C. 將資料庫改為 Amazon Redshift
- D. 將資料庫移到 Amazon DynamoDB

**答案**  
A

## Question #270

**題目**  
某公司使用集中式 AWS 帳戶，將日誌資料儲存在多個 Amazon S3 bucket 中。解決方案架構師需要確保資料在上傳至 S3 bucket 前就已完成靜態加密，且在傳輸過程中也必須加密。哪個解決方案最符合需求？

**選項**
- A. 使用 client-side encryption，並透過 HTTPS 上傳到 Amazon S3
- B. 使用 SSE-S3，並允許 HTTP 上傳
- C. 使用未加密檔案上傳至 S3，再由 Lambda 進行加密
- D. 使用 Amazon Macie 自動加密上傳資料

**答案**  
A

## Question #271

**題目**  
某位解決方案架構師觀察到，一個夜間批次工作需要先自動擴展 1 小時，才能達到所需的 Amazon EC2 容量。尖峰容量每天晚上都相同，而且批次工作固定在凌晨 1 點開始。解決方案架構師需要找出最具成本效益的方案，以便在批次工作開始時立即提供所需容量。哪個方案最適合？

**選項**
- A. 使用 scheduled scaling
- B. 使用 dynamic scaling
- C. 使用 predictive scaling
- D. 使用 step scaling

**答案**  
A

## Question #272

**題目**  
某公司透過位於 Application Load Balancer (ALB) 後方的一組 Amazon EC2 執行個體提供動態網站。網站需要支援多種語言，以服務全球客戶。此架構目前部署在 us-west-1 Region，且亞洲使用者延遲偏高。公司希望以最少的架構變更改善全球使用者的體驗。哪個解決方案最符合需求？

**選項**
- A. 使用 Amazon CloudFront，並將 ALB 作為來源站
- B. 將整個應用程式複製到每個 Region
- C. 使用 Route 53 geolocation routing 導向單一 Region 的 ALB
- D. 將網站改成靜態網站並放到 Amazon S3

**答案**  
A

## Question #273

**題目**  
一家快速成長的電商公司目前僅在單一 AWS Region 執行工作負載。解決方案架構師必須設計包含另一個 AWS Region 的災難復原（DR）策略。公司希望在 DR Region 中的資料庫保持最新狀態，且平時營運成本要盡可能低。哪個方案最符合需求？

**選項**
- A. 使用 pilot light 策略，並在另一個 Region 建立資料庫跨區複寫
- B. 每週手動匯出資料庫並上傳至另一個 Region
- C. 僅備份應用程式伺服器，不處理資料庫同步
- D. 使用備份與還原即可，不需持續同步

**答案**  
A

## Question #274

**題目**  
某公司在 Amazon EC2 執行個體上執行一個應用程式。公司需要為該應用程式建置災難復原（DR）方案，且恢復時間目標（RTO）必須小於 4 小時，同時盡量減少持續執行的資源數量。哪個解決方案最符合需求？

**選項**
- A. 使用備份與還原策略
- B. 使用 pilot light 策略
- C. 使用 warm standby 策略
- D. 使用多站點 active/active 策略

**答案**  
B

## Question #275

**題目**  
某公司執行一個內部瀏覽器型應用程式，部署在 Application Load Balancer 後方的 Amazon EC2 執行個體上。這些執行個體位於跨多個 Availability Zone 的 Auto Scaling group 中。Auto Scaling group 最多會擴展到 20 台執行個體，但每晚在所有員工登入前都會發生延遲，因為新的執行個體需要時間啟動。公司想降低延遲，同時維持彈性與成本效益。哪個解決方案最符合需求？

**選項**
- A. 使用 Auto Scaling warm pool
- B. 將所有執行個體改成 Reserved Instances
- C. 停用 Auto Scaling，固定開 20 台執行個體
- D. 將應用程式改寫為 Lambda

**答案**  
A

## Question #276

**題目**  
某公司有一個多層式應用程式，部署在 Auto Scaling group 中的多台 Amazon EC2 執行個體上。資料層使用 Amazon RDS for Oracle，且應用程式仰賴 Oracle 專屬的 PL/SQL 功能。流量持續增加，導致資料庫成為瓶頸。公司希望提升可擴展性，同時保留 Oracle 相容性與現有程式碼。哪個解決方案最符合需求？

**選項**
- A. 遷移到 Amazon Aurora PostgreSQL
- B. 將資料庫遷移到 Amazon RDS for Oracle 並建立 read replica
- C. 將應用程式改寫為 DynamoDB
- D. 使用 Amazon Redshift 取代交易資料庫

**答案**  
B

## Question #277

**題目**  
某公司提供線上服務，讓使用者上傳影片並轉碼成可供各種行動平台使用的格式。應用程式架構使用 Amazon EFS Standard 收集與儲存影片，讓多台 Amazon EC2 Linux 執行個體可以存取。公司希望降低儲存成本，因為影片一旦轉碼完成後，很少再被存取。哪個解決方案最符合需求？

**選項**
- A. 將檔案移到 Amazon EFS Infrequent Access (EFS IA)
- B. 改用 instance store
- C. 將所有影片保存在 EBS gp3
- D. 刪除原始影片並只保留轉碼後影片於記憶體中

**答案**  
A

## Question #278

**題目**  
某公司想建立一個應用程式，用來以階層式結構關係儲存員工資料。公司需要在高流量查詢下仍具備最低延遲回應，且必須保護敏感資料。此外，公司每月還需要收到稽核報告，了解哪些員工資料被查詢。哪個解決方案最符合需求？

**選項**
- A. 使用 Amazon DynamoDB 儲存資料，搭配 AWS KMS 加密，並使用 CloudTrail 進行稽核
- B. 使用 Amazon S3 儲存 JSON 檔
- C. 使用 Amazon Redshift 儲存階層資料
- D. 使用 Amazon ElastiCache 作為主資料來源

**答案**  
A

## Question #279

**題目**  
某公司有一個以 Amazon DynamoDB table 為基礎的應用程式。公司的法遵要求規定：資料庫備份必須每月執行一次、保留 6 個月，且還必須額外保留 7 年。哪個解決方案可以同時滿足這些要求？

**選項**
- A. 使用 DynamoDB point-in-time recovery
- B. 使用 AWS Backup 設定不同保留期限的備份計畫
- C. 每月手動匯出到 Amazon S3，並手動刪除
- D. 將資料複製到另一個 DynamoDB table

**答案**  
B

## Question #280

**題目**  
某公司在其網站前使用 Amazon CloudFront，並已啟用 logging，將 CloudFront 日誌儲存在其中一個 Amazon S3 bucket。公司需要對這些日誌進行進階分析並建立視覺化報表。解決方案架構師應該怎麼做？

**選項**
- A. 使用 Amazon Athena 查詢 S3 中的日誌，並使用 Amazon QuickSight 建立視覺化
- B. 將日誌下載到本機以 Excel 分析
- C. 使用 CloudFront 直接產生儀表板
- D. 將日誌複製到 DynamoDB 後分析

**答案**  
A

## Question #281

**題目**  
某公司執行一組 Web 伺服器，後端使用 Amazon RDS for PostgreSQL DB instance。例行法遵檢查後，公司制定新標準，要求所有正式環境資料庫的恢復點目標（RPO）都必須小於 1 秒。哪個解決方案最符合這些需求？

**選項**
- A. 使用 RDS Multi-AZ
- B. 使用 cross-Region read replica
- C. 每分鐘建立 snapshot
- D. 使用 S3 匯出資料

**答案**  
A

## Question #282

**題目**  
某公司執行一個 Web 應用程式，部署在 VPC 私有子網路中的 Amazon EC2 執行個體上。跨越公有子網路的 Application Load Balancer (ALB) 將 Web 流量導向這些 EC2 執行個體。公司希望新增安全措施，以便過濾常見 Web 攻擊流量，且盡量減少營運負擔。哪個解決方案最符合需求？

**選項**
- A. 在 ALB 前方部署 AWS WAF
- B. 在每台 EC2 執行個體上安裝第三方防火牆
- C. 將 ALB 換成 NAT gateway
- D. 使用 Network ACL 進行內容過濾

**答案**  
A

## Question #283

**題目**  
某研究公司執行由模擬應用程式與視覺化應用程式所組成的實驗。模擬應用程式執行於 Linux，且每 5 分鐘將中間資料輸出到 NFS share。視覺化應用程式是 Windows 桌面應用程式，需要讀取相同資料。公司希望將兩個工作負載移轉到 AWS，並保留共享儲存存取，且延遲要低。哪個解決方案最符合需求？

**選項**
- A. 使用 Amazon FSx for NetApp ONTAP 提供多重協定存取
- B. 使用 Amazon EBS 提供給所有執行個體共用
- C. 使用 Amazon S3 Static Website Hosting
- D. 使用 DynamoDB 儲存中間資料

**答案**  
A

## Question #284

**題目**  
在預算規劃的一部分中，管理階層想要一份依使用者列出 AWS 帳單項目的報表。這些資料將用於建立各部門預算。解決方案架構師需要以最有效率的方式取得這份報表資訊。哪個解決方案符合需求？

**選項**
- A. 啟用成本分攤標籤（cost allocation tags）並使用 Cost Explorer
- B. 手動檢查每個 IAM 使用者的 API 呼叫
- C. 使用 CloudTrail 直接產生帳單報表
- D. 為每位使用者建立獨立 AWS 帳戶

**答案**  
A

## Question #285

**題目**  
某公司以 Amazon S3 代管其靜態網站，現在想在網頁中加入聯絡表單。該聯絡表單需要動態的伺服器端元件，讓使用者輸入姓名、電子郵件、電話與訊息內容。公司預期流量會有明顯波動。哪個解決方案最符合需求？

**選項**
- A. 使用 Amazon API Gateway 與 AWS Lambda 處理表單提交
- B. 在 Amazon EC2 上自行架設 Web 伺服器處理表單
- C. 將表單內容寫入本機文字檔
- D. 使用 Amazon CloudFront Functions 直接將資料寫入資料庫

**答案**  
A

## Question #286

**題目**  
某公司有一個靜態網站，架設在 Amazon CloudFront 前方，後端是 Amazon S3。該靜態網站使用資料庫後端。公司注意到網站沒有反映 Git 儲存庫中已完成的更新。公司檢查後發現內容實際上已成功更新到 S3。解決方案架構師應如何處理這個問題？

**選項**
- A. 建立 CloudFront invalidation，讓快取內容失效
- B. 重新建立整個 S3 bucket
- C. 關閉 CloudFront logging
- D. 將資料庫改成 DynamoDB

**答案**  
A

## Question #287

**題目**  
某公司想將一個 Windows 型應用程式從內部環境遷移到 AWS Cloud。此應用程式為三層架構：application tier、business tier，以及使用 Microsoft SQL Server 的 database tier。公司希望使用 SQL Server 的特定功能，例如原生複寫與 SQL Server Agent。哪個解決方案最符合需求？

**選項**
- A. 將資料庫部署在 Amazon RDS for SQL Server
- B. 將資料庫部署在 Amazon EC2 上的 SQL Server
- C. 將資料庫改成 Aurora MySQL
- D. 將資料庫改成 DynamoDB

**答案**  
B

## Question #288

**題目**  
某公司正將一組 Linux 型 Web 伺服器遷移到 AWS。這些 Web 伺服器需要存取共用檔案儲存中的部分內容，且公司不能對應用程式做任何修改。解決方案架構師應如何滿足這些需求？

**選項**
- A. 建立 Amazon EFS 並掛載到所有 Web 伺服器
- B. 使用 Amazon S3 並要求應用程式改寫 API
- C. 為每台執行個體建立獨立 EBS
- D. 使用 instance store 進行內容同步

**答案**  
A

## Question #289

**題目**  
某公司有一個 AWS Lambda function，需要對同一個 AWS 帳戶中的 Amazon S3 bucket 具有讀取權限。哪個解決方案能以最安全的方式滿足這些需求？

**選項**
- A. 在 S3 bucket policy 中授予所有 Lambda function 讀取權限
- B. 建立 IAM role 並附加最小權限的 S3 讀取 policy，再將該 role 指派給 Lambda function
- C. 將 Access Key 寫在 Lambda 環境變數中
- D. 將 S3 bucket 設為公開可讀

**答案**  
B

## Question #290

**題目**  
某公司在多台 Amazon EC2 執行個體上代管 Web 應用程式。這些 EC2 執行個體位於會依使用者需求進行擴展的 Auto Scaling group 中。公司希望在不做長期承諾的前提下最佳化成本節省。哪種 EC2 購買選項最適合？

**選項**
- A. Reserved Instances
- B. Dedicated Hosts
- C. Spot Instances
- D. Savings Plans（3 年期）

**答案**  
C

## Question #291

**題目**  
某媒體公司使用 Amazon CloudFront 傳遞其公開可用的串流影片內容。公司想保護儲存在 Amazon S3 中的影片內容，並控制誰可以存取。部分使用者使用的是不支援 cookie 的自訂 HTTP client。哪個解決方案最符合需求？

**選項**
- A. 使用 CloudFront signed URLs
- B. 使用 CloudFront signed cookies
- C. 將 S3 bucket 設為公開
- D. 只使用 Referer header 限制存取

**答案**  
A

## Question #292

**題目**  
某公司正在準備一個新的資料平台，將從多個來源擷取即時串流資料。公司需要在將資料寫入 Amazon S3 前先進行轉換，且需要能夠用 SQL 查詢轉換後的資料。哪些解決方案可以滿足這些需求？（選兩項）

**選項**
- A. 使用 Amazon Kinesis Data Firehose 將資料轉換後寫入 Amazon S3
- B. 使用 Amazon Athena 以 SQL 查詢儲存在 Amazon S3 中的資料
- C. 使用 Amazon SQS 直接查詢原始串流資料
- D. 使用 Route 53 對資料做 SQL 分析
- E. 使用 AWS Directory Service 整理資料格式

**答案**  
AB

## Question #293

**題目**  
某公司原本在內部部署使用磁碟區備份解決方案，現在該方案已達生命週期終點。公司希望將 AWS 作為新備份方案的一部分，同時保留所有資料的本地端存取能力。公司還希望確保資料在傳輸到 AWS 後會自動進行完整性驗證。哪個解決方案最符合需求？

**選項**
- A. 使用 AWS Storage Gateway stored volumes
- B. 使用 AWS DataSync 將所有資料直接同步到 S3，並刪除本地資料
- C. 使用 Amazon EBS snapshot 做本地備份
- D. 使用 Amazon FSx for Lustre 作為備份目標

**答案**  
A

## Question #294

**題目**  
一個部署在 Amazon EC2 執行個體上的應用程式需要存取 Amazon S3 bucket，且流量不得經過網際網路。解決方案架構師應如何設定存取方式以符合需求？

**選項**
- A. 使用 Amazon Route 53 建立 private hosted zone
- B. 建立 S3 gateway VPC endpoint
- C. 建立 NAT gateway
- D. 建立 internet gateway

**答案**  
B

## Question #295

**題目**  
某電商公司在 AWS Cloud 中儲存數 TB 的客戶資料，其中包含個人可識別資訊（PII）。公司希望將資料提供給三個應用程式使用，但只有其中一個應用程式需要處理 PII。資料在提供給其他應用程式前，必須先移除 PII。哪個解決方案最符合需求，且營運負擔最低？

**選項**
- A. 使用 Amazon S3 Object Lambda，在讀取時移除不需要的 PII
- B. 複製三份資料，各自手動維護
- C. 將所有資料改存到公開 S3 bucket
- D. 使用 EC2 定期離線處理所有資料副本

**答案**  
A

## Question #296

**題目**  
某開發團隊已在 development VPC 內部的 Amazon EC2 執行個體上部署新應用程式。解決方案架構師需要在同一個帳戶中建立新的 VPC，並與 development VPC 建立 peering。development VPC 的 CIDR 區塊為 10.0.0.0/16。新的 VPC CIDR 區塊應符合哪個條件？

**選項**
- A. 可與 10.0.0.0/16 重疊
- B. 不可與 10.0.0.0/16 重疊
- C. 必須完全相同
- D. 必須是公有 IP 範圍

**答案**  
B

## Question #297

**題目**  
某公司在五台 Amazon EC2 執行個體上部署應用程式，並由 Application Load Balancer (ALB) 透過 target group 分散流量。每台執行個體的平均 CPU 使用率大多低於 10%，僅偶爾短暫飆升至 65%。解決方案架構師需要找出具成本效益且能維持良好效能的自動擴展策略。哪個方案最適合？

**選項**
- A. 使用 target tracking scaling policy
- B. 使用固定排程，每小時擴展一次
- C. 永遠將執行個體數量固定在最大值
- D. 停用 ALB 健康檢查

**答案**  
A

## Question #298

**題目**  
某公司正在關鍵業務應用程式上使用位於 Application Load Balancer 後方的 Amazon EC2 執行個體。這些 EC2 執行個體位於 Auto Scaling group 中，並存取 Amazon RDS DB instance。由於 EC2 執行個體與 RDS DB instance 都位於單一 Availability Zone，因此此設計未通過營運審查。公司需要在盡量少修改架構的前提下提升高可用性。哪個解決方案最符合需求？

**選項**
- A. 將 Auto Scaling group 與 RDS 改為跨多個 Availability Zone 部署
- B. 只增加更多單一 AZ 的 EC2 執行個體
- C. 改用單台更大的 EC2 執行個體
- D. 將 RDS 改成 on-premises 資料庫

**答案**  
A

## Question #299

**題目**  
某研究實驗室需要處理約 8 TB 的資料。儲存子系統必須提供低於 1 毫秒的延遲，以及至少 6 GBps 的吞吐量。數百台執行 Amazon Linux 的 Amazon EC2 執行個體將分散式處理這些資料。哪個儲存解決方案最符合需求？

**選項**
- A. Amazon FSx for Lustre
- B. Amazon EFS Standard
- C. Amazon S3 Standard
- D. Amazon EBS gp3

**答案**  
A

## Question #300

**題目**  
某公司因硬體容量限制，必須將一個舊版應用程式從內部部署資料中心遷移到 AWS Cloud。該應用程式全天候 24x7 運作，且其資料庫儲存容量會隨時間持續成長。解決方案架構師應該如何建議此公司設計解決方案？

**選項**
- A. 應用程式部署在 Amazon EC2，資料庫使用 Amazon Aurora
- B. 應用程式與資料庫都部署在單一 EC2 執行個體
- C. 應用程式改寫為 Lambda，資料庫改為本機檔案
- D. 將資料庫儲存在 instance store

**答案**  
A
