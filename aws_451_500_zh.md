# AWS SAA 第 451–500 題中文整理

說明：
- 已將第 451–500 題整理為中文。
- AWS 服務名稱與關鍵英文名詞盡量保留原文。
- 第 477 題因原始檔選項內容缺漏，已依題意補成可用版本，答案維持原檔為 C。

## Question #451
**題目**  
一家公司正將應用程式與資料庫遷移到 AWS Cloud。公司將使用 Amazon ECS、AWS Direct Connect 與 Amazon RDS。下列哪些工作會由公司的營運團隊負責？（選三項）

**選項**
- A. 管理 Amazon RDS 的基礎設施層、作業系統與平台
- B. 建立 Amazon RDS DB instance，並設定排程維護時段
- C. 在 Amazon ECS 上設定額外軟體元件，用於監控、修補程式管理、日誌管理與主機管理
- D. 為 Amazon RDS 安裝所有次版本與主版本的資料庫修補程式
- E. 確保資料中心內 Amazon RDS 基礎設施的實體安全
- F. 加密經由 Direct Connect 傳輸中的資料

**答案**  
BCF

## Question #452
**題目**  
某公司在 Amazon EC2 instance 上執行一個 Java 工作。此工作每小時執行一次，每次執行 10 秒，需使用 1 GB 記憶體。除了短暫尖峰期間會吃滿 CPU 外，其餘時間 CPU 使用率都很低。公司希望最佳化執行成本。哪個解決方案最符合需求？

**選項**
- A. 使用 AWS App2Container (A2C) 將工作容器化，並以 Amazon ECS task 執行
- B. 將程式碼改成 AWS Lambda function（1 GB 記憶體），並用 Amazon EventBridge 排程執行
- C. 使用 A2C 容器化後，將容器安裝到既有 AMI 中繼續執行
- D. 在工作完成後停止 EC2 instance，下次排程前再重新啟動

**答案**  
B

## Question #453
**題目**  
某公司要為 Amazon EC2 資料與多個 Amazon S3 bucket 建立備份策略。基於法規要求，備份檔必須保留特定期間，而且在保留期間內不得被修改。哪個解決方案符合需求？

**選項**
- A. 使用 AWS Backup 建立啟用 vault lock（governance mode）的 backup vault，並建立所需的 backup plan
- B. 使用 Amazon Data Lifecycle Manager 建立自動快照政策
- C. 使用 Amazon S3 File Gateway 建立備份，並搭配 S3 Lifecycle 管理
- D. 使用 AWS Backup 建立啟用 vault lock（compliance mode）的 backup vault，並建立所需的 backup plan

**答案**  
A

## Question #454
**題目**  
某公司在多個 AWS Region 與多個帳號中有資源。新到任的 solutions architect 發現前任員工沒有留下資源清單細節。現在需要建立並繪製各工作負載在所有帳號中的關聯地圖。哪個方案在營運效率上最佳？

**選項**
- A. 使用 AWS Systems Manager Inventory 從詳細報表產生 map view
- B. 使用 AWS Step Functions 蒐集工作負載細節，再手動繪製架構圖
- C. 使用 Workload Discovery on AWS 產生工作負載架構圖
- D. 使用 AWS X-Ray 檢視工作負載細節，再建立關聯架構圖

**答案**  
A

## Question #455
**題目**  
某公司使用 AWS Organizations。公司希望為部分 AWS 帳號設定不同預算，並在特定期間達到預算門檻時收到通知，且自動阻止這些帳號再建立額外 AWS 資源。哪一組方案符合需求？（選三項）

**選項**
- A. 使用 AWS Budgets 建立預算，並在 Cost and Usage Reports 區段設定預算金額
- B. 使用 AWS Budgets 建立預算，並在 Billing dashboard 中設定預算金額
- C. 建立 IAM user 供 AWS Budgets 執行 budget actions
- D. 建立 IAM role 供 AWS Budgets 執行 budget actions
- E. 設定警示，並在 budget action 中選擇 IAM identity policy 來阻止建立新資源
- F. 設定警示，並在 budget action 中選擇 SCP 來限制帳號建立新資源

**答案**  
BDF

## Question #456
**題目**  
某公司在一個 AWS Region 的 Amazon EC2 instances 上執行應用程式。公司希望把這些 EC2 instances 備份到第二個 Region，也希望能在第二個 Region 佈建 EC2 資源，並從單一 AWS 帳號集中管理。哪個方案最符合成本效益？

**選項**
- A. 建立 DR 計畫，在第二個 Region 部署相同數量的 EC2 instances，並設定資料複寫
- B. 建立 point-in-time Amazon EBS snapshots，並將快照複製到第二個 Region
- C. 使用 AWS Backup 建立 backup plan，並為 EC2 instances 啟用 cross-Region backup 到第二個 Region
- D. 在第二個 Region 部署相同數量的 EC2 instances，並用 AWS DataSync 傳輸資料

**答案**  
C

## Question #457
**題目**  
某公司要建立一個應用程式，將資料傳送給產品製造商。公司有自己的 identity provider (IdP)，並希望在使用者透過應用程式傳輸資料時，由該 IdP 進行驗證。同時必須使用 AS2 protocol。哪個方案符合需求？

**選項**
- A. 使用 AWS DataSync 傳輸資料，並以 AWS Lambda 處理 IdP 驗證
- B. 使用 Amazon AppFlow 傳輸資料，並以 Amazon ECS task 處理 IdP 驗證
- C. 使用 AWS Transfer Family 傳輸資料，並以 AWS Lambda 處理 IdP 驗證
- D. 使用 AWS Storage Gateway 傳輸資料，並以 Amazon Cognito identity pool 處理 IdP 驗證

**答案**  
C

## Question #458
**題目**  
某 solutions architect 正在用 Amazon API Gateway 設計一個現金回饋服務的 REST API。此應用程式運算資源需求為 1 GB 記憶體與 2 GB 儲存空間，且資料必須是關聯式格式。哪一組額外 AWS 服務能以最低管理成本滿足需求？（選兩項）

**選項**
- A. Amazon EC2
- B. AWS Lambda
- C. Amazon RDS
- D. Amazon DynamoDB
- E. Amazon EKS

**答案**  
BC

## Question #459
**題目**  
某公司使用 AWS Organizations 在多個 AWS 帳號中執行工作負載。公司有 tagging policy，會在建立標籤時自動加上 department tag。會計團隊需要計算 Amazon EC2 的花費，並判斷各部門該負責哪些成本，不論資源屬於哪個 AWS 帳號。會計團隊可存取整個組織的 AWS Cost Explorer。哪個方案在營運效率上最佳？

**選項**
- A. 在管理帳號的 billing console 啟用使用者自訂 cost allocation tag：department，並建立跨帳號報表
- B. 在管理帳號的 billing console 啟用 AWS 定義的 cost allocation tag：department，並建立跨帳號報表
- C. 在 member account 的 billing console 啟用使用者自訂 cost allocation tag：department，並建立 Cost Explorer 報表
- D. 在 member account 的 billing console 啟用 AWS 定義的 cost allocation tag：department，並建立 Cost Explorer 報表

**答案**  
C

## Question #460
**題目**  
某公司要在其 SaaS 應用程式的 Salesforce 帳號與 Amazon S3 之間安全交換資料。資料靜態加密必須使用 AWS KMS customer managed keys (CMKs)，傳輸中也必須加密。公司已啟用 Salesforce 帳號的 API 存取。哪個方案符合需求？

**選項**
- A. 建立 AWS Lambda functions，安全地將資料從 Salesforce 傳到 Amazon S3
- B. 建立 AWS Step Functions workflow 來安全傳輸資料
- C. 建立 Amazon AppFlow flows，安全地將資料從 Salesforce 傳到 Amazon S3
- D. 為 Salesforce 建立 custom connector 來傳輸資料

**答案**  
C

## Question #461
**題目**  
某公司在單一 AWS Region 開發一款手機遊戲 App。App 執行於 Auto Scaling group 中的多台 Amazon EC2 instances，資料儲存在 Amazon DynamoDB。使用者與伺服器之間同時使用 TCP 與 UDP 通訊，而且要提供全球使用者最低延遲。哪個方案符合需求？

**選項**
- A. 使用 AWS Global Accelerator，後方搭配 Application Load Balancer (ALB)
- B. 使用 AWS Global Accelerator，後方搭配 Network Load Balancer (NLB)
- C. 使用 Amazon CloudFront，後方搭配 NLB
- D. 使用 Amazon CloudFront，後方搭配 ALB

**答案**  
A

## Question #462
**題目**  
某公司有一套處理客戶訂單的應用程式，部署在 Amazon EC2 instance 上，並將訂單寫入 Amazon Aurora。當流量升高時，工作負載偶爾來不及處理訂單。solutions architect 應如何盡可能快速且可靠地把訂單寫入資料庫？

**選項**
- A. 流量升高時放大 EC2 instance 規格，並將訂單寫入 Amazon SNS
- B. 先將訂單寫入 Amazon SQS queue，再用位於 Auto Scaling group 中的 EC2 instances 處理
- C. 將訂單寫入 Amazon SNS，並讓資料庫端點直接訂閱 SNS topic
- D. 當 EC2 instance CPU 達門檻時才把訂單寫入 Amazon SQS queue

**答案**  
B

## Question #463
**題目**  
某 IoT 公司推出一款床墊，內建感測器蒐集使用者睡眠資料，並將資料送到 Amazon S3 bucket。每晚每張床墊約產生 2 MB 資料。公司必須為每張床墊處理並摘要資料，且結果要盡快可用。資料處理需要 1 GB 記憶體，30 秒內可完成。哪個方案最具成本效益？

**選項**
- A. 使用 AWS Glue 搭配 Scala job
- B. 使用 Amazon EMR 搭配 Apache Spark script
- C. 使用 AWS Lambda 搭配 Python script
- D. 使用 AWS Glue 搭配 PySpark job

**答案**  
C

## Question #464
**題目**  
某公司經營線上購物應用程式，所有訂單都儲存在 Amazon RDS for PostgreSQL Single-AZ DB instance。管理層希望消除單點故障，並要求在不修改應用程式程式碼的前提下，盡量縮短資料庫停機時間。哪個方案符合需求？

**選項**
- A. 直接修改既有 DB instance，啟用 Multi-AZ deployment
- B. 建立新的 RDS Multi-AZ deployment，從目前 RDS instance snapshot 還原
- C. 在另一個 Availability Zone 建立只讀 replica，並用 Route 53 導流
- D. 將 PostgreSQL 放進 EC2 Auto Scaling group，最少兩台

**答案**  
A

## Question #465
**題目**  
某公司要在同一個 Availability Zone 內，將應用程式部署到多台 Nitro-based Amazon EC2 instances 上。公司還希望應用程式可同時在多台 Nitro-based EC2 instances 上寫入多個 block storage volumes，以提升可用性。哪個方案符合需求？

**選項**
- A. 使用 gp3 EBS volumes 搭配 Amazon EBS Multi-Attach
- B. 使用 st1 EBS volumes 搭配 Amazon EBS Multi-Attach
- C. 使用 io2 EBS volumes 搭配 Amazon EBS Multi-Attach
- D. 使用 gp2 EBS volumes 搭配 Amazon EBS Multi-Attach

**答案**  
C

## Question #466
**題目**  
某公司原本設計了一套無狀態二層式應用程式，使用單一 Availability Zone 的 Amazon EC2，以及 Amazon RDS Multi-AZ DB instance。新的管理層希望整套應用程式具備高可用性。solutions architect 應如何調整？

**選項**
- A. 將應用程式改為 Multi-AZ EC2 Auto Scaling，並建立 Application Load Balancer
- B. 為 EC2 instances 建立 snapshots 並複製到另一個 AWS Region
- C. 使用 Amazon Route 53 latency-based routing 導流到應用程式
- D. 使用 Amazon Route 53 規則處理流量，並建立 Multi-AZ Application Load Balancer

**答案**  
A

## Question #467
**題目**  
某公司使用 AWS Organizations。一個 member account 購買了 Compute Savings Plan，但因工作負載變化，該帳號現在無法完全吃滿承諾額度，只用了不到 50% 的已購買運算能力。哪個方案最合適？

**選項**
- A. 在購買 Savings Plan 的 member account 的 Billing Preferences 中啟用 discount sharing
- B. 在 Organizations 管理帳號的 Billing Preferences 中啟用 discount sharing
- C. 將其他 AWS 帳號中的額外運算工作負載遷移到這個已購買 Savings Plan 的帳號
- D. 在 Reserved Instance Marketplace 出售多餘的 Savings Plan 承諾

**答案**  
B

## Question #468
**題目**  
某公司要開發一個微服務應用程式，提供客戶搜尋目錄。前端必須使用 REST APIs，且這些 REST APIs 必須存取部署在私有 VPC 子網中的容器化後端服務。哪個方案符合需求？

**選項**
- A. 使用 Amazon API Gateway WebSocket API，後端使用私有子網中的 Amazon ECS
- B. 使用 Amazon API Gateway REST API，後端使用私有子網中的 Amazon ECS
- C. 使用 Amazon API Gateway WebSocket API，後端使用公有子網中的 Amazon ECS
- D. 使用 Amazon API Gateway REST API，後端使用公有子網中的 Amazon ECS

**答案**  
B

## Question #469
**題目**  
某公司將原始蒐集資料儲存在 Amazon S3 bucket，並代表不同客戶執行多種分析。不同分析需求會造成不同的 S3 物件存取模式，且公司無法預測或控制。公司希望降低 S3 成本。哪個方案符合需求？

**選項**
- A. 使用 S3 replication 將低頻存取物件轉到 S3 Standard-IA
- B. 使用 S3 Lifecycle 將物件從 S3 Standard 轉到 S3 Standard-IA
- C. 使用 S3 Lifecycle 將物件從 S3 Standard 轉到 S3 Intelligent-Tiering
- D. 使用 S3 Inventory 找出未存取物件，再轉到 S3 Intelligent-Tiering

**答案**  
C

## Question #470
**題目**  
某公司的應用程式執行在具備 IPv6 位址的 Amazon EC2 instances 上。這些應用程式需要主動透過網際網路與外部應用通訊，但公司安全政策規定外部服務不能主動連回這些 EC2 instances。solutions architect 應建議哪個方案？

**選項**
- A. 建立 NAT gateway，並在 subnet route table 中設為目的地
- B. 建立 internet gateway，並在 subnet route table 中設為目的地
- C. 建立 virtual private gateway，並在 subnet route table 中設為目的地
- D. 建立 egress-only internet gateway，並在 subnet route table 中設為目的地

**答案**  
D

## Question #471
**題目**  
某公司正在建立一個執行於 VPC 內容器上的應用程式。應用程式會存取 Amazon S3 bucket，在開發階段每天會在 S3 讀寫 1 TB 資料。公司希望降低成本，並盡可能避免流量走公網。哪個方案符合需求？

**選項**
- A. 為 S3 bucket 啟用 S3 Intelligent-Tiering
- B. 為 S3 bucket 啟用 S3 Transfer Acceleration
- C. 建立 Amazon S3 gateway VPC endpoint，並關聯到 VPC 中所有 route tables
- D. 建立 Amazon S3 interface endpoint，並關聯到 VPC 中所有 route tables

**答案**  
C

## Question #472
**題目**  
某公司有一個行動聊天應用程式，資料儲存在 Amazon DynamoDB。使用者希望能以最低延遲讀取新訊息。solutions architect 需要在盡量少改應用程式的情況下設計最佳方案。應選哪個方法？

**選項**
- A. 為新訊息資料表配置 Amazon DynamoDB Accelerator (DAX)，並更新程式碼改用 DAX endpoint
- B. 為 DynamoDB 加 read replicas，並讓應用程式改連 read endpoint
- C. 將該資料表的 read capacity units 加倍，並維持既有 DynamoDB endpoint
- D. 新增 Amazon ElastiCache for Redis，並讓應用程式改連 Redis endpoint

**答案**  
A

## Question #473
**題目**  
某公司網站部署在 Application Load Balancer (ALB) 後方的 Amazon EC2 instances 上，並提供靜態內容。網站流量持續增加，公司擔心成本上升。哪個方案最適合？

**選項**
- A. 建立 Amazon CloudFront distribution，在 edge locations 快取靜態檔案
- B. 建立 Amazon ElastiCache 叢集，並讓 ALB 連到 ElastiCache 供應快取檔案
- C. 建立 AWS WAF web ACL 並加入快取靜態檔案規則
- D. 在另一個 AWS Region 建第二個 ALB，並將流量導向最近 Region 以降低傳輸成本

**答案**  
A

## Question #474
**題目**  
某公司在多個 AWS Region 擁有多個 VPC，以支援彼此隔離的工作負載。近期因應新應用需求，這些 VPC 現在必須能與所有其他 Region 的 VPC 通訊。哪個方案在管理成本上最低？

**選項**
- A. 在單一 Region 內與跨 Region 都使用 VPC peering
- B. 使用 AWS Direct Connect gateways 連接所有 Region 的 VPC
- C. 在單一 Region 使用 AWS Transit Gateway 管理 VPC 通訊，跨 Region 則使用 Transit Gateway peering
- D. 使用 AWS PrivateLink 跨 Region 連接所有 VPC

**答案**  
C

## Question #475
**題目**  
某公司正在設計一個使用 Amazon ECS 的容器化應用程式。應用程式需要存取共用檔案系統，該檔案系統必須具備高耐久性，並可在 RPO 8 小時內復原到另一個 AWS Region。檔案系統在單一 Region 內需於每個 Availability Zone 提供 mount target。solutions architect 想用 AWS Backup 管理跨 Region 複寫。哪個方案符合需求？

**選項**
- A. 使用 Multi-AZ deployment 的 Amazon FSx for Windows File Server
- B. 使用 Multi-AZ deployment 的 Amazon FSx for NetApp ONTAP
- C. 使用 Standard storage class 的 Amazon EFS
- D. 使用 Amazon FSx for OpenZFS

**答案**  
C

## Question #476
**題目**  
某公司預期近期快速成長。solutions architect 需要為現有使用者設定權限，並為新使用者授權。已決定依部門建立 IAM groups，並將新使用者加入對應群組。哪個額外動作是最安全的授權方式？

**選項**
- A. 使用 SCP 管理存取權限
- B. 建立具有 least privilege 的 IAM roles，並將 roles 掛到 IAM groups
- C. 建立具有 least privilege 的 IAM policy，並將 policy 掛到 IAM groups
- D. 建立 IAM roles，並為 roles 設定 permissions boundary

**答案**  
C

## Question #477
**題目**  
某群組需要列出 Amazon S3 bucket 內容，並刪除該 bucket 內的物件。管理員已建立 IAM policy 並套用到該群組，但群組仍無法刪除物件。公司遵循 least-privilege 原則。應在 policy 中補上哪段 statement 才能修正權限？

**選項**
- A. 允許 `s3:*Object`，資源為 `arn:aws:s3:::bucket-name/*`
- B. 允許 `s3:*`，資源為 `arn:aws:s3:::bucket-name/*`
- C. 允許 `s3:DeleteObject`，資源為 `arn:aws:s3:::bucket-name/*`
- D. 允許 `s3:DeleteObjectVersion`，資源為 `arn:aws:s3:::bucket-name/*`

**答案**  
C

## Question #478
**題目**  
某法律事務所需要對外公開分享資訊，內容包含數百個檔案，且這些檔案必須可被公開讀取。但在指定未來日期之前，任何人都不得修改或刪除這些檔案。哪個方案在安全性上最佳？

**選項**
- A. 將所有檔案上傳到啟用 static website hosting 的 Amazon S3 bucket，並對所有 AWS principals 給 read-only IAM 權限
- B. 建立新的 Amazon S3 bucket，啟用 S3 Versioning，並使用 S3 Object Lock 設定符合需求的 retention period
- C. 建立新的 Amazon S3 bucket，啟用 S3 Versioning，並在檔案異動時觸發 AWS Lambda 還原
- D. 將檔案放到啟用 static website hosting 的 Amazon S3 bucket，並以 S3 存取設定限制修改

**答案**  
B

## Question #479
**題目**  
某公司以手動方式為新網站原型建立基礎設施，包含 Auto Scaling group、Application Load Balancer 與 Amazon RDS database。經過充分驗證後，公司希望能自動化地立即將這套基礎設施部署到開發與正式環境，並涵蓋兩個 Availability Zones。solutions architect 應建議哪個方案？

**選項**
- A. 使用 AWS Systems Manager 複製並佈建原型基礎設施
- B. 以原型為藍本，將基礎設施定義為 template，並用 AWS CloudFormation 部署
- C. 使用 AWS Config 記錄原型資源清單，再用 AWS Config 部署到兩個 Availability Zones
- D. 使用 AWS Elastic Beanstalk 參照原型自動部署新環境

**答案**  
B

## Question #480
**題目**  
某商業應用程式部署在 Amazon EC2 上，並使用 Amazon S3 儲存加密物件。資訊安全長要求 EC2 與 S3 之間的應用程式流量不得經過公網。solutions architect 應使用哪項能力來符合要求？

**選項**
- A. AWS KMS
- B. VPC endpoint
- C. Private subnet
- D. Virtual private gateway

**答案**  
B

## Question #481
**題目**  
某公司在 AWS Cloud 上部署三層式 Web 應用程式。資料庫層為 Amazon RDS for MySQL Multi-AZ，快取層為 Amazon ElastiCache。公司希望在客戶將資料新增到資料庫時，同步把資料加入或更新到快取中，並確保快取資料永遠與資料庫一致。哪個方案符合需求？

**選項**
- A. Lazy loading caching strategy
- B. Write-through caching strategy
- C. TTL caching strategy
- D. AWS AppConfig caching strategy

**答案**  
B

## Question #482
**題目**  
某公司要把 100 GB 歷史資料從內部環境遷移到 Amazon S3 bucket。內部端有 100 Mbps 網路，且資料在傳輸到 S3 過程中必須加密。之後新資料會直接存到 Amazon S3。哪個方案在管理成本上最低？

**選項**
- A. 使用 AWS CLI 的 `s3 sync` 直接把資料搬到 S3 bucket
- B. 使用 AWS DataSync 將資料從內部環境遷移到 S3 bucket
- C. 使用 AWS Snowball 將資料搬到 S3 bucket
- D. 建立從內部到 AWS 的 IPsec VPN，再使用 AWS CLI 的 `s3 cp`

**答案**  
B

## Question #483
**題目**  
某公司把一個在 Windows container 中執行的 .NET 6 工作做了容器化，現在想在 AWS Cloud 上執行。此工作每 10 分鐘跑一次，每次執行時間介於 1 到 3 分鐘。哪個方案最具成本效益？

**選項**
- A. 以該 container image 建立 AWS Lambda function，並用 Amazon EventBridge 每 10 分鐘觸發
- B. 使用 AWS Batch 建立 job，並以 AWS Fargate 執行，每 10 分鐘排程一次
- C. 使用 Amazon ECS on AWS Fargate 執行，並建立 scheduled task 每 10 分鐘執行一次
- D. 使用 Amazon ECS on AWS Fargate 執行 standalone task，再用 Windows task scheduler 每 10 分鐘觸發

**答案**  
A

## Question #484
**題目**  
某公司想從多個獨立 AWS 帳號，轉成整合式多帳號架構，並將為不同事業單位建立許多新 AWS 帳號。公司希望以集中式企業目錄服務來驗證這些 AWS 帳號的存取。solutions architect 應建議哪兩個動作？（選兩項）

**選項**
- A. 在 AWS Organizations 建立啟用 all features 的 organization，並在其中建立新 AWS 帳號
- B. 建立 Amazon Cognito identity pool，並讓 AWS IAM Identity Center 接受 Cognito 驗證
- C. 建立 SCP 管理 AWS 帳號，並把 IAM Identity Center 加到 AWS Directory Service
- D. 建立新的 organization，並直接用 AWS Directory Service 作為組織驗證機制
- E. 在 organization 中設定 AWS IAM Identity Center，並整合公司的企業目錄服務

**答案**  
AE

## Question #485
**題目**  
某公司要把舊新聞影片存成 AWS 上的影片封存。公司希望盡可能降低成本，而且很少需要還原這些檔案；但一旦需要時，最多 5 分鐘內必須可取得。哪個方案最具成本效益？

**選項**
- A. 存在 Amazon S3 Glacier，使用 Expedited retrieval
- B. 存在 Amazon S3 Glacier，使用 Standard retrieval
- C. 存在 Amazon S3 Standard-IA
- D. 存在 Amazon S3 One Zone-IA

**答案**  
C

## Question #486
**題目**  
某公司正在 AWS 上建立三層式應用程式。展示層提供靜態網站；邏輯層為容器化應用程式；此應用程式會把資料存進關聯式資料庫。公司希望簡化部署並降低營運成本。哪個方案符合需求？

**選項**
- A. 用 Amazon S3 代管靜態內容；運算使用 Amazon ECS with AWS Fargate；資料庫使用受管 Amazon RDS cluster
- B. 用 Amazon CloudFront 代管靜態內容；運算使用 Amazon ECS on Amazon EC2；資料庫使用受管 Amazon RDS cluster
- C. 用 Amazon S3 代管靜態內容；運算使用 Amazon EKS with AWS Fargate；資料庫使用受管 Amazon RDS cluster
- D. 用 Amazon EC2 Reserved Instances 代管靜態內容；運算使用 Amazon EKS on Amazon EC2；資料庫使用受管 Amazon RDS cluster

**答案**  
A

## Question #487
**題目**  
某公司需要一套儲存方案，必須高可用、可擴充，而且要能作為檔案系統，透過原生協定同時掛載到 AWS 與內部環境中的多台 Linux instances 上，且沒有最小容量限制。公司已建立從內部網路到 VPC 的 Site-to-Site VPN。哪個儲存方案符合需求？

**選項**
- A. Amazon FSx Multi-AZ deployments
- B. Amazon EBS Multi-Attach volumes
- C. Amazon EFS 搭配多個 mount targets
- D. Amazon EFS 搭配單一 mount target 與多個 access points

**答案**  
C

## Question #488
**題目**  
某成立 4 年的媒體公司使用 AWS Organizations all features 來管理 AWS 帳號。財務團隊要求 member accounts 的帳單資訊不可被任何人存取，包括 member account 的 root user。哪個方案符合需求？

**選項**
- A. 將財務人員加入 IAM 群組並附加 Billing AWS managed policy
- B. 建立 identity-based policy，拒絕所有使用者（包含 root user）存取帳單資訊
- C. 建立 SCP 拒絕存取帳單資訊，並套用到 root OU
- D. 從 Organizations all features 改成 consolidated billing feature set

**答案**  
C

## Question #489
**題目**  
某電商公司在 AWS Cloud 上執行應用程式，並整合了內部倉儲系統。公司使用 Amazon SNS 將訂單訊息傳送到內部 HTTPS endpoint，供倉儲系統處理。但內部資料中心團隊發現有些訂單訊息沒有收到。solutions architect 需要保留未成功傳遞的訊息，並可在 14 天內分析這些訊息，同時希望開發工作最少。哪個方案符合需求？

**選項**
- A. 為 Amazon SNS 設定 dead-letter queue，目標為 Amazon Kinesis Data Stream，保留期 14 天
- B. 在應用程式與 Amazon SNS 之間新增 Amazon SQS queue，保留期 14 天
- C. 為 Amazon SNS 設定 dead-letter queue，目標為 Amazon SQS queue，保留期 14 天
- D. 為 Amazon SNS 設定 dead-letter queue，目標為 Amazon DynamoDB，並設定 TTL 為 14 天

**答案**  
C

## Question #490
**題目**  
某遊戲公司使用 Amazon DynamoDB 儲存使用者資訊，例如地理位置、玩家資料與排行榜。公司需要以最少程式碼將持續備份輸出到 Amazon S3 bucket，且不能影響應用程式可用性，也不能影響資料表已設定的 read capacity units (RCUs)。哪個方案符合需求？

**選項**
- A. 使用 Amazon EMR 與 Apache Hive job 備份到 Amazon S3
- B. 直接把 DynamoDB 匯出到 Amazon S3，並為資料表啟用 point-in-time recovery
- C. 啟用 DynamoDB Streams，並用 AWS Lambda 讀取 stream 後匯出到 S3
- D. 定期用 AWS Lambda 將資料匯出到 S3，並啟用 point-in-time recovery

**答案**  
B

## Question #491
**題目**  
某 solutions architect 正在為銀行設計一個非同步應用程式，用來處理信用卡資料驗證請求。應用程式必須安全，且每筆請求至少要被處理一次。哪個方案最具成本效益？

**選項**
- A. 使用 AWS Lambda event source mapping，來源為 Amazon SQS standard queue；加密使用 SSE-KMS，並在 Lambda execution role 加上 `kms:Decrypt`
- B. 使用 AWS Lambda event source mapping，來源為 Amazon SQS FIFO queue；加密使用 SSE-SQS，並給 Lambda 加密金鑰呼叫權限
- C. 使用 AWS Lambda event source mapping，來源為 Amazon SQS FIFO queue；加密使用 SSE-KMS，並在 Lambda execution role 加上 `kms:Decrypt`
- D. 使用 AWS Lambda event source mapping，來源為 Amazon SQS standard queue；加密使用 SSE-KMS，並給 Lambda 加密金鑰呼叫權限

**答案**  
A

## Question #492
**題目**  
某公司有多個 AWS 帳號用於開發。有些員工經常建立過大的 Amazon EC2 instance，導致開發帳號超出年度預算。公司希望以集中方式限制這些帳號建立 AWS 資源。哪個方案需要的開發工作最少？

**選項**
- A. 開發 AWS Systems Manager templates，使用核准流程建立 EC2 instances
- B. 使用 AWS Organizations 將帳號整理到 OUs，並透過 SCP 控制可使用的 EC2 instance types
- C. 建立 Amazon EventBridge 規則，在建立 EC2 instance 時觸發 AWS Lambda，停止不允許的 instance types
- D. 建立 AWS Service Catalog products，只允許員工透過這些產品建立核准的 EC2 instance types

**答案**  
B

## Question #493
**題目**  
某公司想使用 AI 判斷客服通話品質。目前通話有四種語言（包含英文），未來還會增加新語言。公司沒有資源定期維護 ML models，且需要從客服通話錄音產出書面情緒分析報告，同時將文字翻譯成英文。哪一組步驟符合需求？（選三項）

**選項**
- A. 使用 Amazon Comprehend 將錄音翻譯成英文
- B. 使用 Amazon Lex 產生書面情緒分析報告
- C. 使用 Amazon Polly 將錄音轉成文字
- D. 使用 Amazon Transcribe 將任意語言的錄音轉成文字
- E. 使用 Amazon Translate 將任意語言文字翻譯成英文
- F. 使用 Amazon Comprehend 產生情緒分析報告

**答案**  
DEF

## Question #494
**題目**  
某公司使用 Amazon EC2 instances 代管內部系統。一次部署作業中，管理員嘗試用 AWS CLI 終止某台 EC2 instance，卻收到 403 (Access Denied)。該管理員使用的 IAM role 已附加題目中的 IAM policy。為何請求失敗？

**選項**
- A. 該 EC2 instance 有 resource-based policy，內含 Deny statement
- B. policy statement 中沒有指定 principal
- C. `Action` 欄位沒有授予終止 EC2 instance 所需動作
- D. 終止 EC2 instance 的請求來源不是來自 `192.0.2.0/24` 或 `203.0.113.0/24`

**答案**  
D

## Question #495
**題目**  
某公司正在進行內部稽核，希望確認與 AWS Lake Formation data lake 相關的 Amazon S3 bucket 中，不含敏感客戶資料或員工資料。公司要找出 personally identifiable information (PII) 或金融資料，例如護照號碼與信用卡號。哪個方案符合需求？

**選項**
- A. 在帳號中設定 AWS Audit Manager，並選擇 PCI DSS 作為稽核標準
- B. 為 S3 bucket 設定 Amazon S3 Inventory，再用 Amazon Athena 查詢 inventory
- C. 設定 Amazon Macie，執行使用 managed identifiers 的 data discovery job
- D. 使用 Amazon S3 Select 對整個 S3 bucket 執行報表查詢

**答案**  
C

## Question #496
**題目**  
某公司使用內部伺服器代管應用程式，現在儲存容量不足。應用程式同時使用 block storage 與 NFS storage。公司需要高效能方案、支援本地快取，且不想重新設計既有應用。solutions architect 應採取哪兩個動作？（選兩項）

**選項**
- A. 將 Amazon S3 掛載成檔案系統到內部伺服器
- B. 部署 AWS Storage Gateway file gateway 取代 NFS storage
- C. 部署 AWS Snowball Edge 來提供 NFS mount
- D. 部署 AWS Storage Gateway volume gateway 取代 block storage
- E. 部署 Amazon EFS 並掛載到內部伺服器

**答案**  
BD

## Question #497
**題目**  
某公司有一個服務，會在同一個 AWS Region 中，從 Amazon S3 bucket 讀寫大量資料。此服務部署在 VPC 私有子網的 Amazon EC2 instances 上，目前是透過公有子網中的 NAT gateway 與 Amazon S3 通訊。公司想降低資料輸出成本。哪個方案最具成本效益？

**選項**
- A. 在公有子網建立專用 EC2 NAT instance，並讓私有子網 route table 將所有 S3 流量導向該 ENI
- B. 在私有子網建立專用 EC2 NAT instance，並讓公有子網 route table 將所有 S3 流量導向該 ENI
- C. 建立 VPC gateway endpoint，並讓私有子網 route table 將所有 S3 流量導向該 endpoint
- D. 再建立第二個 NAT gateway，並讓私有子網 route table 將所有 S3 流量導向該 NAT gateway

**答案**  
C

## Question #498
**題目**  
某公司使用 Amazon S3 儲存高解析度圖片。為了盡量少改應用程式，圖片都以同一個 S3 物件的最新版本儲存。公司只需要保留最近兩個版本，並希望降低成本。S3 bucket 已被辨識為主要花費來源。哪個方案在管理成本最低的情況下可降低 S3 成本？

**選項**
- A. 使用 S3 Lifecycle 刪除過期版本，只保留最近兩個版本
- B. 使用 AWS Lambda 檢查舊版本並刪除，只保留最近兩個版本
- C. 使用 S3 Batch Operations 刪除 noncurrent object versions，只保留最近兩個版本
- D. 關閉 S3 bucket 的 versioning，只保留最近兩個版本

**答案**  
A

## Question #499
**題目**  
某公司想降低其 1 Gbps AWS Direct Connect connection 的成本。目前平均使用率不到 10%。solutions architect 必須提出既能降低成本、又不影響安全性的方案。哪個方案符合需求？

**選項**
- A. 再建立一條新的 1 Gbps Direct Connect connection，並與其他 AWS 帳號共用
- B. 在 AWS Management Console 中建立新的 200 Mbps Direct Connect connection
- C. 聯絡 AWS Direct Connect Partner 申請 1 Gbps connection，並與其他 AWS 帳號共用
- D. 聯絡 AWS Direct Connect Partner，為既有 AWS 帳號申請 200 Mbps hosted connection

**答案**  
B

## Question #500
**題目**  
某公司在內部環境有多台 Windows file servers，現在想把檔案遷移並整併到 Amazon FSx for Windows File Server。必須保留檔案權限，確保存取權限不變。哪兩個方案符合需求？（選兩項）

**選項**
- A. 在內部部署 AWS DataSync agents，並排程 DataSync tasks 將資料傳到 Amazon FSx for Windows File Server
- B. 先用 AWS CLI 把各 file server 的 shares 複製到 Amazon S3 buckets，再排程 DataSync tasks 傳到 Amazon FSx for Windows File Server
- C. 將各 file server 的硬碟拆下寄到 AWS 匯入 Amazon S3，再排程 DataSync tasks 傳到 Amazon FSx for Windows File Server
- D. 訂購 AWS Snowcone，接到內部網路後，在裝置上執行 AWS DataSync agents，並排程 DataSync tasks 傳到 Amazon FSx for Windows File Server
- E. 訂購 AWS Snowball Edge Storage Optimized，連到內部網路後用 AWS CLI 將資料複製進裝置，再寄回 AWS 匯入 Amazon S3，之後排程 DataSync tasks 傳到 Amazon FSx for Windows File Server

**答案**  
AD
