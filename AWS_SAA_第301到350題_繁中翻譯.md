# AWS SAA 第 301～350 題繁中翻譯

僅保留：題號、題目、選項、答案。

## Question #301

**題目**  
某大學研究實驗室需要將 30 TB 資料，從內部部署的 Windows 檔案伺服器遷移到 Amazon FSx for Windows File Server。實驗室只有一條 1 Gbps 網路連線，且該連線由校內多個部門共用。實驗室希望導入一個資料遷移服務，在最大化資料傳輸效能的同時，也能控制服務所使用的頻寬，避免影響其他部門。此次資料遷移必須在接下來 5 天內完成。哪一個 AWS 解決方案最符合這些需求？

**選項**
- A. AWS Snowcone
- B. Amazon FSx File Gateway
- C. AWS DataSync
- D. AWS Transfer Family

**答案**  
C

## Question #302

**題目**  
某公司想建立一個行動 App，讓使用者可以在行動裝置上串流播放慢動作影片片段。目前，App 會先擷取影片片段，再將原始格式的影片上傳到 Amazon S3 bucket，並直接從 S3 bucket 取回影片。不過原始格式影片檔案很大，導致使用者在行動裝置上發生緩衝與播放問題。公司希望在最低營運負擔下，提升 App 的效能與可擴展性。哪一組解決方案最符合這些需求？（選兩項）

**選項**
- A. 部署 Amazon CloudFront 進行內容傳遞與快取
- B. 使用 AWS DataSync 將影片檔複寫到其他 AWS Region 的 S3 bucket
- C. 使用 Amazon Elastic Transcoder 將影片檔轉換成更合適的格式
- D. 在 Local Zones 部署 Amazon EC2 Auto Scaling 群組，以進行內容傳遞與快取
- E. 部署 Amazon EC2 Auto Scaling 群組，將影片檔轉換為更合適的格式

**答案**  
A

## Question #303

**題目**  
某公司即將上線一個新應用程式，該應用程式部署在 Amazon Elastic Container Service (Amazon ECS) 叢集上，並使用 Fargate 作為 ECS task 的啟動類型。公司正在監控 CPU 與記憶體使用量，因為預期應用程式上線後流量會很高；但當使用率下降時，公司也希望能降低成本。解決方案架構師應該建議什麼做法？

**選項**
- A. 使用 Amazon EC2 Auto Scaling，依過去流量模式在特定時段進行擴縮
- B. 使用 AWS Lambda function，根據觸發 Amazon CloudWatch alarm 的指標異常來調整 Amazon ECS 規模
- C. 使用 Amazon EC2 Auto Scaling 搭配 simple scaling policy，在 ECS 指標異常觸發 Amazon CloudWatch alarm 時進行擴縮
- D. 使用 AWS Application Auto Scaling 搭配 target tracking policy，在 ECS 指標異常觸發 Amazon CloudWatch alarm 時進行擴縮

**答案**  
D

## Question #304

**題目**  
某公司最近在另一個 AWS Region 建立了災難復原站點。公司需要定期在兩個 Region 的 NFS 檔案系統之間，雙向傳輸大量資料。哪一個解決方案能以最低營運負擔滿足這些需求？

**選項**
- A. 使用 AWS DataSync
- B. 使用 AWS Snowball 裝置
- C. 在 Amazon EC2 上架設 SFTP server
- D. 使用 AWS Database Migration Service (AWS DMS)

**答案**  
A

## Question #305

**題目**  
某公司正在為部署於 AWS Cloud 的遊戲應用程式設計共用儲存方案。公司需要能讓 SMB client 存取資料，而且解決方案必須是全受管。哪一個 AWS 解決方案符合這些需求？

**選項**
- A. 建立一個 AWS DataSync task，將資料分享成可掛載的檔案系統，並將該檔案系統掛載到應用程式伺服器
- B. 建立一台 Amazon EC2 Windows instance，安裝並設定 Windows file share 角色，然後讓應用程式連線使用
- C. 建立 Amazon FSx for Windows File Server 檔案系統，並將其連接到來源伺服器，再讓應用程式伺服器連線使用
- D. 建立 Amazon S3 bucket，將 IAM role 指派給應用程式以存取 S3 bucket，並將 S3 bucket 掛載到應用程式伺服器

**答案**  
C

## Question #306

**題目**  
某公司要為執行於 Amazon EC2 執行個體上的低延遲敏感型應用程式，部署一個記憶體內資料庫。該應用程式每分鐘處理超過 100,000 筆交易，且需要高網路吞吐量。解決方案架構師需要設計一個兼顧成本效益的網路架構，同時將資料傳輸費用降到最低。哪一個解決方案最符合需求？

**選項**
- A. 在同一個 AWS Region、同一個 Availability Zone 內啟動所有 EC2 執行個體，並指定使用 cluster strategy 的 placement group
- B. 在同一個 AWS Region 的不同 Availability Zone 啟動所有 EC2 執行個體，並指定使用 partition strategy 的 placement group
- C. 部署一個 Auto Scaling group，依網路使用率目標在不同 Availability Zone 啟動 EC2 執行個體
- D. 部署一個具有 step scaling policy 的 Auto Scaling group，在不同 Availability Zone 啟動 EC2 執行個體

**答案**  
A

## Question #307

**題目**  
某公司主要在內部部署執行應用程式伺服器，現在決定遷移到 AWS。公司希望盡量減少在內部部署擴充 Internet Small Computer Systems Interface (iSCSI) 儲存設備的需求，並只讓最近被存取的資料保留在本地端。公司應該使用哪一個 AWS 解決方案？

**選項**
- A. Amazon S3 File Gateway
- B. AWS Storage Gateway Tape Gateway
- C. AWS Storage Gateway Volume Gateway stored volumes
- D. AWS Storage Gateway Volume Gateway cached volumes

**答案**  
A

## Question #308

**題目**  
某公司有多個 AWS account，並使用 consolidated billing。公司在 90 天內運行了多個活躍的高效能 Amazon RDS for Oracle On-Demand DB instance。財務團隊可存取 consolidated billing account 與其他所有 AWS account 的 AWS Trusted Advisor。財務團隊需要使用正確的 AWS account 來查看與 RDS 相關的 Trusted Advisor 建議，並檢視適當的檢查項目以降低 RDS 成本。哪兩個步驟最符合這些需求？（選兩項）

**選項**
- A. 從執行 RDS 執行個體的 account 查看 Trusted Advisor 建議
- B. 從 consolidated billing account 查看 Trusted Advisor 建議，以同時看到所有 RDS instance 檢查結果
- C. 檢視 Amazon RDS Reserved Instance Optimization 的 Trusted Advisor 檢查項目
- D. 檢視 Amazon RDS Idle DB Instances 的 Trusted Advisor 檢查項目
- E. 檢視 Amazon Redshift Reserved Node Optimization 的 Trusted Advisor 檢查項目

**答案**  
AC

## Question #309

**題目**  
某位解決方案架構師需要最佳化儲存成本，並找出那些已經不再被存取或很少被存取的 Amazon S3 bucket。哪一個解決方案能以最低營運負擔完成這個目標？

**選項**
- A. 使用 S3 Storage Lens dashboard 的 advanced activity metrics 分析 bucket 存取模式
- B. 使用 AWS Management Console 中的 S3 dashboard 分析 bucket 存取模式
- C. 為 bucket 啟用 Amazon CloudWatch BucketSizeBytes metric，並使用指標資料分析存取模式
- D. 為 S3 object monitoring 啟用 AWS CloudTrail，並使用整合後的 CloudTrail log 分析 bucket 存取模式

**答案**  
D

## Question #310

**題目**  
某公司販售資料集給從事人工智慧與機器學習（AI/ML）研究的客戶。這些資料集是大型格式化檔案，儲存在 us-east-1 Region 的 Amazon S3 bucket 中。公司另有一個 Web 應用程式，部署在多台 Amazon EC2 執行個體並透過 Application Load Balancer 提供服務，供客戶購買特定資料集的存取權。完成購買後，客戶會收到一個可存取檔案的 S3 signed URL。客戶主要分布在北美與歐洲。公司希望降低資料傳輸成本，並維持或提升效能。解決方案架構師應該怎麼做？

**選項**
- A. 在既有 S3 bucket 上啟用 S3 Transfer Acceleration，並將客戶請求導向 S3 Transfer Acceleration endpoint
- B. 部署一個 Amazon CloudFront distribution，並以既有 S3 bucket 作為 origin，將客戶請求導向 CloudFront URL
- C. 在 eu-central-1 Region 再建立一個 S3 bucket，並使用 S3 Cross-Region Replication 複寫資料，再將客戶請求導向該 bucket
- D. 修改 Web 應用程式，改由應用程式直接串流資料集給終端使用者，並讓應用程式從 S3 bucket 讀取資料

**答案**  
B

## Question #311

**題目**  
某公司正在使用 AWS 設計一個用來處理保險報價的 Web 應用程式。使用者會向應用程式提出報價請求。報價必須依報價類型分流、必須在 24 小時內回應，而且不能遺失。此解決方案必須最大化營運效率並最小化維護負擔。哪一個解決方案符合這些需求？

**選項**
- A. 依照報價類型建立多個 Amazon Kinesis data stream，並讓 Web 應用程式將訊息送到對應的 stream
- B. 針對每一種報價類型建立一個 AWS Lambda function 與一個 Amazon Simple Notification Service (Amazon SNS) topic，並訂閱對應的處理流程
- C. 建立單一 Amazon Simple Notification Service (Amazon SNS) topic，並訂閱多個 Amazon Simple Queue Service (Amazon SQS) queue
- D. 依照報價類型建立多個 Amazon Kinesis Data Firehose delivery stream，將資料串流傳送至 Amazon ...

**答案**  
D

## Question #312

**題目**  
某公司的應用程式執行於多台 Amazon EC2 執行個體上，每台 EC2 執行個體都掛載了多個 Amazon Elastic Block Store (Amazon EBS) data volume。公司的應用程式 EC2 instance 設定與資料都需要每晚備份，且應用程式還必須能在另一個 AWS Region 中復原。哪一個解決方案能以最高營運效率滿足這些需求？

**選項**
- A. 撰寫 AWS Lambda function，排程在夜間建立 EBS volume snapshot，並將 snapshot 複製到另一個 Region
- B. 使用 AWS Backup 建立 backup plan 進行每晚備份，並複製備份到另一個 Region；將應用程式的 EC2 執行個體加入備份範圍
- C. 使用 AWS Backup 建立 backup plan 進行每晚備份，並複製備份到另一個 Region；將應用程式的 EBS volume 加入備份範圍
- D. 撰寫 AWS Lambda function，排程在夜間建立 EBS volume snapshot，並將 snapshot 複製到另一個 Region

**答案**  
C

## Question #313

**題目**  
某公司正在 AWS 上建置一個行動 App，並希望將服務擴展到數百萬使用者。公司需要建立一個平台，讓已授權的使用者可以在行動裝置上觀看公司內容。解決方案架構師應該建議哪一種做法？

**選項**
- A. 將內容發布到公開的 Amazon S3 bucket，並使用 AWS Key Management Service (AWS KMS) key 進行串流
- B. 在行動 App 與 AWS 環境之間建立 IPsec VPN 來串流內容
- C. 使用 Amazon CloudFront，並提供 signed URL 來串流內容
- D. 在行動 App 與 AWS 環境之間建立 AWS Client VPN 來串流內容

**答案**  
C

## Question #314

**題目**  
某公司有一套供全球業務團隊使用的內部部署 MySQL 資料庫，其存取模式並不頻繁，但業務團隊要求資料庫停機時間必須極低。資料庫管理員想將這個資料庫遷移到 AWS，同時又不想先預先選定特定執行個體類型，因為未來使用者可能變多。解決方案架構師應該建議哪一項服務？

**選項**
- A. Amazon Aurora MySQL
- B. Amazon Aurora Serverless for MySQL
- C. Amazon Redshift Spectrum
- D. Amazon RDS for MySQL

**答案**  
B

## Question #315

**題目**  
某公司曾發生資安事件，影響了其內部部署資料中心中的多個應用程式。攻擊者利用了執行於伺服器上的自訂應用程式漏洞。公司目前正將這些應用程式遷移到 Amazon EC2 執行個體上。公司希望導入一個能主動掃描 EC2 執行個體漏洞，並輸出包含發現結果報告的解決方案。哪一個解決方案符合這些需求？

**選項**
- A. 部署 AWS Shield 掃描 EC2 執行個體漏洞，並建立 AWS Lambda function 將結果記錄到 AWS CloudTrail
- B. 部署 Amazon Macie 與 AWS Lambda function 掃描 EC2 執行個體漏洞，並將結果記錄到 AWS CloudTrail
- C. 啟用 Amazon GuardDuty，將 GuardDuty agent 部署到 EC2 執行個體，並設定 AWS Lambda function 自動化處理
- D. 啟用 Amazon Inspector，將 Amazon Inspector agent 部署到 EC2 執行個體，並設定 AWS Lambda function 自動化處理

**答案**  
C

## Question #316

**題目**  
某公司使用一台 Amazon EC2 執行個體執行腳本，以輪詢並處理 Amazon Simple Queue Service (Amazon SQS) queue 中的訊息。公司希望在維持可處理日益增加訊息量能力的同時，降低營運成本。解決方案架構師應該建議什麼做法？

**選項**
- A. 升級 EC2 執行個體規格，以更快處理訊息
- B. 使用 Amazon EventBridge 在 EC2 執行個體使用率過低時將其關閉
- C. 將 EC2 執行個體上的腳本遷移到具有適當 runtime 的 AWS Lambda function
- D. 使用 AWS Systems Manager Run Command 依需求執行該腳本

**答案**  
A

## Question #317

**題目**  
某公司使用一套舊系統產生 CSV 格式資料，並將輸出資料存放到 Amazon S3。公司正部署一套新的商用現成（COTS）應用程式，該程式只能分析儲存在 Amazon Redshift 與 Amazon S3 中的資料，且能執行複雜 SQL 查詢；但這套 COTS 應用程式無法處理舊系統產生的 .csv 檔案。公司也無法修改舊系統以輸出其他格式。公司需要在最低營運負擔下，讓 COTS 應用程式可以使用這些資料。哪一個解決方案最符合需求？

**選項**
- A. 建立一個定期執行的 AWS Glue extract, transform, and load (ETL) job，將 .csv 檔處理並 ...
- B. 開發一支在 Amazon EC2 執行個體上執行的 Python script，將 .csv 檔轉為 .sql 檔，並以 cron 排程執行
- C. 建立 AWS Lambda function 與 Amazon DynamoDB table，透過 S3 event 觸發 Lambda function，並設定 ...
- D. 使用 Amazon EventBridge 每週啟動一個 Amazon EMR cluster，執行 extract、transform 與 load 流程

**答案**  
A

## Question #318

**題目**  
某公司最近已將整個 IT 環境遷移到 AWS Cloud。公司發現使用者會部署過大規格的 Amazon EC2 執行個體，並在未經適當變更流程下修改 security group 規則。解決方案架構師必須擬定一套策略，用來追蹤並稽核這些資產與設定變更。哪兩項做法最符合需求？（選兩項）

**選項**
- A. 啟用 AWS CloudTrail 並用於稽核
- B. 為 Amazon EC2 執行個體使用 data lifecycle policy
- C. 啟用 AWS Trusted Advisor 並參考其 security dashboard
- D. 啟用 AWS Config，並建立稽核與合規用途的規則
- E. 使用 AWS CloudFormation template 還原先前的資源設定

**答案**  
AD

## Question #319

**題目**  
某公司在 AWS Cloud 中有數百台以 Linux 為基礎的 Amazon EC2 執行個體。系統管理員一直使用共用 SSH key 來管理這些執行個體。近期稽核後，公司資安團隊要求移除所有共用 key。解決方案架構師必須設計一套能安全存取 EC2 執行個體的方案，且管理負擔要最低。哪一個解決方案最符合需求？

**選項**
- A. 使用 AWS Systems Manager Session Manager 連線到 EC2 執行個體
- B. 使用 AWS Security Token Service (AWS STS) 依需求產生一次性 SSH key
- C. 允許一組 bastion instance 使用共用 SSH 存取，再將其他執行個體設定為僅允許來自 bastion instance 的 SSH 連線
- D. 使用 Amazon Cognito custom authorizer 驗證使用者，並呼叫 AWS Lambda function 產生暫時性 SSH key

**答案**  
B

## Question #320

**題目**  
某公司使用一組 Amazon EC2 執行個體，從內部部署資料來源擷取資料。資料格式為 JSON，擷取速率最高可達 1 MB/s。當 EC2 執行個體重新開機時，傳輸中的資料會遺失。公司的資料科學團隊希望能近乎即時查詢已擷取的資料。哪一個解決方案能以可擴展且最小資料遺失的方式，提供近乎即時的資料查詢？

**選項**
- A. 將資料發布到 Amazon Kinesis Data Streams，並使用 Kinesis Data Analytics 查詢資料
- B. 將資料發布到 Amazon Kinesis Data Firehose，目的地設為 Amazon Redshift，並使用 Amazon Redshift 查詢資料
- C. 將擷取資料存放在 EC2 instance store，再將資料發布到 Amazon Kinesis Data Firehose，目的地為 Amazon S3，並使用 ...
- D. 將擷取資料存放在 Amazon Elastic Block Store (Amazon EBS) volume，再發布到 Amazon ElastiCache for Redis，並訂閱 ...

**答案**  
A

## Question #321

**題目**  
解決方案架構師應該怎麼做，才能確保所有上傳到 Amazon S3 bucket 的物件都已加密？

**選項**
- A. 更新 bucket policy，若 PutObject 沒有設定 s3:x-amz-acl header，則拒絕
- B. 更新 bucket policy，若 PutObject 沒有將 s3:x-amz-acl header 設為 private，則拒絕
- C. 更新 bucket policy，若 PutObject 沒有將 aws:SecureTransport header 設為 true，則拒絕
- D. 更新 bucket policy，若 PutObject 沒有設定 x-amz-server-side-encryption header，則拒絕

**答案**  
D

## Question #322

**題目**  
某位解決方案架構師正在為一家公司設計多層式應用程式。使用者會從行動裝置上傳圖片，系統需要為每張圖片產生縮圖，並回傳訊息給使用者，確認圖片已成功上傳。縮圖產生可能需要長達 60 秒，但公司希望能更快通知使用者原始圖片已經收到。解決方案架構師必須設計一套能在不同應用層之間非同步派送請求的架構。應該怎麼做？

**選項**
- A. 撰寫自訂 AWS Lambda function 產生縮圖並通知使用者，以上傳流程作為事件來源
- B. 建立 AWS Step Functions workflow，由 Step Functions 協調各應用層並通知使用者
- C. 建立 Amazon Simple Queue Service (Amazon SQS) message queue。當圖片上傳後，將訊息放入 SQS queue 中
- D. 建立 Amazon Simple Notification Service (Amazon SNS) topic 與 subscription，並使用其中一個 subscription ...

**答案**  
C

## Question #323

**題目**  
某公司的辦公設施在每個入口都設有 badge reader。當 badge 被刷取時，讀卡機會透過 HTTPS 傳送訊息，指出是誰嘗試進入哪個入口。解決方案架構師必須設計一套系統來處理這些來自感測器的訊息。此解決方案必須具備高可用性，且結果必須能提供給公司資安團隊分析。解決方案架構師應建議哪種系統架構？

**選項**
- A. 啟動一台 Amazon EC2 執行個體作為 HTTPS endpoint 並處理訊息，然後將資料儲存 ...
- B. 在 Amazon API Gateway 建立 HTTPS endpoint，並設定該 endpoint 呼叫 AWS Lambda function 來處理訊息
- C. 使用 Amazon Route 53 將感測器訊息導向 AWS Lambda function，並由 Lambda function 處理 ...
- D. 為 Amazon S3 建立 gateway VPC endpoint，並從設施網路到 VPC 建立 Site-to-Site VPN ...

**答案**  
B

## Question #324

**題目**  
某公司想為主要的內部部署檔案儲存磁碟區建立災難復原計畫。該檔案儲存磁碟區是從本地儲存伺服器上的 Internet Small Computer Systems Interface (iSCSI) 裝置掛載而來，且持有數百 TB 的資料。公司希望確保終端使用者仍可從內部部署系統立即存取所有類型的檔案，且不會感受到延遲，同時又希望盡量少變更現有基礎設施。哪一個解決方案最符合需求？

**選項**
- A. 以 VM 形式在內部部署環境中建置 Amazon S3 File Gateway，將本地快取設為 10 TB，並修改 ...
- B. 建置 AWS Storage Gateway tape gateway，使用備份解決方案將所有既有資料備份到虛擬磁帶庫
- C. 建置 AWS Storage Gateway Volume Gateway cached volume，將本地快取設為 10 TB，並將 cached volume 掛載 ...
- D. 建置 AWS Storage Gateway Volume Gateway stored volume，並配置與現有檔案儲存相同大小的磁碟空間

**答案**  
C

## Question #325

**題目**  
某公司從 Amazon S3 bucket 提供一個 Web 應用程式。此應用程式使用 Amazon Cognito 作為身分提供者，驗證使用者後回傳 JSON Web Token (JWT)，讓使用者可以存取儲存在另一個 S3 bucket 中的受保護資源。應用程式部署完成後，使用者回報發生錯誤，無法存取受保護內容。解決方案架構師必須提供正確權限來解決此問題。哪一個解決方案最符合需求？

**選項**
- A. 更新 Amazon Cognito identity pool，讓其在存取受保護內容時能假設正確的 IAM role
- B. 更新 S3 ACL，允許應用程式存取受保護內容
- C. 重新將應用程式部署到 Amazon S3，以避免 S3 bucket 的 eventually consistent read 影響存取
- D. 更新 Amazon Cognito pool，使用 identity pool 中的自訂屬性對應，並授與使用者正確權限

**答案**  
A

## Question #326

**題目**  
某影像託管公司會將大型資產上傳到 Amazon S3 Standard bucket。公司使用 S3 API 進行平行 multipart upload，若上傳相同物件則會直接覆寫。物件在上傳後前 30 天會被頻繁存取，30 天後存取頻率降低，但每個物件的存取模式並不一致。公司必須在維持高可用性與高耐久性的前提下，最佳化 S3 儲存成本。解決方案架構師應建議哪兩項作法？（選兩項）

**選項**
- A. 30 天後將資產移轉到 S3 Intelligent-Tiering
- B. 設定 S3 Lifecycle policy 清理未完成的 multipart upload
- C. 設定 S3 Lifecycle policy 清理過期的 object delete marker
- D. 30 天後將資產移轉到 S3 Standard-Infrequent Access (S3 Standard-IA)
- E. 30 天後將資產移轉到 S3 One Zone-Infrequent Access (S3 One Zone-IA)

**答案**  
AB

## Question #327

**題目**  
某位解決方案架構師必須保護一個承載 Amazon EC2 執行個體的 VPC 網路。這些 EC2 執行個體儲存高度敏感資料，並執行於私有子網路中。依公司政策，VPC 中的 EC2 執行個體只能透過第三方提供的 URL，存取經核准的第三方軟體儲存庫，以取得產品更新；其他所有對外網際網路流量都必須被阻擋。哪一個解決方案符合需求？

**選項**
- A. 更新私有子網路的 route table，將對外流量導向 AWS Network Firewall，並設定 domain list ...
- B. 建立 AWS WAF web ACL，使用自訂規則依來源與目的地 IP 範圍過濾流量
- C. 實作嚴格的 inbound security group 規則，並設定 outbound 規則，只允許流量前往授權的軟體儲存庫
- D. 在 EC2 執行個體前方設定 Application Load Balancer (ALB)，將所有 outbound 流量導向 ALB，並使用 URL 型規則 ...

**答案**  
A

## Question #328

**題目**  
某公司在 AWS Cloud 上託管一個三層式電子商務應用程式。網站託管於 Amazon S3，並整合一個處理銷售請求的 API。該 API 執行於三台位於 Application Load Balancer (ALB) 後方的 Amazon EC2 執行個體上。API 包含靜態與動態前端內容，以及會非同步處理銷售請求的後端 worker。公司預期在新產品發表活動期間，銷售請求數量會突然大幅增加。解決方案架構師應建議什麼做法，以確保所有請求都能成功處理？

**選項**
- A. 為動態內容新增 Amazon CloudFront distribution，並增加 EC2 執行個體數量以處理流量增加
- B. 為靜態內容新增 Amazon CloudFront distribution，並將 EC2 執行個體放入 Auto Scaling group 以啟動新執行個體
- C. 為動態內容新增 Amazon CloudFront distribution，並在 ALB 前加入 Amazon ElastiCache instance 以降低 ...
- D. 為靜態內容新增 Amazon CloudFront distribution，並加入 Amazon Simple Queue Service (Amazon SQS) queue 以接收 ...

**答案**  
D

## Question #329

**題目**  
某次資安稽核發現 Amazon EC2 執行個體未定期進行修補。解決方案架構師需要提供一個方案，能夠對大量 EC2 執行個體定期執行安全掃描，並依固定排程修補 EC2 執行個體，同時提供每台執行個體的修補狀態報告。哪一個解決方案最符合需求？

**選項**
- A. 設定 Amazon Macie 掃描 EC2 執行個體的軟體漏洞，並在每台 EC2 上設定 cron job 進行修補
- B. 在 account 中啟用 Amazon GuardDuty，設定 GuardDuty 掃描 EC2 執行個體軟體漏洞，並設定 AWS ...
- C. 設定 Amazon Detective 掃描 EC2 執行個體軟體漏洞，並建立 Amazon EventBridge 排程規則進行修補
- D. 在 account 中啟用 Amazon Inspector，設定 Amazon Inspector 掃描 EC2 執行個體軟體漏洞，並設定 ...

**答案**  
D

## Question #330

**題目**  
某公司打算將資料儲存在 Amazon RDS DB instance 中，且資料必須在靜態儲存時加密。解決方案架構師應該怎麼做才能滿足此需求？

**選項**
- A. 在 AWS Key Management Service (AWS KMS) 中建立金鑰，並為 DB instance 啟用加密
- B. 建立加密金鑰，並儲存在 AWS Secrets Manager 中，再使用該金鑰加密 DB instance
- C. 在 AWS Certificate Manager (ACM) 產生憑證，並使用該憑證在 DB instance 上啟用 SSL/TLS
- D. 在 AWS Identity and Access Management (IAM) 產生憑證，並使用該憑證在 DB instance 上啟用 SSL/TLS

**答案**  
C

## Question #331

**題目**  
某公司必須在 30 天內，將 20 TB 資料從資料中心遷移到 AWS Cloud。公司的網路頻寬僅有 15 Mbps，且使用率不能超過 70%。解決方案架構師應該怎麼做？

**選項**
- A. 使用 AWS Snowball
- B. 使用 AWS DataSync
- C. 使用安全 VPN 連線
- D. 使用 Amazon S3 Transfer Acceleration

**答案**  
A

## Question #332

**題目**  
某公司需要為員工提供安全方式來存取機密與敏感檔案，並確保只有授權使用者可以存取，且這些檔案必須能安全下載到員工裝置上。檔案目前儲存在內部部署的 Windows 檔案伺服器中，但因遠端使用量增加，檔案伺服器容量即將不足。哪一個解決方案最符合需求？

**選項**
- A. 將檔案伺服器遷移到公有子網路中的 Amazon EC2 執行個體，並設定 security group 限制 inbound 流量
- B. 將檔案遷移到 Amazon FSx for Windows File Server 檔案系統，並與內部部署的 ... 整合
- C. 將檔案遷移到 Amazon S3，並建立 private VPC endpoint，再使用 signed URL 允許下載
- D. 將檔案遷移到 Amazon S3，並建立 public VPC endpoint，讓員工透過 AWS IAM Identity Center ... 登入

**答案**  
B

## Question #333

**題目**  
某公司的應用程式執行於 Application Load Balancer (ALB) 後方的 Amazon EC2 執行個體上，並位於跨多個 Availability Zone 的 Amazon EC2 Auto Scaling group 中。每月第一天午夜，系統執行月結財務批次計算時，應用程式會變得非常緩慢，導致 EC2 執行個體 CPU 使用率立即飆到 100%，進而影響應用程式服務。解決方案架構師應該建議什麼做法，以確保應用程式能處理該工作負載並避免停機？

**選項**
- A. 在 ALB 前方設定 Amazon CloudFront distribution
- B. 根據 CPU 使用率設定 EC2 Auto Scaling simple scaling policy
- C. 根據每月固定時程設定 EC2 Auto Scaling scheduled scaling policy
- D. 設定 Amazon ElastiCache，將部分工作負載從 EC2 執行個體移出

**答案**  
C

## Question #334

**題目**  
某公司希望讓客戶能使用內部部署的 Microsoft Active Directory，透過 SFTP client 下載儲存在 Amazon S3 中的檔案。公司希望以最低營運負擔實現，且不能修改客戶端應用程式。哪一個解決方案最符合需求？

**選項**
- A. 為 Amazon S3 建立使用 SFTP 的 AWS Transfer Family，並設定整合式 Active Directory 驗證
- B. 設定 AWS Database Migration Service (AWS DMS) 將內部部署 client 與 Amazon S3 同步，並設定整合式 Active Directory ...
- C. 設定 AWS DataSync 在內部部署位置與 S3 位置之間同步，並使用 AWS IAM Identity Center ...
- D. 架設一台 Windows Amazon EC2 執行個體提供 SFTP，作為內部部署 client 與 Amazon S3 之間的橋接，並整合 AWS Identity ...

**答案**  
B

## Question #335

**題目**  
某公司正面臨需求突然增加的情況。公司需要從 Amazon Machine Image (AMI) 佈建大型 Amazon EC2 執行個體，這些執行個體將運行於 Auto Scaling group 中。公司需要一個能提供最低初始化延遲的解決方案，以因應此需求。哪一個解決方案符合需求？

**選項**
- A. 使用 aws ec2 register-image 指令從 snapshot 建立 AMI，並用 AWS Step Functions 取代 Auto Scaling group 中的 AMI
- B. 對 snapshot 啟用 Amazon Elastic Block Store (Amazon EBS) fast snapshot restore，使用該 snapshot 建立 AMI，並取代 ...
- C. 啟用 AMI 建立功能，並在 Amazon Data Lifecycle Manager (Amazon DLM) 中定義 lifecycle rule，再建立 AWS Lambda function ...
- D. 使用 Amazon EventBridge 呼叫 AWS Backup lifecycle policy 來建立 AMI，並設定 Auto Scaling group 容量上限 ...

**答案**  
C

## Question #336

**題目**  
某公司託管一個多層式 Web 應用程式，資料儲存在 Amazon Aurora MySQL DB cluster 中，應用層則執行於 Amazon EC2 執行個體上。公司 IT 安全規範要求資料庫憑證必須加密，且每 14 天輪替一次。解決方案架構師應該怎麼做，才能以最低營運負擔滿足此需求？

**選項**
- A. 建立新的 AWS Key Management Service (AWS KMS) encryption key，並使用 AWS Secrets Manager 建立使用該金鑰的新 secret
- B. 在 AWS Systems Manager Parameter Store 建立兩個參數：一個以 string parameter 儲存使用者名稱，另一個 ...
- C. 將包含憑證的檔案儲存在經 AWS Key Management Service (AWS KMS) 加密的 Amazon Elastic File System ...
- D. 將包含憑證的檔案儲存在經 AWS Key Management Service (AWS KMS) 加密的 Amazon S3 bucket，供應用程式存取

**答案**  
A

## Question #337

**題目**  
某公司已在 AWS 上部署 Web 應用程式。後端資料庫使用 Amazon RDS for MySQL，包含一個 primary DB instance 與五個 read replica 以支援擴展需求。這些 read replica 與 primary DB instance 的延遲不得超過 1 秒。資料庫會定期執行排程 stored procedure。隨著網站流量增加，在尖峰負載期間，replica 延遲會進一步升高。解決方案架構師必須盡可能降低 replication lag，同時最小化應用程式程式碼變更與持續營運負擔。哪一個解決方案最符合需求？

**選項**
- A. 將資料庫遷移到 Amazon Aurora MySQL，以 Aurora Replica 取代 read replica，並設定 Aurora Auto Scaling
- B. 在資料庫前方部署 Amazon ElastiCache for Redis cluster，並修改應用程式先查快取再查資料庫
- C. 將資料庫遷移到執行於 Amazon EC2 執行個體上的 MySQL，並為所有執行個體選用大型 compute optimized 規格
- D. 將資料庫遷移到 Amazon DynamoDB，並配置大量 read capacity units (RCUs) 以支援所需吞吐量

**答案**  
A

## Question #338

**題目**  
某位解決方案架構師必須為高流量軟體即服務（SaaS）平台建立災難復原（DR）計畫。平台所有資料都儲存在 Amazon Aurora MySQL DB cluster 中。DR 計畫必須將資料複寫到次要 AWS Region。哪一個解決方案最具成本效益？

**選項**
- A. 使用 MySQL binary log replication，將資料複寫到次要 Region 的 Aurora cluster，並在次要 Region 的 Aurora cluster 中配置一個 DB instance
- B. 為 DB cluster 設定 Aurora global database，完成後移除次要 Region 中的 DB instance
- C. 使用 AWS Database Migration Service (AWS DMS) 將資料持續複寫到次要 Region 的 Aurora cluster，然後移除 ...
- D. 為 DB cluster 設定 Aurora global database，並在次要 Region 至少保留一個 DB instance

**答案**  
D

## Question #339

**題目**  
某公司有一個自訂應用程式，內嵌了資料庫憑證，並會從 Amazon RDS MySQL DB instance 擷取資料。管理層要求在最少程式修改下提升安全性。解決方案架構師應該怎麼做？

**選項**
- A. 使用 AWS Key Management Service (AWS KMS) 建立金鑰，並讓應用程式從 AWS ... 載入資料庫憑證
- B. 在 RDS for MySQL 資料庫中建立應用程式使用者的憑證，並將憑證儲存在 AWS Secrets Manager
- C. 在 RDS for MySQL 資料庫中建立應用程式使用者的憑證，並將憑證儲存在 AWS Secrets Manager
- D. 在 RDS for MySQL 資料庫中建立應用程式使用者的憑證，並將憑證儲存在 AWS Systems Manager

**答案**  
D

## Question #340

**題目**  
某媒體公司在 AWS 上託管網站。網站應用程式架構包含一組位於 Application Load Balancer (ALB) 後方的 Amazon EC2 執行個體，以及一個託管在 Amazon Aurora 的資料庫。公司的資安團隊指出，該應用程式存在 SQL injection 風險。公司應該如何解決此問題？

**選項**
- A. 在 ALB 前方使用 AWS WAF，並將適當的 web ACL 與 AWS WAF 關聯
- B. 建立 ALB listener rule，對 SQL injection 請求回應固定回應
- C. 訂閱 AWS Shield Advanced，自動阻擋所有 SQL injection 嘗試
- D. 設定 Amazon Inspector，自動阻擋所有 SQL injection 嘗試

**答案**  
C

## Question #341

**題目**  
某公司擁有一個由 AWS Lake Formation 管理的 Amazon S3 data lake。公司希望在 Amazon QuickSight 中建立視覺化分析，將 data lake 中的資料與儲存在 Amazon Aurora MySQL 資料庫中的營運資料進行 join。公司還希望實施欄位層級的授權，讓行銷團隊只能存取資料庫中的部分欄位。哪一個解決方案能以最低營運負擔滿足需求？

**選項**
- A. 使用 Amazon EMR 將資料直接從資料庫匯入 QuickSight SPICE engine，且只包含需要的欄位
- B. 使用 AWS Glue Studio 將資料從資料庫匯入 S3 data lake，並對 QuickSight 使用者附加 IAM policy 以強制欄位層級權限
- C. 使用 AWS Glue Elastic Views 在 Amazon S3 中為資料庫建立 materialized view，並以 S3 bucket policy 強制欄位層級授權
- D. 使用 Lake Formation blueprint 將資料從資料庫匯入 S3 data lake，並使用 Lake Formation 實施欄位層級授權

**答案**  
C

## Question #342

**題目**  
某交易處理公司每週都會在 Amazon EC2 執行個體上執行批次工作，這些 EC2 執行個體位於 Auto Scaling group 中。交易量雖然會波動，但每次執行時的基準 CPU 使用率至少都有 60%。公司需要在批次作業開始前 30 分鐘先備妥容量。現在工程師是手動調整 Auto Scaling group 參數來完成這件事。公司沒有多餘人力分析 Auto Scaling group 所需容量趨勢，因此需要一套自動方式來調整 Auto Scaling group 的 desired capacity。哪一個解決方案能以最低營運負擔滿足需求？

**選項**
- A. 為 Auto Scaling group 建立 dynamic scaling policy，根據 CPU 使用率進行擴縮，並設定 ...
- B. 為 Auto Scaling group 建立 scheduled scaling policy，並設定適當的 desired capacity、minimum capacity 與 maximum capacity
- C. 為 Auto Scaling group 建立 predictive scaling policy，根據預測結果進行擴縮，並將 scaling metric 設為 ...
- D. 建立 Amazon EventBridge event，當 Auto Scaling group 的 CPU 使用率指標達到門檻時，呼叫 AWS Lambda function

**答案**  
C

## Question #343

**題目**  
某位解決方案架構師正在設計公司的災難復原（DR）架構。公司目前有一個執行於私有子網路中 Amazon EC2 執行個體上的 MySQL 資料庫，並搭配排程備份。DR 設計需要涵蓋多個 AWS Region。哪一個解決方案能以最低營運負擔滿足這些需求？

**選項**
- A. 將 MySQL 資料庫遷移到多台 EC2 執行個體，並在 DR Region 設定 standby EC2 執行個體，再啟用 replication
- B. 將 MySQL 資料庫遷移到 Amazon RDS，使用 Multi-AZ 部署，並為 primary DB instance 在 ... 啟用 read replication
- C. 將 MySQL 資料庫遷移到 Amazon Aurora global database，在主要 Region 部署 primary DB cluster，並在次要 Region 部署 ...
- D. 將 MySQL 排程備份儲存在啟用 S3 Cross-Region Replication (CRR) 的 Amazon S3 bucket 中

**答案**  
B

## Question #344

**題目**  
某公司有一個 Java 應用程式使用 Amazon Simple Queue Service (Amazon SQS) 來解析訊息，但該應用程式無法處理超過 256 KB 的訊息。公司希望能讓應用程式在最少程式碼修改下，支援處理最大至 50 MB 的訊息。哪一個解決方案最符合需求？

**選項**
- A. 使用 Amazon SQS Extended Client Library for Java，將大於 256 KB 的訊息存放在 Amazon S3
- B. 使用 Amazon EventBridge 取代 Amazon SQS 來傳送大型訊息
- C. 直接修改 Amazon SQS 的上限，使其可處理超過 256 KB 的訊息
- D. 將超過 256 KB 的訊息儲存在 Amazon Elastic File System (Amazon EFS)，並讓 Amazon SQS 參照該資料

**答案**  
A

## Question #345

**題目**  
某公司希望限制其主要 Web 應用程式中某些內容的存取，並使用 AWS 提供的授權機制來保護內容。公司希望採用 serverless 架構，並為少於 100 位使用者提供驗證方案。此解決方案必須能與主要 Web 應用程式整合，並能將 Web 內容全球提供，同時隨著使用者成長而自動擴展，並提供最低登入延遲。哪一個解決方案最具成本效益？

**選項**
- A. 使用 Amazon Cognito 進行驗證，使用 Lambda@Edge 進行授權，並使用 Amazon CloudFront 提供 Web 應用程式內容
- B. 使用 AWS Directory Service for Microsoft Active Directory 進行驗證，使用 AWS Lambda 進行授權，並使用 Application ...
- C. 使用 Amazon Cognito 進行驗證，使用 AWS Lambda 進行授權，並使用 Amazon S3 Transfer Acceleration 提供 Web 應用程式內容
- D. 使用 AWS Directory Service for Microsoft Active Directory 進行驗證，使用 Lambda@Edge 進行授權，並使用 AWS Elastic ...

**答案**  
A

## Question #346

**題目**  
某公司資料中心內有一套老舊的 NAS 陣列，提供 SMB share 與 NFS share 給用戶端工作站使用。公司不想再購買新的 NAS，也不想支付原 NAS 陣列的續約維護費。部分資料常被存取，但大量資料其實處於不活躍狀態。解決方案架構師需要設計一個方案，把資料遷移到 Amazon S3、使用 S3 Lifecycle policy，並維持用戶端工作站相同的使用體驗。架構師已將 AWS Storage Gateway 納入方案。為符合需求，應部署哪一種類型的 storage gateway？

**選項**
- A. Volume Gateway
- B. Tape Gateway
- C. Amazon FSx File Gateway
- D. Amazon S3 File Gateway

**答案**  
C

## Question #347

**題目**  
某公司有一套執行於 Amazon EC2 執行個體上的應用程式。解決方案架構師已依公司目前需求，將執行個體家族與不同執行個體大小標準化。公司希望在未來 3 年內最大化成本節省，但也需要在未來 6 個月內，能因應應用程式受歡迎程度與使用量變化，調整 instance family 與大小。哪一個解決方案最具成本效益？

**選項**
- A. Compute Savings Plan
- B. EC2 Instance Savings Plan
- C. Zonal Reserved Instances
- D. Standard Reserved Instances

**答案**  
D

## Question #348

**題目**  
某公司從大量穿戴式裝置使用者蒐集資料，並將資料儲存在 Amazon DynamoDB table 中，再由應用程式進行分析。此資料工作負載穩定且可預測。公司希望 DynamoDB 成本能維持在預估預算內或以下。哪一個解決方案最具成本效益？

**選項**
- A. 使用 provisioned mode 與 DynamoDB Standard-Infrequent Access (DynamoDB Standard-IA)，並為預估容量購買 reserved capacity
- B. 使用 provisioned mode，直接指定 read capacity units (RCUs) 與 write capacity units (WCUs)
- C. 使用 on-demand mode，並將 read capacity units (RCUs) 與 write capacity units (WCUs) 設得足夠高以因應變化
- D. 使用 on-demand mode，並為 read capacity units (RCUs) 與 write capacity units (WCUs) 購買 reserved capacity

**答案**  
A

## Question #349

**題目**  
某公司在 ap-southeast-3 Region 的 Amazon Aurora PostgreSQL 資料庫中儲存機密資料。該資料庫使用 AWS Key Management Service (AWS KMS) customer managed key 加密。公司最近被收購，現在必須將資料庫備份安全地分享給收購方在 ap-southeast-3 的 AWS account。解決方案架構師應該怎麼做？

**選項**
- A. 建立資料庫 snapshot，將 snapshot 複製成新的未加密 snapshot，並分享給收購方的 AWS account
- B. 建立資料庫 snapshot，將收購方 AWS account 加入 KMS key policy，並將 snapshot 分享給收購方 AWS account
- C. 建立使用不同 AWS managed KMS key 的資料庫 snapshot，將收購方 AWS account 加入 KMS key policy，並分享 ...
- D. 建立資料庫 snapshot，下載後再上傳到 Amazon S3 bucket，並更新 S3 ...

**答案**  
B

## Question #350

**題目**  
某公司在 us-east-1 Region 使用一個 100 GB 的 Amazon RDS for Microsoft SQL Server Single-AZ DB instance 來儲存客戶交易資料。公司需要高可用性與 DB instance 的自動復原能力，並且每年還需要數次對該 RDS 資料庫執行報表工作。報表執行時會使交易寫入客戶帳戶的速度變慢。公司需要一個能提升報表效能的解決方案。哪兩個步驟最符合需求？（選兩項）

**選項**
- A. 將 DB instance 從 Single-AZ 修改為 Multi-AZ deployment
- B. 對目前 DB instance 建立 snapshot，並在另一個 Availability Zone 還原成新的 RDS 部署
- C. 在不同 Availability Zone 建立 DB instance 的 read replica，並將所有報表請求導向 read replica
- D. 將資料庫遷移到 RDS Custom
- E. 使用 RDS Proxy，將報表請求限制在 maintenance window 中

**答案**  
AC
