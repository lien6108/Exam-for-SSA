# AWS SAA 第 101～150 題繁中翻譯

僅保留：題號、題目、選項、答案。

---

## Question #101

**題目**  
某位解決方案架構師正在設計一個包含公有子網與私有子網的 VPC。VPC 與子網皆使用 IPv4 CIDR 區塊。為了達成高可用性，在三個可用區（AZ）中，每個 AZ 都各有一個公有子網與一個私有子網。公有子網透過 internet gateway 提供網際網路存取。私有子網需要能夠連上網際網路，讓其中的 Amazon EC2 執行個體下載軟體更新。解決方案架構師應該怎麼做，才能讓私有子網存取網際網路？

**選項**
- A. 建立三個 NAT gateway，每個 AZ 的公有子網各一個。為每個 AZ 建立一張私有路由表，將非 VPC 流量導向同一 AZ 的 NAT gateway。
- B. 建立三個 NAT instance，每個 AZ 的私有子網各一個。為每個 AZ 建立一張私有路由表，將非 VPC 流量導向對應的 NAT instance。
- C. 在其中一個私有子網上建立第二個 internet gateway。更新私有子網的路由表，將非 VPC 流量導向該 internet gateway。
- D. 在其中一個公有子網上建立 egress-only internet gateway。更新私有子網的路由表，將非 VPC 流量導向該 egress-only internet gateway。

**答案**  
A

---

## Question #102

**題目**  
某公司想將內部部署資料中心遷移到 AWS。資料中心內有一台 SFTP 伺服器，資料儲存在以 NFS 為基礎的檔案系統中。該伺服器持有 200 GB 資料需要轉移。伺服器必須部署在使用 Amazon Elastic File System (Amazon EFS) 檔案系統的 Amazon EC2 執行個體上。哪一組步驟可以自動化完成這項任務？（選兩項）

**選項**
- A. 將 EC2 執行個體部署在與 EFS 檔案系統相同的可用區。
- B. 在內部部署資料中心安裝 AWS DataSync agent。
- C. 在 EC2 執行個體上建立第二個 Amazon Elastic Block Store (Amazon EBS) 磁碟區來存放資料。
- D. 手動使用作業系統的複製指令，將資料推送到 EC2 執行個體。
- E. 使用 AWS DataSync 為內部部署的 SFTP 伺服器建立適當的位置設定。

**答案**  
AB

---

## Question #103

**題目**  
某公司有一個 AWS Glue ETL 工作，每天固定時間執行一次。此工作會處理位於 Amazon S3 bucket 中的 XML 資料，而該 S3 bucket 每天都會新增資料。某位解決方案架構師注意到 AWS Glue 每次執行時都會重複處理所有資料。為了避免重新處理舊資料，應該怎麼做？

**選項**
- A. 編輯該工作，使用 job bookmarks。
- B. 編輯該工作，讓資料在處理完成後自動刪除。
- C. 編輯該工作，將 NumberOfWorkers 設為 1。
- D. 使用 FindMatches 機器學習轉換。

**答案**  
A

---

## Question #104

**題目**  
某位解決方案架構師必須為一個網站設計高可用基礎架構。該網站由執行於 Amazon EC2 執行個體上的 Windows Web 伺服器提供服務。架構師必須實作一套可緩解來自數千個 IP 位址的大規模 DDoS 攻擊的方案，而且網站不能接受停機。應採取哪些行動來保護網站？（選兩項）

**選項**
- A. 使用 AWS Shield Advanced 來阻擋 DDoS 攻擊。
- B. 設定 Amazon GuardDuty，自動封鎖攻擊者。
- C. 將網站設定為使用 Amazon CloudFront 提供靜態與動態內容。
- D. 使用 AWS Lambda 函數，自動把攻擊者 IP 加入 VPC network ACL。
- E. 在 Auto Scaling 群組中使用 EC2 Spot Instances，並設定目標追蹤擴展策略為 80% CPU 使用率。

**答案**  
AC

---

## Question #105

**題目**  
某公司正在部署新的無伺服器工作負載。某位解決方案架構師必須依照最小權限原則，設定執行 AWS Lambda 函數所需的權限。該函數將由 Amazon EventBridge（Amazon CloudWatch Events）規則觸發。哪一個方案符合需求？

**選項**
- A. 為函數新增一個 execution role，設定動作為 lambda:InvokeFunction，主體為 *。
- B. 為函數新增一個 execution role，設定動作為 lambda:InvokeFunction，主體為 Service: lambda.amazonaws.com。
- C. 為函數新增 resource-based policy，設定動作為 lambda:*，主體為 Service: events.amazonaws.com。
- D. 為函數新增 resource-based policy，設定動作為 lambda:InvokeFunction，主體為 Service: events.amazonaws.com。

**答案**  
D

---

## Question #106

**題目**  
某公司準備將機密資料儲存在 Amazon S3。基於合規要求，資料必須在靜態時加密，且金鑰使用情況必須可記錄以供稽核。金鑰還必須每年輪換一次。哪個方案最符合需求且營運效率最高？

**選項**
- A. 使用 customer-provided keys 的伺服器端加密（SSE-C）。
- B. 使用 Amazon S3 managed keys 的伺服器端加密（SSE-S3）。
- C. 使用 AWS KMS keys 的伺服器端加密（SSE-KMS），並手動輪換。
- D. 使用 AWS KMS keys 的伺服器端加密（SSE-KMS），並啟用自動輪換。

**答案**  
D

---

## Question #107

**題目**  
某共享單車公司正在開發一套多層架構，以在尖峰營運時段追蹤單車位置。公司希望將這些資料點納入既有的分析平台。某位解決方案架構師必須決定最可行的多層方案來支援此架構。這些資料點必須可由 REST API 存取。哪個動作符合儲存與擷取位置資料的需求？

**選項**
- A. 使用 Amazon Athena 搭配 Amazon S3。
- B. 使用 Amazon API Gateway 搭配 AWS Lambda。
- C. 使用 Amazon QuickSight 搭配 Amazon Redshift。
- D. 使用 Amazon API Gateway 搭配 Amazon Kinesis Data Analytics。

**答案**  
D

---

## Question #108

**題目**  
某公司有一個汽車銷售網站，其刊登資料儲存在 Amazon RDS 資料庫中。當汽車售出後，該刊登資訊必須從網站移除，且資料必須傳送到多個目標系統。解決方案架構師應該建議哪種設計？

**選項**
- A. 建立一個 AWS Lambda 函數，於 Amazon RDS 資料庫更新時觸發，並將資訊傳送到 Amazon Simple Notification Service (Amazon SNS) 主題。
- B. 建立一個 AWS Lambda 函數，於 Amazon RDS 資料庫更新時觸發，並將資訊傳送到 Amazon Simple Queue Service (Amazon SQS) 佇列。
- C. 訂閱 RDS event notification，並將事件傳送到 Amazon Simple Queue Service (Amazon SQS) 佇列，再扇出到多個目標。
- D. 訂閱 RDS event notification，並將事件傳送到 Amazon Simple Notification Service (Amazon SNS) 主題，再扇出到多個目標。

**答案**  
C

---

## Question #109

**題目**  
某公司需要將資料儲存在 Amazon S3 中，且必須避免資料被變更。公司希望新上傳到 Amazon S3 的物件在公司決定修改前，都保持不可變更。只有公司 AWS 帳號中的特定使用者才可以刪除物件。解決方案架構師應該怎麼做？

**選項**
- A. 建立一個 S3 Glacier vault，並對物件套用 write-once, read-many (WORM) vault lock policy。
- B. 建立一個啟用 S3 Object Lock 的 S3 bucket，並啟用 versioning。將保留期間設為 100 年，並使用 governance mode。
- C. 建立一個 S3 bucket，使用 AWS CloudTrail 追蹤所有修改物件的 S3 API 事件；收到通知後再還原被修改的物件。
- D. 建立一個啟用 S3 Object Lock 的 S3 bucket，並啟用 versioning。對物件加上 legal hold，並將 s3:PutObjectLegalHold 權限授予特定使用者。

**答案**  
D

---

## Question #110

**題目**  
某社群媒體公司允許使用者將圖片上傳到網站。網站執行在 Amazon EC2 執行個體上。上傳請求發生時，網站會將圖片調整成標準尺寸，並將調整後的圖片儲存在 Amazon S3。使用者反映網站上傳速度很慢。公司需要降低應用程式耦合度並提升網站效能。某位解決方案架構師必須設計在營運上最有效率的圖片上傳流程。應採取哪些動作？（選兩項）

**選項**
- A. 將應用程式設定為把圖片上傳到 S3 Glacier。
- B. 將 Web 伺服器設定為把原始圖片上傳到 Amazon S3。
- C. 將應用程式設定為透過 presigned URL，讓使用者瀏覽器直接把圖片上傳到 Amazon S3。
- D. 設定 S3 Event Notifications，在圖片上傳後觸發 AWS Lambda 函數，由函數負責調整圖片大小。
- E. 建立一個 Amazon EventBridge（Amazon CloudWatch Events）規則，定期觸發 AWS Lambda 函數來調整圖片大小。

**答案**  
BD

---

## Question #111

**題目**  
某公司最近將一套訊息處理系統遷移到 AWS。系統把訊息接收到執行在 Amazon EC2 執行個體上的 ActiveMQ 佇列中，訊息由另一個執行在 Amazon EC2 上的 consumer application 處理，並把結果寫入執行在 Amazon EC2 上的 MySQL 資料庫。公司希望此應用程式具備高可用性，且營運複雜度低。哪種架構可提供最高的可用性？

**選項**
- A. 在另一個可用區新增第二台 ActiveMQ 伺服器，並在另一個可用區新增一台 consumer EC2 執行個體。
- B. 使用 Amazon MQ，並將 broker 設定為跨兩個可用區的 active/standby。再於另一個可用區新增一台 consumer EC2 執行個體。
- C. 使用 Amazon MQ，並將 broker 設定為跨兩個可用區的 active/standby。再於多個可用區部署另一台 consumer EC2 執行個體。
- D. 使用 Amazon MQ，並將 broker 設定為跨兩個可用區的 active/standby。為 consumer EC2 執行個體建立 Auto Scaling 群組。

**答案**  
D

---

## Question #112

**題目**  
某公司將容器化的 Web 應用程式部署在內部部署伺服器群上，以處理傳入請求。請求量快速成長，現有內部部署伺服器已無法負荷。公司希望在盡量少修改程式碼與開發工作量的前提下，將應用程式遷移到 AWS。哪個方案能以最低營運負擔滿足這些需求？

**選項**
- A. 使用 AWS Fargate 搭配 Amazon Elastic Container Service (Amazon ECS) 執行容器化 Web 應用程式，並啟用 Service Auto Scaling。
- B. 使用兩台 Amazon EC2 執行個體來承載容器化 Web 應用程式，並使用 Application Load Balancer 分配流量。
- C. 使用 AWS Lambda，並重寫程式碼為支援的語言，建立多個 Lambda 函數來支援流量。
- D. 使用高效能運算（HPC）方案，例如 AWS ParallelCluster，建立 HPC 叢集來處理請求。

**答案**  
A

---

## Question #113

**題目**  
某公司有 50 TB 的資料用於報表。公司想把這些資料從內部部署搬到 AWS。公司資料中心中有一套自訂應用程式，每週會執行一次資料轉換工作。公司計劃在資料轉移完成前暫停該應用程式，並希望盡快開始轉移流程。資料中心目前沒有額外網路頻寬可供其他工作負載使用。某位解決方案架構師必須完成資料轉移，並讓資料轉換工作之後能持續在 AWS Cloud 上執行。哪個方案可在最低營運負擔下滿足需求？

**選項**
- A. 使用 AWS DataSync 搬移資料，並使用 AWS Glue 建立自訂資料轉換工作。
- B. 訂購一台 AWS Snowcone 裝置來搬移資料，並把轉換應用程式部署到該裝置上。
- C. 訂購一台 AWS Snowball Edge Storage Optimized 裝置，將資料複製到該裝置，並使用 AWS Glue 建立自訂轉換工作。
- D. 訂購一台包含 Amazon EC2 運算能力的 AWS Snowball Edge Storage Optimized 裝置，將資料複製到裝置上，並在裝置上建立新的轉換工作。

**答案**  
C

---

## Question #114

**題目**  
某公司建立了一個影像分析應用程式，使用者可以上傳照片，並為照片加上相框。使用者會上傳影像與 metadata，以指示要套用哪種相框。應用程式目前使用單一 Amazon EC2 執行個體與 Amazon DynamoDB 儲存 metadata。此應用逐漸受到歡迎，使用者數量持續增加，而且預期同時在線的使用者數量會依照一天中的時間與星期幾而大幅波動。公司必須確保應用程式能夠隨著使用者成長而擴展。哪個方案符合需求？

**選項**
- A. 使用 AWS Lambda 處理照片，並將照片與 metadata 都儲存在 DynamoDB。
- B. 使用 Amazon Kinesis Data Firehose 處理照片，並儲存照片與 metadata。
- C. 使用 AWS Lambda 處理照片，將照片儲存在 Amazon S3，並保留 DynamoDB 來儲存 metadata。
- D. 將 EC2 執行個體數量增加到三台，並使用 Provisioned IOPS SSD (io2) Amazon Elastic Block Store (Amazon EBS) 磁碟區。

**答案**  
A

---

## Question #115

**題目**  
某醫療紀錄公司在 Amazon EC2 執行個體上執行應用程式。該應用程式會處理儲存在 Amazon S3 上的客戶資料檔案。EC2 執行個體位於公有子網，且目前透過網際網路存取 Amazon S3，但除此之外不需要任何其他網路存取。新需求要求檔案傳輸流量必須走私有路徑，不可經由網際網路。為了符合需求，解決方案架構師應建議如何調整網路架構？

**選項**
- A. 建立 NAT gateway，並設定公有子網的路由表將流量導向 NAT gateway 後再前往 Amazon S3。
- B. 設定 EC2 執行個體的 security group，限制對外流量只能存取 S3 prefix list。
- C. 將 EC2 執行個體移到私有子網，建立 Amazon S3 的 VPC endpoint，並將 endpoint 關聯到私有子網的路由表。
- D. 移除 VPC 的 internet gateway，改為建立 AWS Direct Connect，並透過 Direct Connect 將流量導向 Amazon S3。

**答案**  
C

---

## Question #116

**題目**  
某公司使用熱門的內容管理系統（CMS）作為企業網站，但維護與修補工作相當繁重。公司正在重新設計網站，並希望採用新方案。網站每年只會更新四次，而且不需要任何動態內容。新方案必須具備高擴展性與更佳安全性。哪一組變更可在最低營運負擔下符合需求？（選兩項）

**選項**
- A. 在網站前方配置 Amazon CloudFront，使用其 HTTPS 功能。
- B. 在網站前方部署 AWS WAF web ACL，提供 HTTPS 功能。
- C. 建立並部署 AWS Lambda 函數來管理與提供網站內容。
- D. 建立新網站與 Amazon S3 bucket，並透過靜態網站託管功能將網站部署在 S3 bucket 上。
- E. 建立新網站，並以位於 Application Load Balancer 後方的 Amazon EC2 Auto Scaling 群組來部署網站。

**答案**  
AD

---

## Question #117

**題目**  
某公司將應用程式日誌儲存在 Amazon CloudWatch Logs log group 中。新政策要求所有應用程式日誌都必須近即時地儲存到 Amazon OpenSearch Service（Amazon Elasticsearch Service）。哪個方案能以最低營運負擔滿足此需求？

**選項**
- A. 設定 CloudWatch Logs subscription，將日誌串流至 Amazon OpenSearch Service（Amazon Elasticsearch Service）。
- B. 建立 AWS Lambda 函數，並由 log group 觸發該函數，將日誌寫入 Amazon OpenSearch Service（Amazon Elasticsearch Service）。
- C. 建立 Amazon Kinesis Data Firehose delivery stream，將 log group 設為來源，並設定輸出至 Amazon OpenSearch Service（Amazon Elasticsearch Service）。
- D. 在每台應用伺服器安裝並設定 Amazon Kinesis Agent，將日誌送到 Amazon Kinesis Data Streams，再串接 Amazon OpenSearch Service（Amazon Elasticsearch Service）。

**答案**  
C

---

## Question #118

**題目**  
某公司正在建置一套 Web 應用程式，執行於多個可用區中的 Amazon EC2 執行個體上。該應用程式需要提供一個總量約 900 TB 的文字文件儲存庫存取。公司預期 Web 應用程式在某些時段會有高需求。某位解決方案架構師必須確保存放文字文件的儲存元件可隨時擴展，以滿足需求，同時公司也很在意整體成本。哪種儲存方案最具成本效益？

**選項**
- A. Amazon Elastic Block Store (Amazon EBS)
- B. Amazon Elastic File System (Amazon EFS)
- C. Amazon OpenSearch Service (Amazon Elasticsearch Service)
- D. Amazon S3

**答案**  
D

---

## Question #119

**題目**  
某全球性公司正在使用 Amazon API Gateway 設計 REST API，供 us-east-1 與 ap-southeast-2 區域中的會員系統使用。某位解決方案架構師必須設計一套方案，在多個 AWS 帳號中保護這些 API Gateway 管理的 REST API，防範 SQL injection 與 cross-site scripting 攻擊。哪個方案可在最低管理負擔下滿足需求？

**選項**
- A. 在兩個區域中都設定 AWS WAF，並將 Regional web ACL 關聯到 API stage。
- B. 在兩個區域中都設定 AWS Firewall Manager，並集中管理 AWS WAF 規則。
- C. 在兩個區域中都設定 AWS Shield，並將 Regional web ACL 關聯到 API stage。
- D. 只在其中一個區域設定 AWS Shield，並將 Regional web ACL 關聯到 API stage。

**答案**  
A

---

## Question #120

**題目**  
某公司在 us-west-2 區域中，透過一組位於 Network Load Balancer (NLB) 後方的三台 Amazon EC2 執行個體，實作自管 DNS 解決方案。大多數使用者位於美國與歐洲。公司想提升此方案的效能與可用性，因此又在 eu-west-1 區域中啟動並設定三台 EC2 執行個體，並將它們加入新的 NLB 作為目標。公司可以使用哪個方案，將流量導向所有 EC2 執行個體？

**選項**
- A. 建立 Amazon Route 53 geolocation routing policy，將請求導向兩個 NLB 之一，並建立 Amazon CloudFront distribution。
- B. 建立 AWS Global Accelerator standard accelerator，在 us-west-2 與 eu-west-1 建立 endpoint groups，並將兩個 NLB 加入為端點。
- C. 為六台 EC2 執行個體都綁定 Elastic IP，並建立 Amazon Route 53 geolocation routing policy，將請求導向其中一組。
- D. 將兩個 NLB 都改為 Application Load Balancer (ALB)，並使用 Amazon Route 53 latency routing policy 將請求導向對應 ALB。

**答案**  
A

---

## Question #121

**題目**  
某公司正在 AWS 上執行一個線上交易處理（OLTP）工作負載。此工作負載使用一個未加密的 Amazon RDS DB instance，並採 Multi-AZ 部署。系統每天都會對此 DB instance 建立快照。為了確保未來資料庫與快照都保持加密，解決方案架構師應該怎麼做？

**選項**
- A. 將最新的 DB snapshot 複製並加密，再透過還原加密後的 snapshot 來取代既有 DB instance。
- B. 建立新的加密 Amazon Elastic Block Store (Amazon EBS) 磁碟區，將快照複製到其中，並在 DB instance 上啟用加密。
- C. 複製快照並使用 AWS Key Management Service (AWS KMS) 啟用加密，再將加密後的 snapshot 還原到既有 DB instance。
- D. 將快照複製到使用 AWS Key Management Service (AWS KMS) 伺服器端加密的 Amazon S3 bucket 中。

**答案**  
A

---

## Question #122

**題目**  
某公司想建立一套可擴展的金鑰管理基礎架構，以支援開發人員在應用程式中加密資料。解決方案架構師應該怎麼做，才能降低營運負擔？

**選項**
- A. 使用 multi-factor authentication (MFA) 保護加密金鑰。
- B. 使用 AWS Key Management Service (AWS KMS) 保護加密金鑰。
- C. 使用 AWS Certificate Manager (ACM) 建立、儲存並指派加密金鑰。
- D. 使用 IAM policy 限縮可存取加密金鑰的使用者範圍。

**答案**  
B

---

## Question #123

**題目**  
某公司有一個動態 Web 應用程式，執行在兩台 Amazon EC2 執行個體上。公司擁有自己的 SSL 憑證，目前憑證安裝在每台執行個體上進行 SSL 終止。最近流量增加，營運團隊發現 SSL 加解密造成 Web 伺服器運算資源達到上限。解決方案架構師應該怎麼做，以提升應用程式效能？

**選項**
- A. 使用 AWS Certificate Manager (ACM) 建立新的 SSL 憑證，並安裝到每台執行個體上。
- B. 建立 Amazon S3 bucket，將 SSL 憑證遷移至該 bucket，並設定 EC2 執行個體從 bucket 讀取憑證。
- C. 建立另一台 EC2 執行個體作為 proxy server，將 SSL 憑證移至該執行個體，並轉送連線到原有 Web 伺服器。
- D. 將 SSL 憑證匯入 AWS Certificate Manager (ACM)，建立一個使用該憑證的 HTTPS listener 的 Application Load Balancer。

**答案**  
D

---

## Question #124

**題目**  
某公司有一個高度動態的批次處理工作，需要使用大量 Amazon EC2 執行個體來完成。此工作是無狀態的，可以隨時啟動或停止而不會造成負面影響，且通常總執行時間超過 60 分鐘。公司要求解決方案架構師設計一套可擴展且具成本效益的方案。應建議哪種做法？

**選項**
- A. 使用 EC2 Spot Instances。
- B. 購買 EC2 Reserved Instances。
- C. 使用 EC2 On-Demand Instances。
- D. 以 AWS Lambda 執行此批次處理。

**答案**  
A

---

## Question #125

**題目**  
某公司在 AWS 上執行兩層式電商網站。Web 層由負載平衡器將流量導向 Amazon EC2 執行個體，資料庫層使用 Amazon RDS DB instance。EC2 執行個體與 RDS DB instance 都不應暴露於公網。EC2 執行個體需要能連上網際網路，以透過第三方 Web 服務完成付款處理。應用程式必須具備高可用性。哪一組設定可以符合需求？（選兩項）

**選項**
- A. 使用 Auto Scaling 群組，在私有子網中啟動 EC2 執行個體；在私有子網中部署 RDS Multi-AZ DB instance。
- B. 建立一個 VPC，包含兩個私有子網與分布於兩個可用區的兩個 NAT gateway；在公有子網中部署 Application Load Balancer。
- C. 使用 Auto Scaling 群組，在跨兩個可用區的公有子網中啟動 EC2 執行個體；部署 RDS Multi-AZ DB instance。
- D. 建立一個 VPC，包含一個公有子網、一個私有子網，以及分布於兩個可用區的兩個 NAT gateway；部署 Application Load Balancer。
- E. 建立一個 VPC，包含兩個公有子網、兩個私有子網，以及分布於兩個可用區的兩個 NAT gateway；部署 Application Load Balancer。

**答案**  
CE

---

## Question #126

**題目**  
某位解決方案架構師需要實作一套方案，以降低公司儲存成本。公司的所有資料目前都位於 Amazon S3 Standard 儲存類別中。公司必須至少保存所有資料 25 年，而最近 2 年內的資料必須具備高可用性且可立即擷取。哪個方案可滿足需求？

**選項**
- A. 立即設定 S3 Lifecycle policy，將物件轉移到 S3 Glacier Deep Archive。
- B. 設定 S3 Lifecycle policy，在 2 年後將物件轉移到 S3 Glacier Deep Archive。
- C. 使用 S3 Intelligent-Tiering，並啟用歸檔選項，確保資料會被封存到 S3 Glacier Deep Archive。
- D. 立即設定 S3 Lifecycle policy，先將物件轉移到 S3 One Zone-Infrequent Access (S3 One Zone-IA)，再轉移到 S3 Glacier。

**答案**  
B

---

## Question #127

**題目**  
某媒體公司正在評估是否將系統遷移到 AWS Cloud。公司需要至少 10 TB 儲存空間，且必須提供最高可能的 I/O 效能來進行影片處理；還需要 300 TB 的高耐久儲存空間來保存媒體內容；另外還需要 900 TB 的儲存空間，用於長期封存不再使用的媒體資料。解決方案架構師應推薦哪組服務？

**選項**
- A. 使用 Amazon EBS 提供最高效能、Amazon S3 提供高耐久資料儲存、Amazon S3 Glacier 提供封存儲存。
- B. 使用 Amazon EBS 提供最高效能、Amazon EFS 提供高耐久資料儲存、Amazon S3 Glacier 提供封存儲存。
- C. 使用 Amazon EC2 instance store 提供最高效能、Amazon EFS 提供高耐久資料儲存、Amazon S3 提供封存儲存。
- D. 使用 Amazon EC2 instance store 提供最高效能、Amazon S3 提供高耐久資料儲存、Amazon S3 Glacier 提供封存儲存。

**答案**  
A

---

## Question #128

**題目**  
某公司想在 AWS Cloud 上以容器方式執行應用程式。這些應用程式是無狀態的，且可以容忍底層基礎設施中斷。公司需要一套可同時降低成本與營運負擔的方案。解決方案架構師應該怎麼做？

**選項**
- A. 使用 Spot Instances 搭配 Amazon EC2 Auto Scaling 群組來執行應用程式容器。
- B. 使用 Spot Instances 搭配 Amazon Elastic Kubernetes Service (Amazon EKS) managed node group。
- C. 使用 On-Demand Instances 搭配 Amazon EC2 Auto Scaling 群組來執行應用程式容器。
- D. 使用 On-Demand Instances 搭配 Amazon Elastic Kubernetes Service (Amazon EKS) managed node group。

**答案**  
A

---

## Question #129

**題目**  
某公司目前在內部部署環境中執行一個多層 Web 應用程式。Web 應用程式已容器化，執行在多台 Linux 主機上，並連接到一個 PostgreSQL 資料庫儲存使用者資料。維護基礎設施與容量規劃的營運負擔已限制公司成長。某位解決方案架構師必須改善此應用程式的基礎架構。應採取哪些行動？（選兩項）

**選項**
- A. 將 PostgreSQL 資料庫遷移到 Amazon Aurora。
- B. 將 Web 應用程式遷移到 Amazon EC2 執行個體上。
- C. 為 Web 應用程式內容設置 Amazon CloudFront distribution。
- D. 在 Web 應用程式與 PostgreSQL 資料庫之間加入 Amazon ElastiCache。
- E. 將 Web 應用程式遷移到 AWS Fargate 搭配 Amazon Elastic Container Service (Amazon ECS)。

**答案**  
AE

---

## Question #130

**題目**  
某應用程式執行於跨多個可用區的 Amazon EC2 執行個體上。這些執行個體位於 Amazon EC2 Auto Scaling 群組中，並位於 Application Load Balancer 後方。當 EC2 執行個體的 CPU 使用率接近 40% 時，應用程式效能最佳。解決方案架構師應該怎麼做，才能讓 Auto Scaling 群組中的所有執行個體持續維持這個效能水準？

**選項**
- A. 使用 simple scaling policy 動態調整 Auto Scaling 群組。
- B. 使用 target tracking policy 動態調整 Auto Scaling 群組。
- C. 使用 AWS Lambda 函數更新 Auto Scaling 群組的 desired capacity。
- D. 使用 scheduled scaling actions 來對 Auto Scaling 群組進行擴縮。

**答案**  
B

---

## Question #131

**題目**  
某公司正在開發一個檔案分享應用程式，並將使用 Amazon S3 bucket 作為儲存空間。公司希望透過 Amazon CloudFront distribution 對外提供所有檔案，但不希望使用者能直接透過 S3 URL 存取這些檔案。解決方案架構師應該怎麼做？

**選項**
- A. 為每個 S3 bucket 撰寫個別 policy，只授予 CloudFront 讀取權限。
- B. 建立一個 IAM user，授予該 user 對 S3 bucket 中物件的讀取權限，再將該 user 指派給 CloudFront。
- C. 撰寫 S3 bucket policy，將 CloudFront distribution ID 設為 Principal，並將目標 S3 bucket 設為 Amazon Resource Name。
- D. 建立 origin access identity (OAI)，將 OAI 指派給 CloudFront distribution，並設定 S3 bucket 權限只允許 OAI 存取。

**答案**  
D

---

## Question #132

**題目**  
某公司的網站提供使用者下載歷史績效報表。網站需要一套能在全球範圍內隨需求擴展、具成本效益、盡量少佈建基礎設施，且能提供最快回應時間的方案。解決方案架構師應該建議哪一組服務？

**選項**
- A. Amazon CloudFront 搭配 Amazon S3
- B. AWS Lambda 搭配 Amazon DynamoDB
- C. Application Load Balancer 搭配 Amazon EC2 Auto Scaling
- D. Amazon Route 53 搭配 internal Application Load Balancers

**答案**  
A

---

## Question #133

**題目**  
某公司目前在內部部署環境執行 Oracle 資料庫。作為遷移到 AWS 的一部分，公司希望把資料庫升級到目前可用的最新版本，並建立災難復原（DR）機制。公司希望在日常營運與 DR 設定方面，盡量降低營運負擔，同時仍須保有對資料庫底層作業系統的存取權限。哪個方案符合需求？

**選項**
- A. 將 Oracle 資料庫遷移到 Amazon EC2 執行個體上，並在不同 AWS 區域建立資料庫複寫。
- B. 將 Oracle 資料庫遷移到 Amazon RDS for Oracle，並啟用 Cross-Region automated backups，將 snapshot 複寫到另一區域。
- C. 將 Oracle 資料庫遷移到 Amazon RDS Custom for Oracle，並在另一個 AWS 區域建立 read replica。
- D. 將 Oracle 資料庫遷移到 Amazon RDS for Oracle，並在另一個可用區建立 standby database。

**答案**  
D

---

## Question #134

**題目**  
某公司希望將現有應用程式遷移到無伺服器方案。新的無伺服器方案必須能夠使用機器學習分析既有資料與新資料。公司將資料儲存在 Amazon S3 bucket 中，且資料必須加密並複寫到另一個 AWS 區域。哪個方案在營運負擔最低的情況下最符合需求？

**選項**
- A. 建立新的 S3 bucket，將資料載入新 bucket，並使用 S3 Cross-Region Replication (CRR) 將加密物件複寫到另一區域的 S3 bucket。
- B. 建立新的 S3 bucket，將資料載入新 bucket，並使用其他方式將加密物件複寫到另一區域的 S3 bucket。
- C. 將資料載入既有 S3 bucket，並使用 S3 Cross-Region Replication (CRR) 將加密物件複寫到另一區域的 S3 bucket。
- D. 將資料載入既有 S3 bucket，並使用其他方式將加密物件複寫到另一區域的 S3 bucket。

**答案**  
A

---

## Question #135

**題目**  
某公司在 AWS 上執行工作負載，現在需要連線到外部供應商提供的服務。該服務部署在供應商的 VPC 中。根據公司資安團隊要求，連線必須為私有連線，且只能存取目標服務；此外，連線必須只能由公司的 VPC 主動發起。哪個方案可滿足這些需求？

**選項**
- A. 在公司的 VPC 與供應商的 VPC 之間建立 VPC peering connection，並更新路由表以連接目標服務。
- B. 要求供應商在其 VPC 中建立 virtual private gateway，並使用 AWS PrivateLink 連接目標服務。
- C. 在公司的 VPC 公有子網中建立 NAT gateway，並更新路由表以連接目標服務。
- D. 要求供應商為目標服務建立 VPC endpoint service，並使用 AWS PrivateLink 連接目標服務。

**答案**  
D

---

## Question #136

**題目**  
某公司正將內部部署 PostgreSQL 資料庫遷移到 Amazon Aurora PostgreSQL。遷移過程中，內部部署資料庫必須持續在線並可供存取，同時 Aurora 資料庫也必須與內部部署資料庫保持同步。為了滿足這些需求，解決方案架構師必須採取哪些動作？（選兩項）

**選項**
- A. 建立 ongoing replication task。
- B. 對內部部署資料庫建立資料庫備份。
- C. 建立 AWS Database Migration Service (AWS DMS) replication server。
- D. 使用 AWS Schema Conversion Tool (AWS SCT) 轉換資料庫 schema。
- E. 建立 Amazon EventBridge（Amazon CloudWatch Events）規則，以監控資料庫同步。

**答案**  
CD

---

## Question #137

**題目**  
某公司使用 AWS Organizations 為各業務單位建立專屬 AWS 帳號，讓各單位可獨立管理自己的帳號。某次寄送到其中一個帳號 root user email address 的通知被遺漏了。公司希望確保未來所有通知都不再被漏接，且未來通知只應限於帳號管理員。哪個方案可符合需求？

**選項**
- A. 將寄送到 AWS 帳號 root user email address 的通知信件，設定由公司郵件伺服器轉寄給帳號管理員。
- B. 將所有 AWS 帳號的 root user email address 都設成 distribution list，寄給數位能回應通知的管理員。
- C. 將所有 AWS 帳號的 root user email 訊息都寄給單一管理員，由其負責監控並處理所有通知。
- D. 將所有既有 AWS 帳號與未來新建帳號都使用相同的 root user email address，並設定 AWS account alternate contacts。

**答案**  
D

---

## Question #138

**題目**  
某公司在 AWS 上執行電商應用程式。每一筆新訂單都會作為訊息發布到執行於單一可用區中、Amazon EC2 執行個體上的 RabbitMQ 佇列。另一個執行於獨立 EC2 執行個體上的應用程式會處理這些訊息，並把明細儲存到另一台 EC2 執行個體上的 PostgreSQL 資料庫。所有 EC2 執行個體都位於同一個可用區。公司需要重新設計架構，以在最低營運負擔下提供最高可用性。解決方案架構師應該怎麼做？

**選項**
- A. 將佇列遷移到 Amazon MQ 上的 RabbitMQ 冗餘組態（active/standby），並為處理應用程式建立 Multi-AZ Auto Scaling 群組，資料庫維持 EC2。
- B. 將佇列遷移到 Amazon MQ 上的 RabbitMQ 冗餘組態（active/standby），為處理應用程式建立 Multi-AZ Auto Scaling 群組，並將 PostgreSQL 遷移到 Amazon RDS Multi-AZ。
- C. 為承載 RabbitMQ 的 EC2 建立 Multi-AZ Auto Scaling 群組，並再為處理應用程式建立另一個 Multi-AZ Auto Scaling 群組。
- D. 為承載 RabbitMQ 的 EC2 建立 Multi-AZ Auto Scaling 群組，並再為處理應用程式建立另一個 Multi-AZ Auto Scaling 群組，資料庫維持 EC2。

**答案**  
B

---

## Question #139

**題目**  
某報表團隊每天會收到檔案並存入一個 Amazon S3 bucket。團隊每天固定時間手動檢查並將檔案複製到另一個分析用 S3 bucket，以供 Amazon QuickSight 使用。現在有更多團隊開始傳送更大、更多的檔案到原始 S3 bucket。報表團隊希望在檔案進入原始 S3 bucket 時，自動將檔案移到分析用 S3 bucket；同時還希望使用 AWS Lambda 函數對複製後的資料執行 pattern-matching 程式碼，並將資料檔送進 Amazon SageMaker Pipelines。哪個方案可在最低營運負擔下滿足需求？

**選項**
- A. 建立 Lambda 函數將檔案複製到分析用 S3 bucket，並為分析用 S3 bucket 建立 S3 event notification。
- B. 建立 Lambda 函數將檔案複製到分析用 S3 bucket，並將分析用 S3 bucket 設定為把事件通知送到其他服務。
- C. 在兩個 S3 bucket 之間設定 S3 replication，並為分析用 S3 bucket 建立 S3 event notification，再設定 Lambda 與 SageMaker。
- D. 在兩個 S3 bucket 之間設定 S3 replication，並將分析用 S3 bucket 設定為將事件送到 Amazon EventBridge，再串接後續服務。

**答案**  
A

---

## Question #140

**題目**  
某位解決方案架構師需要協助公司最佳化在 AWS 上執行某應用程式的成本。該應用程式的運算層會使用 Amazon EC2、AWS Fargate 與 AWS Lambda。EC2 執行個體將執行資料匯入層，此層使用量零星且不可預測，而且執行在 EC2 上的工作負載可以隨時被中斷。應用程式前端將執行在 Fargate 上，API 層將由 Lambda 提供服務，而前端與 API 層在未來一年內的使用量都可預測。哪一組購買選項最具成本效益？（選兩項）

**選項**
- A. 資料匯入層使用 Spot Instances。
- B. 資料匯入層使用 On-Demand Instances。
- C. 為前端與 API 層購買 1 年期 Compute Savings Plan。
- D. 為資料匯入層購買 1 年期 All Upfront Reserved Instances。
- E. 為前端與 API 層購買 1 年期 EC2 Instance Savings Plan。

**答案**  
AC

---

## Question #141

**題目**  
某公司執行一個 Web 入口網站，提供全球即時新聞、本地警示與天氣更新。入口網站會以靜態與動態內容混合方式，為每位使用者提供個人化畫面。內容透過 HTTPS 提供，後端 API 伺服器執行於 Amazon EC2 執行個體上，並位於 Application Load Balancer (ALB) 後方。公司希望此入口網站能盡快地將內容提供給全球使用者。解決方案架構師應如何設計此應用程式，以確保所有使用者的延遲最低？

**選項**
- A. 將應用程式堆疊部署在單一 AWS 區域，並使用 Amazon CloudFront 同時提供靜態與動態內容。
- B. 將應用程式堆疊部署在兩個 AWS 區域，並使用 Amazon Route 53 latency routing policy，從延遲最低的區域 ALB 提供所有內容。
- C. 將應用程式堆疊部署在單一 AWS 區域，使用 Amazon CloudFront 提供靜態內容，動態內容則直接由 ALB 提供。
- D. 將應用程式堆疊部署在兩個 AWS 區域，並使用 Amazon Route 53 geolocation routing policy，從對應區域的 ALB 提供所有內容。

**答案**  
B

---

## Question #142

**題目**  
某遊戲公司正在設計高可用架構。應用程式執行於經修改的 Linux 核心上，且僅支援 UDP 流量。公司需要前端層提供最佳使用者體驗，此層必須具備低延遲、能將流量導向最近的 edge location，並提供靜態 IP 作為應用程式端點入口。解決方案架構師應該怎麼做？

**選項**
- A. 設定 Amazon Route 53 將請求導向 Application Load Balancer，並在 AWS 上以 AWS Lambda 執行應用程式。
- B. 設定 Amazon CloudFront 將請求導向 Network Load Balancer，並在 AWS 上以 AWS Lambda 執行應用程式。
- C. 設定 AWS Global Accelerator 將請求導向 Network Load Balancer，並在 AWS 區域中以 Amazon EC2 執行個體執行應用程式。
- D. 設定 Amazon API Gateway 將請求導向 Application Load Balancer，並在 AWS 區域中以 Amazon EC2 執行個體執行應用程式。

**答案**  
C

---

## Question #143

**題目**  
某公司想將現有的內部部署單體式應用程式遷移到 AWS。公司希望盡可能保留前端與後端程式碼，但也希望將應用程式拆分成較小的服務，並由不同團隊各自管理。公司需要高可擴展性，且希望將營運負擔降到最低。哪個方案符合需求？

**選項**
- A. 將應用程式部署在 AWS Lambda 上，並整合 Amazon API Gateway。
- B. 使用 AWS Amplify 託管應用程式，並連接到整合 AWS Lambda 的 Amazon API Gateway API。
- C. 將應用程式部署在 Amazon EC2 執行個體上，並以設於 Auto Scaling 群組前方的 Application Load Balancer 提供服務。
- D. 將應用程式部署在 Amazon Elastic Container Service (Amazon ECS) 上，並設定 Application Load Balancer 將流量導向 Amazon ECS。

**答案**  
D

---

## Question #144

**題目**  
某公司最近開始使用 Amazon Aurora 作為全球電商應用程式的資料儲存。當大型報表執行時，開發人員反映電商應用程式效能不佳。某位解決方案架構師檢視 Amazon CloudWatch 指標後，發現每月報表執行時，ReadIOPS 與 CPUUtilization 都會大幅上升。哪個方案最具成本效益？

**選項**
- A. 將每月報表工作遷移到 Amazon Redshift。
- B. 將每月報表工作遷移到 Aurora Replica。
- C. 將 Aurora 資料庫升級為更大的 instance class。
- D. 增加 Aurora 執行個體的 Provisioned IOPS。

**答案**  
B

---

## Question #145

**題目**  
某公司將網站分析應用程式部署在單一 Amazon EC2 On-Demand Instance 上。分析軟體以 PHP 撰寫，並使用 MySQL 資料庫。分析軟體、提供 PHP 的 Web 伺服器，以及資料庫伺服器都部署在同一台 EC2 執行個體上。系統在繁忙時段已出現效能下降與 5xx 錯誤。公司需要讓應用程式能夠平順擴展，且最具成本效益。哪個方案符合需求？

**選項**
- A. 將資料庫遷移到 Amazon RDS for MySQL DB instance。為 Web 應用程式建立 AMI，並用 AMI 再啟動第二台執行個體。
- B. 將資料庫遷移到 Amazon RDS for MySQL DB instance。為 Web 應用程式建立 AMI，並用該 AMI 在 Auto Scaling 中啟動多台執行個體。
- C. 將資料庫遷移到 Amazon Aurora MySQL DB instance。建立 AWS Lambda 函數來停止 EC2 執行個體並調整容量。
- D. 將資料庫遷移到 Amazon Aurora MySQL DB instance。為 Web 應用程式建立 AMI，並將 AMI 套用到 launch template 中。

**答案**  
D

---

## Question #146

**題目**  
某公司在生產環境中，將一個無狀態 Web 應用程式部署在一組 Amazon EC2 On-Demand Instances 上，並位於 Application Load Balancer 後方。此應用程式在每個工作日約有 8 小時使用量很高；夜間使用量穩定但中等；週末使用量很低。公司希望在不影響可用性的情況下，將 EC2 成本降到最低。哪個方案可滿足需求？

**選項**
- A. 整個工作負載全部使用 Spot Instances。
- B. 將基礎使用量改用 Reserved Instances，額外所需容量再使用 Spot Instances。
- C. 將基礎使用量改用 On-Demand Instances，額外所需容量再使用 Spot Instances。
- D. 將基礎使用量改用 Dedicated Instances，額外所需容量再使用 On-Demand Instances。

**答案**  
B

---

## Question #147

**題目**  
某公司必須保留關鍵應用程式的日誌檔 10 年。應用團隊會定期存取最近 1 個月內的日誌來進行除錯，但超過 1 個月的日誌幾乎很少被讀取。該應用程式每月會產生超過 10 TB 的日誌。哪種儲存方案最具成本效益？

**選項**
- A. 將日誌存放在 Amazon S3，並使用 AWS Backup 把超過 1 個月的日誌移到 S3 Glacier Deep Archive。
- B. 將日誌存放在 Amazon S3，並使用 S3 Lifecycle policies 把超過 1 個月的日誌移到 S3 Glacier Deep Archive。
- C. 將日誌存放在 Amazon CloudWatch Logs，並使用 AWS Backup 把超過 1 個月的日誌移到 S3 Glacier Deep Archive。
- D. 將日誌存放在 Amazon CloudWatch Logs，並使用 Amazon S3 Lifecycle policies 把超過 1 個月的日誌移到 S3 Glacier Deep Archive。

**答案**  
B

---

## Question #148

**題目**  
某公司有一個資料匯入工作流程，包含以下元件：一個 Amazon Simple Notification Service (Amazon SNS) 主題，用來接收新資料送達的通知；以及一個 AWS Lambda 函數，用來處理並儲存資料。這個工作流程偶爾會因網路連線問題而失敗。當失敗發生時，除非公司手動重新執行作業，對應的資料就不會被匯入。解決方案架構師應該怎麼做，才能確保所有通知最終都能被處理？

**選項**
- A. 將 Lambda 函數設定為跨多個可用區部署。
- B. 修改 Lambda 函數設定，提高 CPU 與記憶體配置。
- C. 調整 SNS 主題的 retry strategy，增加重試次數與每次重試的等待時間。
- D. 設定 Amazon Simple Queue Service (Amazon SQS) 佇列作為 on-failure destination，並修改 Lambda 函數去處理該佇列中的訊息。

**答案**  
D

---

## Question #149

**題目**  
某公司有一項服務會產生事件資料，並希望在資料接收時就於 AWS 上進行處理。這些資料具有特定順序，且在整個處理過程中都必須維持順序。公司希望此方案能將營運負擔降到最低。解決方案架構師應該怎麼做？

**選項**
- A. 建立 Amazon Simple Queue Service (Amazon SQS) FIFO queue 來保存訊息，並設定 AWS Lambda 函數處理這些訊息。
- B. 建立 Amazon Simple Notification Service (Amazon SNS) 主題來傳送包含待處理資料的通知，並設定後續處理。
- C. 建立 Amazon Simple Queue Service (Amazon SQS) standard queue 來保存訊息，並設定 AWS Lambda 函數處理這些訊息。
- D. 建立 Amazon Simple Notification Service (Amazon SNS) 主題來傳送包含待處理資料的通知，並設定其他處理流程。

**答案**  
A

---

## Question #150

**題目**  
某公司正在將應用程式從內部部署伺服器遷移到 Amazon EC2 執行個體。作為遷移設計的一部分，某位解決方案架構師必須實作基礎設施指標警示。公司不需要因短時間 CPU 使用率超過 50% 就採取行動；但如果 CPU 使用率超過 50%，且同時磁碟的 read IOPS 也很高，就必須盡快處理。此外，該架構師還必須降低誤報。應該怎麼做？

**選項**
- A. 盡可能建立 Amazon CloudWatch composite alarms。
- B. 建立 Amazon CloudWatch dashboards 來視覺化指標，並快速反應問題。
- C. 建立 Amazon CloudWatch Synthetics canaries 來監控應用程式並觸發警示。
- D. 盡可能建立具多重門檻的單一 Amazon CloudWatch metric alarms。

**答案**  
A

