# AWS SAA 第 200～250 題繁中翻譯

僅保留：題號、題目、選項、答案。

## Question #200

**題目**
某公司將其應用程式託管在 AWS 上，並使用 Amazon Cognito 管理使用者。當使用者登入應用程式時，應用程式會透過託管於 Amazon API Gateway 的 REST API，從 Amazon DynamoDB 擷取所需資料。公司希望採用 AWS 受管解決方案來控管對此 REST API 的存取，以降低開發工作量。哪一個解決方案能以**最低營運負擔**滿足這些需求？

**選項**
- A. 在 API Gateway 中設定 AWS Lambda 函數作為 authorizer，用來驗證是哪一位使用者提出請求。
- B. 為每位使用者建立並指派一組 API key，且每次請求都必須傳送該 key，再以 AWS Lambda 函數驗證此 key。
- C. 在每次請求的標頭中傳送使用者的電子郵件地址，並呼叫 AWS Lambda 函數驗證該使用者是否有權提出此請求。
- D. 在 API Gateway 中設定 Amazon Cognito user pool authorizer，讓 Amazon Cognito 驗證每一次請求。

**答案**
A

## Question #201

**題目**
某公司正在開發一套面向行動應用程式使用者的行銷溝通服務。公司需要透過簡訊服務（SMS）傳送確認訊息給使用者，且使用者必須能回覆這些簡訊。公司還必須將回覆內容保存 1 年以供分析。解決方案架構師應如何設計以滿足這些需求？

**選項**
- A. 建立 Amazon Connect 聯絡流程（contact flow）來傳送 SMS 訊息，並使用 AWS Lambda 處理回覆。
- B. 建立 Amazon Pinpoint journey，並設定 Amazon Pinpoint 將事件傳送至 Amazon Kinesis data stream 以供分析與保存。
- C. 使用 Amazon Simple Queue Service（Amazon SQS）分發 SMS 訊息，並使用 AWS Lambda 處理回覆。
- D. 建立 Amazon Simple Notification Service（Amazon SNS）FIFO topic，並將 Amazon Kinesis data stream 訂閱到該 SNS topic。

**答案**
A

## Question #202

**題目**
某公司計畫將資料移轉到 Amazon S3 bucket。資料儲存在 S3 bucket 時必須加密，且加密金鑰必須每年自動輪替。哪一個解決方案能以**最低營運負擔**滿足這些需求？

**選項**
- A. 將資料移到 S3 bucket，使用 Amazon S3 受管金鑰的伺服器端加密（SSE-S3），並使用內建金鑰輪替機制。
- B. 建立 AWS Key Management Service（AWS KMS）客戶受管金鑰，啟用自動金鑰輪替，並將 S3 bucket 的預設加密設為使用該金鑰。
- C. 建立 AWS KMS 客戶受管金鑰，將 S3 bucket 的預設加密設為使用該金鑰，但不啟用自動輪替。
- D. 在將資料移到 S3 bucket 之前，先以客戶自有金鑰資料加密，再建立 AWS KMS 金鑰供後續管理。

**答案**
B

## Question #203

**題目**
某金融公司的客戶會透過簡訊向理財顧問預約諮詢。執行於 Amazon EC2 執行個體上的 Web 應用程式會接收預約請求，並透過該 Web 應用程式將簡訊內容發布到 Amazon Simple Queue Service（Amazon SQS）queue。另一個執行於 EC2 執行個體上的應用程式會傳送會議邀請與會議確認電子郵件給客戶。在成功排程後，此應用程式會將會議資訊儲存到 Amazon DynamoDB 資料庫。隨著公司擴張，客戶回報收到會議邀請的時間越來越久。解決方案架構師應建議什麼作法來解決此問題？

**選項**
- A. 在 DynamoDB 資料庫前加上 DynamoDB Accelerator（DAX）叢集。
- B. 在接收預約請求的 Web 應用程式前加上 Amazon API Gateway API。
- C. 新增 Amazon CloudFront distribution，並將接收預約請求的 Web 應用程式設為 origin。
- D. 為傳送會議邀請的應用程式新增 Auto Scaling group，並根據 SQS queue 的相關指標進行擴縮。

**答案**
D

## Question #204

**題目**
某線上零售公司擁有超過 5,000 萬名活躍客戶，每天接收超過 25,000 筆訂單。公司會蒐集客戶購買資料並儲存在 Amazon S3，額外的客戶資料則儲存在 Amazon RDS。公司希望讓各個團隊都能存取所有資料以進行分析。解決方案必須能對資料進行細粒度權限管理，且要將營運負擔降到最低。哪一個解決方案能滿足這些需求？

**選項**
- A. 將購買資料直接改寫入 Amazon RDS，並使用 RDS 存取控制限制權限。
- B. 排程 AWS Lambda 函數定期將資料從 Amazon RDS 複製到 Amazon S3，建立 AWS Glue crawler，再用其他服務進行分析與權限管理。
- C. 使用 AWS Lake Formation 建立 data lake，建立連到 Amazon RDS 的 AWS Glue JDBC connection，並在 Lake Formation 中註冊 S3 bucket。
- D. 建立 Amazon Redshift 叢集，並排程 AWS Lambda 函數定期將 Amazon S3 與 Amazon RDS 的資料複製到 Redshift。

**答案**
D

## Question #205

**題目**
某公司在內部部署資料中心託管一個行銷網站。該網站由靜態文件組成並執行於單一伺服器上。管理員不常更新網站內容，並使用 SFTP 用戶端上傳新文件。公司決定將網站託管到 AWS，並使用 Amazon CloudFront。公司的解決方案架構師已建立 CloudFront distribution。現在必須設計一個**最具成本效益且具備韌性**的網站託管架構，作為 CloudFront 的 origin。哪一個解決方案能滿足這些需求？

**選項**
- A. 使用 Amazon Lightsail 建立虛擬伺服器，在 Lightsail 執行個體上設定 Web 伺服器，並透過 SFTP 上傳網站內容。
- B. 為 Amazon EC2 執行個體建立 AWS Auto Scaling group，搭配 Application Load Balancer，並使用 EC2 上傳網站內容。
- C. 建立私有 Amazon S3 bucket，使用 S3 bucket policy 允許來自 CloudFront origin access identity（OAI）的存取，並將網站內容上傳至該 bucket。
- D. 建立公開 Amazon S3 bucket，設定 AWS Transfer for SFTP，將 S3 bucket 設定為網站託管，並透過 SFTP 上傳網站內容。

**答案**
C

## Question #206

**題目**
某公司想要管理 Amazon Machine Images（AMIs）。目前公司會將 AMI 複製到建立該 AMI 的同一個 AWS Region。公司需要設計一個應用程式，能擷取 AWS API 呼叫，並在公司帳號內發生 Amazon EC2 CreateImage API 操作時發送警示。哪一個解決方案能以**最低營運負擔**滿足這些需求？

**選項**
- A. 建立 AWS Lambda 函數查詢 AWS CloudTrail logs，並在偵測到 CreateImage API 呼叫時發送警示。
- B. 設定 AWS CloudTrail，並在日誌更新時透過 Amazon Simple Notification Service（Amazon SNS）發送通知。
- C. 針對 CreateImage API 呼叫建立 Amazon EventBridge（Amazon CloudWatch Events）rule，並將目標設定為 Amazon SNS topic。
- D. 將 Amazon Simple Queue Service（Amazon SQS）FIFO queue 設為 AWS CloudTrail logs 的目標，再建立 AWS Lambda 函數處理並警示。

**答案**
D

## Question #207

**題目**
某公司擁有一個非同步 API，用來接收使用者請求，並依據請求類型將請求分派給適當的微服務處理。公司使用 Amazon API Gateway 作為 API 前端，並使用會呼叫 Amazon DynamoDB 的 AWS Lambda 函數，先儲存使用者請求再分派到處理微服務。公司已依預算盡可能提高 DynamoDB 吞吐量，但仍持續遇到可用性問題，並遺失使用者請求。在不影響現有使用者的前提下，解決方案架構師應如何處理此問題？

**選項**
- A. 在 API Gateway 上新增節流（throttling）並使用伺服器端節流限制。
- B. 使用 DynamoDB Accelerator（DAX）與 Lambda 來緩衝寫入 DynamoDB 的流量。
- C. 為儲存使用者請求的 DynamoDB 資料表建立 secondary index。
- D. 使用 Amazon Simple Queue Service（Amazon SQS）queue 與 Lambda 來緩衝寫入 DynamoDB 的流量。

**答案**
D

## Question #208

**題目**
某公司需要將資料從 Amazon EC2 執行個體移到 Amazon S3 bucket。公司必須確保所有 API 呼叫與資料傳輸都不會經過公網路由，且只有該 EC2 執行個體可以上傳資料到 S3 bucket。哪一個解決方案能滿足這些需求？

**選項**
- A. 在 EC2 所在子網路中為 Amazon S3 建立 interface VPC endpoint，並將 resource policy 套用到 S3 bucket。
- B. 在 EC2 所在可用區中為 Amazon S3 建立 gateway VPC endpoint，並套用適當的安全設定與 S3 bucket policy。
- C. 在 EC2 執行個體內執行 nslookup 取得 S3 service API endpoint 的私有 IP，再手動建立路由。
- D. 使用 AWS 提供的公開 ip-ranges.json 檔案取得 S3 service API endpoint 的私有 IP。

**答案**
B

## Question #209

**題目**
某解決方案架構師正在為即將部署到 AWS Cloud 的新應用程式設計架構。應用程式將執行於 Amazon EC2 On-Demand Instances 上，並會在多個 Availability Zones 間自動擴縮。EC2 執行個體會在一天中頻繁地擴增與縮減。Application Load Balancer（ALB）將負責負載分配。此架構需要支援分散式工作階段（session）資料管理，公司也願意在必要時修改程式碼。為了確保架構支援分散式 session 資料管理，解決方案架構師應怎麼做？

**選項**
- A. 使用 Amazon ElastiCache 管理並儲存 session 資料。
- B. 使用 ALB 的 session affinity（sticky sessions）管理 session 資料。
- C. 使用 AWS Systems Manager 的 Session Manager 管理 session。
- D. 使用 AWS Security Token Service（AWS STS）的 GetSessionToken API 管理 session。

**答案**
A

## Question #210

**題目**
某公司提供食品外送服務，且業務成長快速。由於成長帶來的流量高峰，公司現有的訂單處理系統在尖峰時段出現擴展問題。現有架構包含：
- 一組執行於 Amazon EC2 Auto Scaling group 中的 Amazon EC2 執行個體，用來從應用程式收集訂單。
- 另一組執行於 Amazon EC2 Auto Scaling group 中的 EC2 執行個體，用來完成訂單履行。
訂單收集流程進行得很快，但訂單履行流程可能需要較長時間。資料不得因擴縮事件而遺失。解決方案架構師必須確保在流量高峰時，訂單收集與訂單履行流程都能正確擴展，同時最佳化 AWS 資源利用率。哪一個解決方案能滿足這些需求？

**選項**
- A. 使用 Amazon CloudWatch metrics 監控各 Auto Scaling group 中每台執行個體的 CPU，並依 CPU 指標設定各自的擴縮政策。
- B. 使用 Amazon CloudWatch metrics 監控各 Auto Scaling group 中每台執行個體的 CPU，並設定 CloudWatch alarm 觸發擴縮。
- C. 佈建兩個 Amazon Simple Queue Service（Amazon SQS）queues：一個用於訂單收集，另一個用於訂單履行，並依 queue 指標設定各階段的擴縮。
- D. 佈建兩個 Amazon Simple Queue Service（Amazon SQS）queues：一個用於訂單收集，另一個用於訂單履行，並以其他方式配置擴縮與處理流程。

**答案**
C

## Question #211

**題目**
某公司託管多個正式環境應用程式。其中一個應用程式由跨多個 AWS Regions 的 Amazon EC2、AWS Lambda、Amazon RDS、Amazon Simple Notification Service（Amazon SNS）與 Amazon Simple Queue Service（Amazon SQS）資源組成。所有公司資源都使用名為 `application` 的 tag，且其值對應到各個應用程式。解決方案架構師需要用**最快速**的方式識別所有具有該 tag 的元件。哪一個解決方案能滿足這些需求？

**選項**
- A. 使用 AWS CloudTrail 產生具有 application tag 的資源清單。
- B. 使用 AWS CLI 查詢所有 Regions 中的各項服務，以回報被標記的元件。
- C. 在 Amazon CloudWatch Logs Insights 中執行查詢，以回報具有 application tag 的元件。
- D. 使用 AWS Resource Groups Tag Editor 進行查詢，以全域方式回報具有 application tag 的資源。

**答案**
D

## Question #212

**題目**
某公司每天需要將資料庫匯出一次到 Amazon S3，供其他團隊存取。匯出的物件大小介於 2 GB 到 5 GB。此資料在 S3 上的存取模式變化大且變動快速。資料必須可立即取得，且需保留可存取狀態最多 3 個月。公司需要最具成本效益且不會增加擷取延遲的解決方案。應使用哪一種 S3 儲存類別？

**選項**
- A. S3 Intelligent-Tiering
- B. S3 Glacier Instant Retrieval
- C. S3 Standard
- D. S3 Standard-Infrequent Access（S3 Standard-IA）

**答案**
A

## Question #213

**題目**
某公司正在開發新的行動應用程式。公司必須實作適當的流量過濾，以保護其 Application Load Balancer（ALB）免受常見應用層攻擊，例如跨站腳本（XSS）或 SQL injection。公司只有極少的基礎設施與營運人員，且希望降低自己在 AWS 環境中管理、更新與保護伺服器的責任比例。解決方案架構師應建議什麼作法來滿足這些需求？

**選項**
- A. 設定 AWS WAF 規則並將其關聯到 ALB。
- B. 使用 Amazon S3 並啟用公開託管來部署應用程式。
- C. 部署 AWS Shield Advanced，並將 ALB 加入受保護資源。
- D. 建立新的 ALB，將流量導向執行第三方防火牆的 Amazon EC2 執行個體，再由其轉送流量。

**答案**
A

## Question #214

**題目**
某公司的報表系統每天會將數百個 `.csv` 檔案傳送到 Amazon S3 bucket。公司必須將這些檔案轉換為 Apache Parquet 格式，並儲存到一個轉換後資料的 bucket 中。哪一個解決方案能以**最少開發工作量**滿足這些需求？

**選項**
- A. 建立安裝 Apache Spark 的 Amazon EMR 叢集，撰寫 Spark 應用程式轉換資料，並使用 EMR File System 輸出結果。
- B. 建立 AWS Glue crawler 來探索資料，再建立 AWS Glue ETL job 轉換資料。
- C. 使用 AWS Batch 建立 Bash 語法的 job definition 來轉換資料，並輸出到轉換後資料 bucket。
- D. 建立 AWS Lambda 函數轉換資料並輸出到轉換後資料 bucket，並設定事件觸發。

**答案**
D

## Question #215

**題目**
某公司在資料中心的網路附加儲存（NAS）中儲存了 700 TB 備份資料。這些備份資料需要能供低頻率的法規查詢存取，且必須保留 7 年。公司決定將這些備份資料移轉到 AWS，且必須在 1 個月內完成。公司在公網連線上有 500 Mbps 的專用頻寬可供資料傳輸使用。解決方案架構師應如何以**最低成本**移轉並儲存這些資料？

**選項**
- A. 訂購 AWS Snowball 裝置進行資料傳輸，並使用 lifecycle policy 將檔案轉移到 Amazon S3 Glacier Deep Archive。
- B. 在資料中心與 Amazon VPC 之間部署 VPN 連線，並使用 AWS CLI 將資料從內部部署複製到 Amazon S3。
- C. 佈建 500 Mbps 的 AWS Direct Connect 連線並將資料傳到 Amazon S3，再使用 lifecycle policy 轉移檔案。
- D. 使用 AWS DataSync 傳輸資料並在內部部署環境部署 DataSync agent，透過 DataSync task 將 NAS 上的檔案複製到 AWS。

**答案**
A

## Question #216

**題目**
某公司有一個無伺服器網站，且在 Amazon S3 bucket 中有數百萬個物件。公司將該 S3 bucket 作為 Amazon CloudFront distribution 的 origin。這些物件上傳前，S3 bucket 並未先設定加密。解決方案架構師現在需要為所有既有物件與未來新增到 S3 bucket 的物件啟用加密。哪一個解決方案能以**最少工作量**滿足這些需求？

**選項**
- A. 建立新的 S3 bucket，開啟新 bucket 的預設加密設定，先將所有既有物件下載到本機暫存，再重新上傳到新 bucket。
- B. 開啟目前 S3 bucket 的預設加密設定，使用 S3 Inventory 建立一份列出未加密物件的 `.csv` 檔案，再批次重新加密這些物件。
- C. 使用 AWS Key Management Service（AWS KMS）建立新的加密金鑰，並變更 S3 bucket 設定改用該金鑰進行伺服器端加密。
- D. 進入 Amazon S3 主控台瀏覽該 S3 bucket 內的物件，依加密欄位排序後逐一選取未加密物件並手動更新。

**答案**
B

## Question #217

**題目**
某公司在 Amazon EC2 執行個體上執行一個全球性 Web 應用程式，前方有 Application Load Balancer，應用程式資料儲存在 Amazon Aurora。公司需要建立災難復原（DR）方案，且可容忍最多 30 分鐘的停機時間與資料遺失。當主要基礎設施健康時，次要方案不需要承擔流量。解決方案架構師應如何設計以滿足這些需求？

**選項**
- A. 在第二個 AWS Region 完整部署應用程式所需基礎設施，並使用 Amazon Route 53 設定主動/被動（active-passive）容錯移轉。
- B. 在第二個 AWS Region 部署縮小版應用程式，並使用 Amazon Route 53 設定主動/主動（active-active）容錯移轉。
- C. 在第二個 AWS Region 複製主要基礎設施，使用 Amazon Route 53 設定 active-active，並建立 Aurora 複寫架構。
- D. 使用 AWS Backup 備份資料，必要時用備份在第二個 AWS Region 建立所需基礎設施，並使用 Amazon Route 53 進行容錯移轉。

**答案**
D

## Question #218

**題目**
某公司有一台 Web 伺服器執行在公有子網路中的 Amazon EC2 執行個體上，並綁定 Elastic IP。該 EC2 執行個體目前使用預設 security group。預設 network ACL 已被修改為封鎖所有流量。解決方案架構師需要讓這台 Web 伺服器可從任何地方透過 443 連接。哪一組步驟可以達成這個目標？（選兩項）

**選項**
- A. 建立一個 security group，新增允許來源 `0.0.0.0/0` 進入 TCP 443 的規則。
- B. 建立一個 security group，新增允許目的地 `0.0.0.0/0` 的 TCP 443 規則。
- C. 更新 network ACL，允許來自來源 `0.0.0.0/0` 的 TCP 443 流量。
- D. 更新 network ACL，允許 inbound/outbound TCP 443，來源與目的地都為 `0.0.0.0/0`。
- E. 更新 network ACL，允許來源 `0.0.0.0/0` 的 inbound TCP 443，並允許回應流量所需的 outbound TCP 32768-65535 到外部。

**答案**
AE

## Question #219

**題目**
某公司的應用程式發生效能問題。該應用程式是有狀態（stateful）的，且需要在 Amazon EC2 執行個體上完成記憶體內運算工作。公司使用 AWS CloudFormation 部署基礎設施，並採用 M5 EC2 instance family。隨著流量增加，應用程式效能下降，使用者在存取應用程式時回報延遲。哪一個解決方案能以**最高營運效率**解決這些問題？

**選項**
- A. 以 AWS Management Console 將 EC2 執行個體替換為 T3 執行個體，並改放入 Auto Scaling group。
- B. 修改 CloudFormation templates，讓 EC2 執行個體運行於 Auto Scaling group 中，並提高 desired capacity 與最大容量。
- C. 修改 CloudFormation templates，將 EC2 執行個體替換為 R5 執行個體，並使用 Amazon CloudWatch 內建 EC2 記憶體監控。
- D. 修改 CloudFormation templates，將 EC2 執行個體替換為 R5 執行個體，並部署 Amazon CloudWatch agent 來蒐集記憶體指標。

**答案**
D

## Question #220

**題目**
某解決方案架構師正在設計一個新的 API，會透過 Amazon API Gateway 接收使用者請求。請求量高度不固定，可能數小時都沒有任何請求。資料處理將以非同步方式進行，但應在請求發出後幾秒內完成。為了以最低成本滿足需求，API 應呼叫哪一種運算服務？

**選項**
- A. AWS Glue job
- B. AWS Lambda function
- C. 託管於 Amazon Elastic Kubernetes Service（Amazon EKS）的容器化服務
- D. 託管於 Amazon ECS with Amazon EC2 的容器化服務

**答案**
B

## Question #221

**題目**
某公司在一組 Amazon Linux EC2 執行個體上執行應用程式。基於合規需求，公司必須保留所有應用程式日誌檔 7 年。報表工具需要能同時存取所有日誌檔進行分析。哪一種儲存方案最具成本效益且能滿足這些需求？

**選項**
- A. Amazon Elastic Block Store（Amazon EBS）
- B. Amazon Elastic File System（Amazon EFS）
- C. Amazon EC2 instance store
- D. Amazon S3

**答案**
D

## Question #222

**題目**
某公司聘請外部廠商在其 AWS 帳號中執行作業。該廠商使用一個自動化工具，而此工具託管在廠商自有的 AWS 帳號中。廠商本身沒有公司 AWS 帳號的 IAM 存取權限。解決方案架構師應如何將此存取權限安全地授予廠商？

**選項**
- A. 在公司帳號中建立 IAM role，將存取權委派給廠商帳號中的 IAM role，並在該角色上附加適當的 IAM policies。
- B. 在公司帳號中建立 IAM user，設定符合密碼複雜度要求的密碼，並附加所需權限。
- C. 在公司帳號中建立 IAM group，將廠商帳號中工具的 IAM user 加入此群組，再附加適當的 IAM policies。
- D. 在 IAM console 中新增新的 identity provider，選擇「AWS account」作為 provider type，並輸入廠商的 AWS account ID。

**答案**
A

## Question #223

**題目**
某公司已將 Java Spring Boot 應用程式部署為 pod，執行於私有子網路中的 Amazon Elastic Kubernetes Service（Amazon EKS）上。該應用程式需要將資料寫入 Amazon DynamoDB table。解決方案架構師必須確保應用程式可以在**不將流量暴露到網際網路**的前提下與 DynamoDB table 互動。應採取哪兩項措施？（選兩項）

**選項**
- A. 將具有足夠權限的 IAM role 附加到 EKS pod。
- B. 將具有足夠權限的 IAM user 附加到 EKS pod。
- C. 透過私有子網路的 network ACL 允許連到 DynamoDB table 的對外連線。
- D. 為 DynamoDB 建立 VPC endpoint。
- E. 將 access keys 直接嵌入 Java Spring Boot 程式碼中。

**答案**
AD

## Question #224

**題目**
某公司最近已將 Web 應用程式重新託管到單一 AWS Region 的 Amazon EC2 執行個體上。公司想重新設計應用程式架構，使其具備高可用性與容錯能力。流量必須能隨機分配到所有正在執行的 EC2 執行個體。公司應採取哪兩項措施來滿足這些需求？（選兩項）

**選項**
- A. 建立 Amazon Route 53 failover routing policy。
- B. 建立 Amazon Route 53 weighted routing policy。
- C. 建立 Amazon Route 53 multivalue answer routing policy。
- D. 啟動 3 台 EC2 執行個體：其中 2 台位於同一個 Availability Zone，另 1 台位於另一個 Availability Zone。
- E. 啟動 4 台 EC2 執行個體：在兩個不同的 Availability Zones 中各放 2 台。

**答案**
CE

## Question #225

**題目**
某媒體公司在內部部署環境中蒐集並分析使用者活動資料，現在希望將此能力移轉到 AWS。使用者活動資料儲存體會持續成長，且規模將達到 PB 等級。公司需要建立一個高可用性的資料擷取方案，讓現有資料與新資料都能支援以 SQL 進行隨需分析，並且要把營運負擔降到最低。哪一個解決方案能滿足這些需求？

**選項**
- A. 將活動資料傳送到 Amazon Kinesis data stream，並設定該 stream 將資料寫入 Amazon S3 bucket。
- B. 將活動資料傳送到 Amazon Kinesis Data Firehose delivery stream，並設定該 stream 將資料寫入 Amazon Redshift。
- C. 將活動資料放入 Amazon S3 bucket，並設定 Amazon S3 在資料寫入 bucket 時觸發 AWS Lambda function 處理資料。
- D. 在跨多個 Availability Zones 的 Amazon EC2 執行個體上建立一個資料擷取服務，並由此服務處理資料流。

**答案**
A

## Question #226

**題目**
某公司透過執行於 Amazon EC2 執行個體上的 RESTful Web 服務應用程式，從數千個遠端裝置收集資料。該 EC2 執行個體會接收原始資料、轉換原始資料，並將所有資料儲存到 Amazon S3 bucket。很快地，遠端裝置數量將增加到數百萬台。公司需要一個高可擴展且能將營運負擔降到最低的解決方案。解決方案架構師應採取哪兩項措施？（選兩項）

**選項**
- A. 使用 AWS Glue 處理 Amazon S3 中的原始資料。
- B. 使用 Amazon Route 53 將流量導向不同的 EC2 執行個體。
- C. 增加更多 EC2 執行個體來容納日益增加的資料流入量。
- D. 將原始資料送到 Amazon Simple Queue Service（Amazon SQS），再由 EC2 執行個體處理資料。
- E. 使用 Amazon API Gateway 將原始資料送到 Amazon Kinesis data stream，並設定 Amazon Kinesis Data Firehose 使用該資料進行後續處理與儲存。

**答案**
AE

## Question #227

**題目**
某公司必須保留其 AWS CloudTrail logs 3 年。公司透過 AWS Organizations 從父帳號統一強制啟用 CloudTrail。CloudTrail 的目標 S3 bucket 已啟用 S3 Versioning，且已設定 S3 Lifecycle policy 在 3 年後刪除 current objects。使用第 4 年後，S3 bucket 指標顯示物件數量持續上升，但新寫入的 CloudTrail logs 數量維持一致。哪一個解決方案能以**最具成本效益**的方式刪除 3 年以上的物件？

**選項**
- A. 設定組織層級的集中式 CloudTrail trail，在 3 年後讓物件過期。
- B. 設定 S3 Lifecycle policy，同時刪除 current versions 與 previous versions。
- C. 建立 AWS Lambda 函數列舉並刪除 Amazon S3 中超過 3 年的物件。
- D. 將父帳號設為所有傳送到 S3 bucket 物件的擁有者。

**答案**
B

## Question #228

**題目**
某公司有一個 API，會從一組監控裝置即時接收資料。API 會將這些資料儲存在 Amazon RDS DB instance 中供後續分析。監控裝置送到 API 的資料量會波動，在高流量時段，API 經常回傳 timeout errors。檢查日誌後，公司發現資料庫無法處理來自 API 的大量寫入流量。解決方案架構師必須將資料庫連線數降到最低，並確保在高流量時不會遺失資料。哪一個解決方案能滿足這些需求？

**選項**
- A. 將 DB instance 升級為擁有更多記憶體的 instance type。
- B. 將 DB instance 修改為 Multi-AZ DB instance，並設定應用程式將資料寫入所有作用中的 RDS DB instances。
- C. 修改 API，將傳入資料先寫入 Amazon Simple Queue Service（Amazon SQS）queue，再使用 AWS Lambda function 將資料寫入資料庫。
- D. 修改 API，將傳入資料寫入 Amazon Simple Notification Service（Amazon SNS）topic，再使用 AWS Lambda function 將資料寫入資料庫。

**答案**
C

## Question #229

**題目**
某公司自行管理執行 MySQL 資料庫的 Amazon EC2 執行個體，並且隨著需求增減手動處理複寫與擴縮。公司需要一個新方案，能簡化資料庫層新增或移除運算容量的流程，並以最少的營運工作提供更好的效能、擴充性與耐久性。哪一個解決方案能滿足這些需求？

**選項**
- A. 將資料庫移轉到 Amazon Aurora Serverless for Aurora MySQL。
- B. 將資料庫移轉到 Amazon Aurora Serverless for Aurora PostgreSQL。
- C. 將多個資料庫合併成一個更大的 MySQL 資料庫，並在更大的 EC2 執行個體上運行。
- D. 為資料庫層建立 EC2 Auto Scaling group，並將現有資料庫移轉到新環境。

**答案**
A

## Question #230

**題目**
某公司擔心目前使用中的兩台 NAT instances 已無法支撐應用程式所需的流量。解決方案架構師希望實作一個具備高可用、容錯且可自動擴展的方案。應建議哪一個解決方案？

**選項**
- A. 移除兩台 NAT instances，改成在同一個 Availability Zone 中使用兩個 NAT gateways。
- B. 為位於不同 Availability Zones 的 NAT instances 搭配 Network Load Balancers 與 Auto Scaling groups。
- C. 移除兩台 NAT instances，改為在不同 Availability Zones 中部署兩個 NAT gateways。
- D. 用位於不同 Availability Zones 的 Spot Instances 取代兩台 NAT instances，並部署 Network Load Balancer。

**答案**
C

## Question #231

**題目**
某應用程式執行於 VPC A 中、具有 Elastic IP address 的 Amazon EC2 執行個體上。該應用程式需要存取位於 VPC B 中的資料庫。兩個 VPC 都位於同一個 AWS 帳號中。哪一個解決方案能以**最安全**的方式提供所需存取？

**選項**
- A. 建立資料庫執行個體的 security group，允許來自 VPC A 中應用程式伺服器公有 IP 的所有流量。
- B. 在 VPC A 與 VPC B 之間設定 VPC peering connection。
- C. 將 DB instance 設為 publicly accessible，並指派 public IP address。
- D. 在 VPC B 中啟動一台具有 Elastic IP 的 EC2 執行個體，並透過該新 EC2 執行個體代理所有請求。

**答案**
B

## Question #232

**題目**
某公司在 Amazon EC2 執行個體上為客戶執行展示環境，每個環境都隔離在自己的 VPC 中。公司的維運團隊需要在有人透過 RDP 或 SSH 連入展示環境時收到通知。

**選項**
- A. 設定 Amazon CloudWatch Application Insights，在偵測到 RDP 或 SSH 存取時建立 AWS Systems Manager OpsItems。
- B. 為 EC2 執行個體設定 IAM instance profile，並附加具有 AmazonSSMManagedInstanceCore policy 的 IAM role。
- C. 將 VPC flow logs 發佈到 Amazon CloudWatch Logs，建立所需的 metric filters，並建立搭配通知的 Amazon CloudWatch metric alarm。
- D. 設定 Amazon EventBridge rule，監聽 EC2 Instance State-change Notification 類型事件，並設定 Amazon Simple Notification Service（Amazon SNS）通知。

**答案**
C

## Question #233

**題目**
某解決方案架構師剛建立一個新的 AWS 帳號，現在必須保護 AWS account root user 的存取安全。哪兩項作法可以達成此目標？（選兩項）

**選項**
- A. 確保 root user 使用高強度密碼。
- B. 為 root user 啟用多重要素驗證（MFA）。
- C. 將 root user 的 access keys 儲存在加密的 Amazon S3 bucket 中。
- D. 將 root user 加入一個含有管理權限的群組中。
- E. 使用 inline policy document 為 root user 套用所需權限。

**答案**
AB

## Question #234

**題目**
某公司正在建置新的 Web 型客戶關係管理應用程式。該應用程式將使用多台 Amazon EC2 執行個體，並以 Amazon Elastic Block Store（Amazon EBS）volumes 作為儲存，前方有 Application Load Balancer（ALB）。應用程式也會使用 Amazon Aurora 資料庫。所有資料都必須在**靜態儲存**與**傳輸過程**中加密。哪一個解決方案能滿足這些需求？

**選項**
- A. 使用 AWS Key Management Service（AWS KMS）憑證在 ALB 上加密傳輸中的資料，並使用 AWS Certificate Manager（ACM）處理靜態資料加密。
- B. 使用 AWS root account 登入 AWS Management Console，上傳公司的加密憑證，並在 root account 下完成所有加密設定。
- C. 使用 AWS Key Management Service（AWS KMS）加密 EBS volumes 與 Aurora 儲存體中的靜態資料，並將 AWS Certificate Manager（ACM）憑證套用到 ALB 以加密傳輸中的資料。
- D. 使用 BitLocker 加密所有靜態資料，並將公司的 TLS 憑證金鑰匯入 AWS KMS。

**答案**
C

## Question #235

**題目**
某公司正將內部部署 Oracle 資料庫移轉到 Amazon Aurora PostgreSQL。資料庫有多個應用程式會寫入相同的資料表，且這些應用程式需要逐一移轉，每次移轉間隔 1 個月。管理階層擔心資料庫具有很高的讀寫量。整個移轉期間，來源與目標資料庫之間的資料都必須保持同步。解決方案架構師應建議什麼做法？

**選項**
- A. 使用 AWS DataSync 進行初始移轉，再使用 AWS Database Migration Service（AWS DMS）建立 change data capture（CDC）機制。
- B. 使用 AWS DataSync 進行初始移轉，再使用 AWS DMS 建立 full load 加上 CDC 機制。
- C. 使用 AWS Schema Conversion Tool 搭配 AWS Database Migration Service（AWS DMS），並使用 memory optimized replication instance。
- D. 使用 AWS Schema Conversion Tool 搭配 AWS Database Migration Service（AWS DMS），並使用 compute optimized replication instance。

**答案**
C

## Question #236

**題目**
某公司有一個三層式圖片分享應用程式。該應用程式使用一台 Amazon EC2 執行個體作為前端層、另一台 EC2 作為應用程式層，第三台 EC2 執行個體則執行 MySQL 資料庫。解決方案架構師必須設計一個可擴展且高可用的解決方案，並且要求對應用程式的變更最少。哪一個解決方案能滿足這些需求？

**選項**
- A. 使用 Amazon S3 託管前端層，應用程式層改用 AWS Lambda functions，並將資料庫移轉到 Amazon RDS。
- B. 以前端層與應用程式層皆採用具負載平衡的 Multi-AZ AWS Elastic Beanstalk 環境，並將資料庫移轉到受管資料庫服務。
- C. 使用 Amazon S3 託管前端層，應用程式層使用位於 Auto Scaling group 中的一組 EC2 執行個體，並移轉資料庫。
- D. 以前端層與應用程式層皆採用具負載平衡的 Multi-AZ AWS Elastic Beanstalk 環境，並將資料庫移轉到 Amazon Aurora。

**答案**
A

## Question #237

**題目**
執行於 VPC-A 中 Amazon EC2 執行個體上的應用程式，需要存取位於 VPC-B 另一台 EC2 執行個體中的檔案。兩個 VPC 位於不同的 AWS 帳號中。網路管理員需要設計一個安全的連線方案，讓 VPC-A 能存取 VPC-B 中的 EC2 執行個體，且此連線不應有單點故障，也不應有頻寬方面的顧慮。哪一個解決方案可滿足需求？

**選項**
- A. 在 VPC-A 與 VPC-B 之間建立 VPC peering connection。
- B. 為 VPC-B 中執行的 EC2 執行個體建立 VPC gateway endpoints。
- C. 將 virtual private gateway 附加到 VPC-B，並從 VPC-A 設定路由。
- D. 為 VPC-B 中執行的 EC2 執行個體建立 private virtual interface（VIF），並從 VPC-A 加入適當路由。

**答案**
A

## Question #238

**題目**
某公司希望讓工程師團隊使用各自獨立的 AWS 帳號進行實驗。公司希望在每個帳號中，當某個月份的 Amazon EC2 執行個體使用量超過特定門檻時，能盡快收到通知。解決方案架構師應如何以**最具成本效益**的方式滿足這項需求？

**選項**
- A. 使用 Cost Explorer 建立依服務區分的每日成本報表，篩選 EC2 instances，並設定 Cost Explorer 傳送通知。
- B. 使用 Cost Explorer 建立依服務區分的每月成本報表，篩選 EC2 instances，並設定 Cost Explorer 傳送通知。
- C. 使用 AWS Budgets 為每個帳號建立成本預算，週期設為每月，範圍限定為 EC2 instances，並設定達到門檻時發送警示。
- D. 使用 AWS Cost and Usage Reports 建立每小時粒度的報表，整合 Amazon Athena，再用其他機制發送通知。

**答案**
B

## Question #239

**題目**
某解決方案架構師需要為公司的應用程式設計一個新的微服務。用戶端必須能透過 HTTPS 端點呼叫此微服務，且此微服務還必須使用 AWS Identity and Access Management（IAM）驗證呼叫。解決方案架構師將使用以 Go 1.x 撰寫的單一 AWS Lambda function 來實作此微服務邏輯。哪一個解決方案能以**最高營運效率**部署此函數？

**選項**
- A. 建立 Amazon API Gateway REST API，將方法設定為使用該 Lambda function，並在 API 上啟用 IAM 驗證。
- B. 為 Lambda function 建立 Lambda function URL，並將驗證類型指定為 `AWS_IAM`。
- C. 建立 Amazon CloudFront distribution，將函數部署到 Lambda@Edge，並在邏輯中整合 IAM 驗證。
- D. 建立 Amazon CloudFront distribution，將函數部署到 CloudFront Functions，並將驗證類型指定為 `AWS_IAM`。

**答案**
A

## Question #240

**題目**
某公司先前已將資料倉儲解決方案移轉到 AWS，且公司也有一條 AWS Direct Connect 連線。企業辦公室使用視覺化工具查詢資料倉儲。每次查詢回傳的平均資料量為 50 MB，而視覺化工具傳送的每個網頁約為 500 KB。資料倉儲回傳的結果集不會被快取。哪一個解決方案能為公司提供**最低的資料傳出費用**？

**選項**
- A. 將視覺化工具部署在內部部署環境，並直接透過網際網路查詢資料倉儲。
- B. 將視覺化工具部署在與資料倉儲相同的 AWS Region，並透過網際網路存取。
- C. 將視覺化工具部署在內部部署環境，並透過 Direct Connect 連線在相應地點直接查詢資料倉儲。
- D. 將視覺化工具部署在與資料倉儲相同的 AWS Region，並透過某個 Direct Connect 地點進行存取。

**答案**
C

## Question #241

**題目**
某線上學習公司正遷移到 AWS Cloud，並將學生紀錄維護在 PostgreSQL 資料庫中。公司需要一個方案，使資料可在多個 AWS Regions 中持續保持可用且在線上。哪一個解決方案能以**最少營運負擔**滿足這些需求？

**選項**
- A. 將 PostgreSQL 資料庫移轉到執行於 Amazon EC2 執行個體上的 PostgreSQL 叢集。
- B. 將 PostgreSQL 資料庫移轉到啟用 Multi-AZ 的 Amazon RDS for PostgreSQL DB instance。
- C. 將 PostgreSQL 資料庫移轉到 Amazon RDS for PostgreSQL DB instance，並在另一個 Region 建立 read replica。
- D. 將 PostgreSQL 資料庫移轉到 Amazon RDS for PostgreSQL DB instance，並將 DB snapshots 複製到另一個 Region。

**答案**
C

## Question #242

**題目**
某公司在 AWS 上以 7 台 Amazon EC2 執行個體託管 Web 應用程式。公司要求在 DNS 查詢回應中，必須回傳所有健康 EC2 執行個體的 IP 位址。應使用哪一種路由政策來滿足這項需求？

**選項**
- A. Simple routing policy
- B. Latency routing policy
- C. Multivalue routing policy
- D. Geolocation routing policy

**答案**
C

## Question #243

**題目**
某醫學研究實驗室產生了一批與新研究相關的資料。實驗室希望讓全國各地的診所，能以**最低延遲**透過其內部部署的檔案型應用程式存取這些資料。資料檔案儲存在 Amazon S3 bucket 中，且每家診所都只有唯讀權限。解決方案架構師應建議什麼作法？

**選項**
- A. 在每家診所的內部部署環境中，以虛擬機器（VM）形式部署 AWS Storage Gateway file gateway。
- B. 使用 AWS DataSync 將檔案遷移到各診所的內部部署應用程式中供後續處理。
- C. 在每家診所的內部部署環境中，以虛擬機器（VM）形式部署 AWS Storage Gateway volume gateway。
- D. 將 Amazon Elastic File System（Amazon EFS）檔案系統掛載到每家診所的內部部署伺服器。

**答案**
C

## Question #244

**題目**
某公司使用的內容管理系統目前執行於單一 Amazon EC2 執行個體上，該 EC2 執行個體同時包含 Web 伺服器與資料庫軟體。公司必須讓網站平台具備高可用性，並且能隨使用者需求擴展。解決方案架構師應建議什麼作法來滿足這些需求？

**選項**
- A. 將資料庫移到 Amazon RDS 並啟用自動備份，再手動於同一個 Availability Zone 啟動另一台 EC2 執行個體。
- B. 將資料庫移轉到 Amazon Aurora，並在與現有 EC2 相同的 Availability Zone 建立 read replica。
- C. 將資料庫移轉到 Amazon Aurora，並在另一個 Availability Zone 建立 read replica。從現有 EC2 建立 Amazon Machine Image（AMI），再透過 Auto Scaling 與負載平衡擴展應用程式層。
- D. 將資料庫移到另一台 EC2 執行個體，並將備份排程到 Amazon S3。從現有 EC2 建立 AMI 以供擴展。

**答案**
C

## Question #245

**題目**
某公司要在 AWS 上推出一個應用程式。該應用程式使用 Application Load Balancer（ALB）將流量導向單一 target group 中至少兩台 Amazon EC2 執行個體。每個環境中的執行個體都位於各自的 Auto Scaling group。公司需要一個開發環境與一個正式環境，而正式環境將會有高流量時段。哪一個解決方案能以**最具成本效益**的方式配置開發環境？

**選項**
- A. 重新設定開發環境中的 target group，使其只包含 1 台 EC2 執行個體作為 target。
- B. 將 ALB 的負載平衡演算法改為 least outstanding requests。
- C. 同時縮小兩個環境中的 EC2 執行個體規格。
- D. 降低開發環境 Auto Scaling group 的最大 EC2 執行個體數量。

**答案**
A

## Question #246

**題目**
某公司在多個 Availability Zones 中的 Amazon EC2 執行個體上執行 Web 應用程式。這些 EC2 執行個體位於私有子網路。解決方案架構師實作了一個面向網際網路的 Application Load Balancer（ALB），並將這些 EC2 執行個體指定為 target group。然而，網際網路流量無法到達這些 EC2 執行個體。解決方案架構師應如何重新配置架構以解決此問題？

**選項**
- A. 以 Network Load Balancer 取代 ALB，並在公有子網路設定 NAT gateway 以允許網際網路流量。
- B. 將 EC2 執行個體移到公有子網路，並在 EC2 的 security group 中新增允許對 `0.0.0.0/0` 的 outbound 規則。
- C. 更新 EC2 子網路的 route tables，使 `0.0.0.0/0` 流量經由 internet gateway 路由，並在相關安全設定中允許流量。
- D. 在每個 Availability Zone 中建立公有子網路，將其與 ALB 關聯，並更新公有子網路的 route tables 與相關設定。

**答案**
C

## Question #247

**題目**
某公司已在 Amazon RDS for MySQL 上部署資料庫。由於交易量增加，資料庫支援團隊回報 DB instance 的讀取速度變慢，並建議新增 read replica。在實作此變更前，解決方案架構師應採取哪兩項措施？（選兩項）

**選項**
- A. 在 RDS primary node 上啟用 binlog replication。
- B. 為來源 DB instance 選擇 failover priority。
- C. 允許來源 DB instance 上的長時間交易完成。
- D. 建立 global table，並指定資料表可用的 AWS Regions。
- E. 在來源執行個體上啟用自動備份，將 backup retention period 設為非 0 值。

**答案**
AC

## Question #248

**題目**
某公司在 Amazon EC2 執行個體上執行分析軟體。該軟體會接受使用者提交的工作請求，以處理已上傳到 Amazon S3 的資料。使用者回報部分提交的資料沒有被處理。Amazon CloudWatch 顯示 EC2 執行個體的 CPU 使用率長期維持在接近或等於 100%。公司希望提升系統效能，並根據使用者負載進行擴展。解決方案架構師應如何滿足這些需求？

**選項**
- A. 建立現有執行個體的副本，並將所有執行個體放到 Application Load Balancer 後方。
- B. 為 Amazon S3 建立 S3 VPC endpoint，並更新軟體使其改用該 endpoint。
- C. 停止 EC2 執行個體，修改 instance type 為具有更強 CPU 與更多記憶體的類型，再重新啟動。
- D. 將傳入請求導向 Amazon Simple Queue Service（Amazon SQS），並根據 queue 大小設定 EC2 Auto Scaling group。

**答案**
D

## Question #249

**題目**
某公司正在為託管於 AWS Cloud 的媒體應用程式實作共享儲存解決方案。公司需要能讓 SMB 用戶端存取資料，且此解決方案必須是完全受管的 AWS 服務。哪一個 AWS 解決方案能滿足這些需求？

**選項**
- A. 建立 AWS Storage Gateway volume gateway，建立使用所需用戶端通訊協定的檔案分享，並讓應用程式連線。
- B. 建立 AWS Storage Gateway tape gateway，將磁帶設定為使用 Amazon S3，並將應用程式伺服器連接到 tape gateway。
- C. 建立一台 Amazon EC2 Windows 執行個體，安裝並設定 Windows file share 角色，再讓應用程式伺服器連線。
- D. 建立 Amazon FSx for Windows File Server 檔案系統，將檔案系統掛載到來源伺服器，並讓應用程式伺服器以 SMB 方式連線。

**答案**
D

## Question #250

**題目**
某公司的資安團隊要求必須在 VPC Flow Logs 中擷取網路流量。這些日誌在前 90 天會被頻繁存取，之後則只會偶爾存取。解決方案架構師在設定這些日誌時，應如何滿足這些需求？

**選項**
- A. 將 Amazon CloudWatch 設為目標，並將 CloudWatch log group 的保存期限設定為 90 天。
- B. 將 Amazon Kinesis 設為目標，並設定 Kinesis stream 永遠保留這些日誌 90 天。
- C. 將 AWS CloudTrail 設為目標，設定 CloudTrail 將資料儲存到 Amazon S3 bucket，並啟用 S3 Intelligent-Tiering。
- D. 將 Amazon S3 設為目標，並啟用 S3 Lifecycle policy，將日誌轉移到 S3 Standard-Infrequent Access（S3 Standard-IA）。

**答案**
A
