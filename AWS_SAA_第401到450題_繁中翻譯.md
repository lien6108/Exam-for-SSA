# AWS SAA 第401到450題繁中翻譯

> 說明：
> - 僅保留：題號、題目、選項、答案
> - AWS 服務名稱保留英文
> - 少數選項在原始檔中本身就有截斷，以下依原始可見內容翻譯並以「…」表示

## Question #401

**題目**  
某公司想使用 AWS Cloud，讓既有應用程式具備高可用性與韌性。此應用程式目前部署在公司的資料中心。最近因為資料庫伺服器發生非預期停電而當機，導致資料遺失。公司需要一個能避免任何單點故障的解決方案，並且必須讓應用程式可以隨使用者需求擴展。哪一個解決方案最符合需求？

**選項**  
- A. 將應用程式伺服器部署為跨多個 Availability Zones 的 Amazon EC2 Auto Scaling group。使用一個…  
- B. 將應用程式伺服器部署為單一 Availability Zone 內的 Amazon EC2 Auto Scaling group。部署…  
- C. 將應用程式伺服器部署為跨多個 Availability Zones 的 Amazon EC2 Auto Scaling group。使用一個…  
- D. 將應用程式伺服器部署為跨多個 Availability Zones 的 Amazon EC2 Auto Scaling group。部署…  

**答案**  
A

## Question #402

**題目**  
某公司需要擷取並處理其應用程式產生的大量串流資料。應用程式執行在 Amazon EC2 執行個體上，並將資料送到採用預設設定的 Amazon Kinesis Data Streams。每隔一天，應用程式會取用這些資料，並將其寫入 Amazon S3 bucket，以供商業智慧（BI）處理。公司發現 Amazon S3 並未收到應用程式送到 Kinesis Data Streams 的所有資料。Solutions architect 應該怎麼做來解決這個問題？

**選項**  
- A. 更新 Kinesis Data Streams 的預設設定，調整資料保留期間。  
- B. 更新應用程式，改用 Kinesis Producer Library (KPL) 將資料送到 Kinesis Data Streams。  
- C. 增加 Kinesis shards 的數量，以處理送往 Kinesis Data Streams 的資料吞吐量。  
- D. 在 S3 bucket 啟用 S3 Versioning，以保留匯入至 S3 bucket 的每個物件版本。  

**答案**  
A

## Question #403

**題目**  
某開發人員有一個使用 AWS Lambda function 上傳檔案到 Amazon S3 的應用程式，且需要具備執行此任務所需的權限。該開發人員已經有一個具備 Amazon S3 所需有效 IAM 憑證的 IAM user。Solutions architect 應該如何授與權限？

**選項**  
- A. 在 Lambda function 的 resource policy 中加入所需的 IAM 權限。  
- B. 在 Lambda function 中使用現有 IAM 憑證建立簽署請求。  
- C. 建立新的 IAM user，並在 Lambda function 中使用現有 IAM 憑證。  
- D. 建立具有所需權限的 IAM execution role，並將 IAM role 附加到 Lambda function。  

**答案**  
A

## Question #404

**題目**  
某公司已部署一個無伺服器應用程式，當新文件上傳到 Amazon S3 bucket 時，會觸發 AWS Lambda function。該應用程式利用 Lambda function 處理文件。最近一次行銷活動後，公司注意到有許多文件未被處理。Solutions architect 應該如何改善此應用程式架構？

**選項**  
- A. 將 Lambda function 的執行逾時設定為 15 分鐘。  
- B. 設定 S3 bucket replication policy，將文件暫存在 S3 bucket 以供後續處理。  
- C. 再部署一個 Lambda function，將文件處理工作在兩個 Lambda functions 間做負載平衡。  
- D. 建立 Amazon SQS queue，將請求送到 queue，並將該 queue 設定為事件來源。  

**答案**  
D

## Question #405

**題目**  
某 solutions architect 正在設計一個軟體展示環境的架構。此環境將執行在位於 Application Load Balancer (ALB) 後方、且屬於 Auto Scaling group 的 Amazon EC2 執行個體上。該系統在上班時間流量會大幅增加，但週末不需要運作。Solutions architect 應採取哪兩個動作，才能確保系統可以依需求擴展？（選二）

**選項**  
- A. 使用 AWS Auto Scaling 根據請求速率調整 ALB 容量。  
- B. 使用 AWS Auto Scaling 擴展 VPC internet gateway 的容量。  
- C. 在多個 AWS Regions 啟動 EC2 執行個體，將負載分散到不同 Regions。  
- D. 使用 target tracking scaling policy，根據執行個體 CPU 使用率擴展 Auto Scaling group。  
- E. 使用 scheduled scaling，在週末把 Auto Scaling group 的最小、最大與期望容量調整為 0，並在工作日恢復。  

**答案**  
D

## Question #406

**題目**  
某 solutions architect 正在設計一個雙層架構，包含 public subnet 與 database subnet。Public subnet 中的 web servers 必須對網際網路開放 443 連接埠。Database subnet 中的 Amazon RDS for MySQL DB instance 則只能讓 web servers 透過 3306 連接埠存取。Solutions architect 應採取哪兩個步驟來滿足需求？（選二）

**選項**  
- A. 為 public subnet 建立 network ACL，加入一條規則以拒絕對 0.0.0.0/0 的 3306 outbound 流量。  
- B. 為 DB instance 建立 security group，加入一條規則允許來自 public subnet CIDR block 的 3306 流量。  
- C. 為 public subnet 中的 web servers 建立 security group，加入一條規則允許來自 0.0.0.0/0 的 443 流量。  
- D. 為 DB instance 建立 security group，加入一條規則允許來自 web servers security group 的 3306 流量。  
- E. 為 DB instance 建立 security group，加入一條規則，除來自 web servers security group 的 3306 流量外，拒絕所有流量。  

**答案**  
CD

## Question #407

**題目**  
某公司正在為部署在 AWS Cloud 的遊戲應用程式實作共享儲存方案。公司需要能夠使用 Lustre 用戶端存取資料，且此方案必須是全受管服務。哪一個解決方案最符合需求？

**選項**  
- A. 建立 AWS DataSync task，將資料分享為可掛載的檔案系統，再把檔案系統掛載到應用程式伺服器。  
- B. 建立 AWS Storage Gateway file gateway，建立使用所需用戶端協定的 file share，並連接應用程式伺服器。  
- C. 建立 Amazon EFS file system，並設定支援 Lustre，再將其附加到來源伺服器。  
- D. 建立 Amazon FSx for Lustre file system，將其附加到來源伺服器，並讓應用程式伺服器連線至該檔案系統。  

**答案**  
C

## Question #408

**題目**  
某公司執行一個應用程式，從分散於各地、使用 UDP 的數千個遠端裝置接收資料。應用程式會立即處理資料，必要時再回傳訊息給裝置，不會儲存任何資料。公司需要一個能將裝置資料傳輸延遲降到最低的方案，且必須能快速切換到另一個 AWS Region。哪一個解決方案最符合需求？

**選項**  
- A. 設定 Amazon Route 53 failover routing policy，在兩個 Regions 各建立一個 NLB，並設定…  
- B. 使用 AWS Global Accelerator，在兩個 Regions 各建立一個 NLB 作為 endpoint，並建立一個…  
- C. 使用 AWS Global Accelerator，在兩個 Regions 各建立一個 ALB 作為 endpoint，並建立一個…  
- D. 設定 Amazon Route 53 failover routing policy，在兩個 Regions 各建立一個 ALB，並建立一個…  

**答案**  
B

## Question #409

**題目**  
某 solutions architect 必須將一個 Windows Internet Information Services (IIS) Web 應用程式遷移到 AWS。此應用程式目前依賴使用者內部部署 NAS 上的 file share。Solutions architect 提議將 IIS Web 伺服器遷移到跨多個 Availability Zones 的 Amazon EC2 執行個體，並連接至儲存方案，同時為執行個體設定 Elastic Load Balancer。哪一種取代內部部署 file share 的方案最具韌性與耐久性？

**選項**  
- A. 將 file share 遷移到 Amazon RDS。  
- B. 將 file share 遷移到 AWS Storage Gateway。  
- C. 將 file share 遷移到 Amazon FSx for Windows File Server。  
- D. 將 file share 遷移到 Amazon EFS。  

**答案**  
A

## Question #410

**題目**  
某公司正在 Amazon EC2 執行個體上部署新應用程式。此應用程式會將資料寫入 Amazon EBS volumes。公司需要確保所有寫入 EBS volumes 的資料在靜態狀態下都受到加密。哪一個解決方案可滿足需求？

**選項**  
- A. 建立一個指定 EBS 加密的 IAM role，並將該 role 附加到 EC2 執行個體。  
- B. 以加密 volume 的方式建立 EBS volumes，再將其附加到 EC2 執行個體。  
- C. 建立一個 EC2 instance tag，鍵值為 Encrypt=True，並將所有需要 EBS 層級加密的執行個體加上標籤。  
- D. 建立 AWS KMS key policy，在帳號中強制 EBS 加密，並確保該 key policy…  

**答案**  
B

## Question #411

**題目**  
某公司有一個使用模式零星的 Web 應用程式。每月初使用量很高、每週初使用量中等，而平日中的使用量則不可預測。此應用程式由資料中心內的 web server 與 MySQL database server 組成。公司想將應用程式遷移到 AWS Cloud，並且需要一個不需修改資料庫、又具成本效益的資料庫平台。哪一個解決方案最符合需求？

**選項**  
- A. Amazon DynamoDB  
- B. Amazon RDS for MySQL  
- C. 相容 MySQL 的 Amazon Aurora Serverless  
- D. 部署在 Amazon EC2 Auto Scaling group 上的 MySQL  

**答案**  
C

## Question #412

**題目**  
某圖片託管公司將物件存放在 Amazon S3 buckets 中。公司想避免 S3 bucket 中的物件被意外公開給大眾。整個 AWS 帳號中的所有 S3 物件都必須維持私有。哪一個解決方案可滿足需求？

**選項**  
- A. 使用 Amazon GuardDuty 監控 S3 bucket policies，建立自動修復規則，透過 AWS Lambda function…  
- B. 使用 AWS Trusted Advisor 找出公開可存取的 S3 buckets，並在有變更時設定 email 通知。  
- C. 使用 AWS Resource Access Manager 找出公開可存取的 S3 buckets，並用 Amazon SNS…  
- D. 在帳號層級使用 S3 Block Public Access，並使用 AWS Organizations 建立 service control policy (SCP)…  

**答案**  
D

## Question #413

**題目**  
某電商公司正面臨使用者流量增加。公司的商店以 Amazon EC2 執行個體部署為雙層 Web 應用程式，包含 web tier 與獨立的 database tier。隨著流量增加，公司發現此架構導致行銷郵件與訂單確認郵件無法即時寄出。公司希望減少處理複雜電子郵件投遞問題的時間，並降低營運負擔。Solutions architect 應如何滿足需求？

**選項**  
- A. 建立一個使用 EC2 執行個體的獨立應用層，專門處理電子郵件。  
- B. 設定 web instance 透過 Amazon SES 發送電子郵件。  
- C. 設定 web instance 透過 Amazon SNS 發送電子郵件。  
- D. 建立一個使用 EC2 執行個體的獨立應用層，專門處理電子郵件，並放入 Auto Scaling group。  

**答案**  
B

## Question #414

**題目**  
某公司有一套商業系統，每天會產生數百份報表，並將報表以 CSV 格式儲存到 network share。公司需要將這些資料以近即時方式存到 AWS Cloud 供分析使用，且管理負擔要最低。哪一個解決方案最符合需求？

**選項**  
- A. 使用 AWS DataSync 將檔案傳輸到 Amazon S3，並建立每日結束時執行的排程任務。  
- B. 建立 Amazon S3 File Gateway，並更新商業系統使其改用 File Gateway 提供的新 network share。  
- C. 使用 AWS DataSync 將檔案傳輸到 Amazon S3，並建立應用程式在自動化流程中使用 DataSync API。  
- D. 部署 AWS Transfer for SFTP endpoint，並建立指令碼檢查 network share 中的新檔案，再上傳…  

**答案**  
C

## Question #415

**題目**  
某公司將 PB 等級資料存放在多個 Amazon S3 Standard buckets 中。這些資料的存取頻率各不相同，公司也不清楚所有資料的存取模式。公司需要針對每一個 S3 bucket 實作一種可最佳化 S3 使用成本的方案，且營運效率要最高。哪一個解決方案最符合需求？

**選項**  
- A. 建立 S3 Lifecycle 設定，將 S3 bucket 中的物件轉移到 S3 Intelligent-Tiering。  
- B. 使用 S3 storage class analysis 決定每個物件的正確分層，並將每個物件移到判定出的層級。  
- C. 建立 S3 Lifecycle 設定，將物件轉移到 S3 Glacier Instant Retrieval。  
- D. 建立 S3 Lifecycle 設定，將物件轉移到 S3 One Zone-IA。  

**答案**  
A

## Question #416

**題目**  
某快速成長的全球電商公司將 Web 應用程式託管在 AWS 上。該 Web 應用程式包含靜態內容與動態內容，網站的 OLTP 資料則存放在 Amazon RDS 資料庫中。網站使用者正遭遇頁面載入緩慢。Solutions architect 應採取哪兩個動作來解決此問題？（選二）

**選項**  
- A. 設定 Amazon Redshift cluster。  
- B. 建立 Amazon CloudFront distribution。  
- C. 將動態 Web 內容託管在 Amazon S3。  
- D. 為 RDS DB instance 建立 read replica。  
- E. 將 RDS DB instance 設定為 Multi-AZ deployment。  

**答案**  
BD

## Question #417

**題目**  
某公司使用 Amazon EC2 執行個體與 AWS Lambda functions 執行其應用程式。公司帳號中的 VPC 具有 public subnets 與 private subnets。EC2 執行個體執行在其中一個 VPC 的 private subnet。為了讓應用程式正常運作，Lambda functions 需要直接以網路方式存取這些 EC2 執行個體。應用程式至少會執行 1 年，且未來 Lambda functions 的數量預計增加。公司希望在所有應用資源上盡可能提高節省幅度，並保持服務間的低網路延遲。哪一個解決方案最符合需求？

**選項**  
- A. 購買 EC2 Instance Savings Plan，並最佳化 Lambda functions 的執行時間、記憶體用量與呼叫次數。  
- B. 購買 EC2 Instance Savings Plan，並最佳化 Lambda functions 的執行時間、記憶體用量、呼叫次數，以及…  
- C. 購買 Compute Savings Plan，並最佳化 Lambda functions 的執行時間、記憶體用量、呼叫次數，以及…  
- D. 購買 Compute Savings Plan，並最佳化 Lambda functions 的執行時間、記憶體用量、呼叫次數，以及…  

**答案**  
C

## Question #418

**題目**  
某 solutions architect 需要讓團隊成員存取兩個不同 AWS 帳號中的 Amazon S3 buckets：一個 development 帳號與一個 production 帳號。團隊目前透過各自獨立的 IAM users 存取 development 帳號中的 S3 buckets，這些使用者屬於一個具有適當權限的 IAM group。Solutions architect 已在 production 帳號中建立一個 IAM role，且該 role 的 policy 已授與 production 帳號中 S3 bucket 的存取權。哪一個解決方案能在符合最小權限原則下滿足需求？

**選項**  
- A. 將 AdministratorAccess policy 附加到 development 帳號的使用者。  
- B. 在 production 帳號中該 role 的 trust policy 內，將 development 帳號加入為 principal。  
- C. 關閉 production 帳號中 S3 bucket 的 S3 Block Public Access。  
- D. 在 production 帳號中為每位團隊成員建立一個獨立憑證的 user。  

**答案**  
B

## Question #419

**題目**  
某公司使用啟用所有功能的 AWS Organizations，並在 ap-southeast-2 Region 執行多個 Amazon EC2 工作負載。公司已有一個 SCP，可防止在其他 Region 建立任何資源。安全政策要求所有靜態資料都必須加密。稽核發現員工建立了一些未加密的 Amazon EBS volumes 供 EC2 使用。公司希望任何 IAM user 或 root user 在 ap-southeast-2 啟動新的 EC2 執行個體時，都自動使用已加密的 EBS volumes，且對建立 EBS volumes 的員工影響要盡可能小。哪兩個步驟可滿足需求？（選二）

**選項**  
- A. 在 Amazon EC2 主控台選擇 EBS encryption account attribute，並定義預設加密金鑰。  
- B. 建立 IAM permission boundary，附加到 root OU，並定義為拒絕…  
- C. 建立 SCP，附加到 root OU，並定義在 ec2:Encrypted 條件為 false 時拒絕 ec2:CreateVolume。  
- D. 更新每個帳號的 IAM policies，在 ec2:Encrypted 條件為 false 時拒絕 ec2:CreateVolume。  
- E. 在 Organizations management account 中指定 Default EBS volume encryption 設定。  

**答案**  
AD

## Question #420

**題目**  
某公司想使用 Amazon RDS for PostgreSQL DB cluster，以簡化正式環境資料庫工作負載中耗時的資料庫管理工作。公司希望確保資料庫具備高可用性，且在大多數情境下可於 40 秒內自動容錯移轉。公司也希望將讀取負載從 primary instance 卸載，並盡可能降低成本。哪一個解決方案最符合需求？

**選項**  
- A. 使用 Amazon RDS Multi-AZ DB instance deployment，建立一個 read replica，並將讀取工作負載導向該 read replica。  
- B. 使用 Amazon RDS Multi-AZ DB cluster deployment，建立兩個 read replicas，並將讀取工作負載導向這些 read replicas。  
- C. 使用 Amazon RDS Multi-AZ DB instance deployment，並將讀取工作負載導向 Multi-AZ 配對中的 secondary instance。  
- D. 使用 Amazon RDS Multi-AZ DB cluster deployment，並將讀取工作負載導向 reader endpoint。  

**答案**  
A

## Question #421

**題目**  
某公司執行一項高可用的 SFTP 服務。此 SFTP 服務使用兩台具備 Elastic IP 的 Amazon EC2 Linux 執行個體，接收來自網際網路可信任 IP 來源的流量。該 SFTP 服務背後有附加到執行個體的共享儲存。使用者帳號是在 SFTP 伺服器上以 Linux users 的方式建立與管理。公司希望改用無伺服器選項，該選項需提供高 IOPS 效能與高度可設定的安全性，同時公司也希望保有對使用者權限的控制。哪一個解決方案最符合需求？

**選項**  
- A. 建立加密的 Amazon EBS volume，建立 AWS Transfer Family SFTP 服務並使用 public…  
- B. 建立加密的 Amazon EFS volume，建立使用 Elastic IP 的 AWS Transfer Family SFTP 服務…  
- C. 建立啟用預設加密的 Amazon S3 bucket，建立使用 public endpoint 的 AWS Transfer Family SFTP 服務，且…  
- D. 建立啟用預設加密的 Amazon S3 bucket，建立使用 VPC endpoint 的 AWS Transfer Family SFTP 服務，且…  

**答案**  
C

## Question #422

**題目**  
某公司正在 AWS 上開發新的機器學習（ML）模型解決方案。這些模型被開發成獨立的微服務，在啟動時會從 Amazon S3 擷取約 1 GB 的模型資料並載入記憶體。使用者透過非同步 API 存取模型，可送出單一請求或批次請求，並指定結果要傳送到哪裡。公司為數百位使用者提供模型服務，而各模型的使用模式不規則：有些模型可能數天或數週都不會被使用，另一些模型則可能一次收到數千筆請求。Solutions architect 應推薦哪一種設計來滿足需求？

**選項**  
- A. 將 API 請求導向 Network Load Balancer (NLB)，將模型部署為由…觸發的 AWS Lambda functions。  
- B. 將 API 請求導向 Application Load Balancer (ALB)，將模型部署為 Amazon ECS…  
- C. 將 API 請求導入 Amazon SQS queue，將模型部署為 AWS Lambda…  
- D. 將 API 請求導入 Amazon SQS queue，將模型部署為 Amazon ECS…  

**答案**  
D

## Question #423

**題目**  
某 solutions architect 想使用下列 JSON 文字作為 identity-based policy 來授與特定權限。該 policy 可以附加到哪些 IAM principals？（選二）

**選項**  
- A. Role  
- B. Group  
- C. Organization  
- D. Amazon ECS resource  
- E. Amazon EC2 resource  

**答案**  
AB

## Question #424

**題目**  
某公司在 Amazon EC2 On-Demand Instances 上執行自訂應用程式。此應用程式包含必須全天候 24x7 運作的 frontend nodes，以及只需依工作負載短時間執行的 backend nodes。Backend nodes 的數量在一天中會變動。公司需要依負載進行擴充與縮減，並希望最具成本效益。哪一個解決方案最符合需求？

**選項**  
- A. Frontend nodes 使用 Reserved Instances；backend nodes 使用 AWS Fargate。  
- B. Frontend nodes 使用 Reserved Instances；backend nodes 使用 Spot Instances。  
- C. Frontend nodes 使用 Spot Instances；backend nodes 使用 Reserved Instances。  
- D. Frontend nodes 使用 Spot Instances；backend nodes 使用 AWS Fargate。  

**答案**  
B

## Question #425

**題目**  
某公司在內部部署環境中執行需要高區塊儲存容量的工作負載。每日尖峰 IOPS 不超過 15,000。公司希望將工作負載遷移到 Amazon EC2，並且希望磁碟效能可獨立於儲存容量來配置。哪一種 Amazon EBS volume type 最具成本效益且符合需求？

**選項**  
- A. gp2  
- B. io2  
- C. gp3  
- D. io1  

**答案**  
C

## Question #426

**題目**  
某公司需要儲存其醫療照護應用程式的資料，而應用程式資料經常變動。新法規要求對儲存資料的各個層級都能進行稽核存取。公司目前在即將用盡儲存容量的內部部署基礎設施上託管此應用程式。Solutions architect 必須將現有資料安全地遷移到 AWS，並同時滿足新法規。哪一個解決方案最符合需求？

**選項**  
- A. 使用 AWS DataSync 將現有資料移到 Amazon S3，並使用 AWS CloudTrail 記錄 data events。  
- B. 使用 AWS Snowcone 將現有資料移到 Amazon S3，並使用 AWS CloudTrail 記錄 management events。  
- C. 使用 Amazon S3 Transfer Acceleration 將現有資料移到 Amazon S3，並使用 AWS CloudTrail 記錄 data events。  
- D. 使用 AWS Storage Gateway 將現有資料移到 Amazon S3，並使用 AWS CloudTrail 記錄 management events。  

**答案**  
B

## Question #427

**題目**  
某 solutions architect 正在實作一個複雜的 Java 應用程式搭配 MySQL 資料庫。Java 應用程式必須部署於 Apache Tomcat，且需具備高可用性。Solutions architect 應如何滿足需求？

**選項**  
- A. 將應用程式部署在 AWS Lambda，並設定 Amazon API Gateway API 與 Lambda functions 連接。  
- B. 使用 AWS Elastic Beanstalk 部署應用程式，設定 load-balanced environment 與 rolling deployment policy。  
- C. 將資料庫遷移到 Amazon ElastiCache，並設定 ElastiCache security group 允許應用程式存取。  
- D. 啟動一台 Amazon EC2 執行個體，在其上安裝 MySQL server，設定應用程式，並建立 AMI。  

**答案**  
B

## Question #428

**題目**  
某無伺服器應用程式使用 Amazon API Gateway、AWS Lambda 與 Amazon DynamoDB。Lambda function 需要有權限對 DynamoDB table 進行讀寫。哪一個解決方案能以最安全的方式讓 Lambda function 存取 DynamoDB table？

**選項**  
- A. 建立一個具有程式化存取能力的 IAM user 給 Lambda function，並附加允許對…讀寫的 policy。  
- B. 建立一個將 Lambda 納為 trusted service 的 IAM role，並附加允許對該 DynamoDB table 讀寫的 policy。  
- C. 建立一個具有程式化存取能力的 IAM user 給 Lambda function，並附加允許對…讀寫的 policy。  
- D. 建立一個將 DynamoDB 納為 trusted service 的 IAM role，並附加允許從…讀寫的 policy。  

**答案**  
B

## Question #429

**題目**  
下列 IAM policy 已附加到某 IAM group，且這是套用在該 group 上的唯一 policy。對於 group 成員而言，這個 policy 的實際 IAM 權限是什麼？

**選項**  
- A. Group 成員在 us-east-1 Region 內允許執行任何 Amazon EC2 動作。Allow 之後的陳述式不會被…  
- B. 除非使用多重要素驗證（MFA）登入，否則 group 成員在 us-east-1 Region 會被拒絕任何 Amazon EC2 權限。  
- C. Group 成員若使用 MFA 登入，則在所有 Regions 都允許 ec2:StopInstances 與 ec2:TerminateInstances。  
- D. Group 成員只有在使用 MFA 登入時，才可在 us-east-1 Region 使用 ec2:StopInstances 與 ec2:TerminateInstances。  

**答案**  
D

## Question #430

**題目**  
某製造公司有機器感測器會將 .csv 檔上傳到 Amazon S3 bucket。這些 .csv 檔必須儘快轉換成影像，供自動產生圖形化報表使用。這些影像在 1 個月後即失去價值，但 .csv 檔必須保留，以便每年兩次用於訓練機器學習（ML）模型。ML 訓練與稽核都會提前數週規劃。哪兩個步驟能以最具成本效益的方式滿足需求？（選二）

**選項**  
- A. 啟動一台 Amazon EC2 Spot Instance，每小時下載 .csv 檔、產生影像檔，再上傳影像到…  
- B. 設計一個 AWS Lambda function，把 .csv 檔轉換成影像並存回 S3 bucket。以上傳事件觸發 Lambda。  
- C. 為 S3 bucket 中的 .csv 與影像檔建立 S3 Lifecycle rules，將 .csv 從 S3 Standard 在 1 天後轉移到 S3 Glacier…  
- D. 為 .csv 與影像檔建立 S3 Lifecycle rules，將 .csv 從 S3 Standard 轉移到 S3 One Zone-IA…  
- E. 為 .csv 與影像檔建立 S3 Lifecycle rules，將 .csv 從 S3 Standard 轉移到 S3 Standard-IA…  

**答案**  
BC

## Question #431

**題目**  
某公司開發了一款以 Web 應用程式形式提供的新線上遊戲。該應用程式採三層架構，部署於 VPC 中，資料庫層使用 Amazon RDS for MySQL。多位玩家將同時在線競賽。開發團隊希望能近即時顯示前 10 名排行榜，並且在停止與恢復遊戲時仍能保留目前分數。Solutions architect 應該怎麼做？

**選項**  
- A. 建立 Amazon ElastiCache for Memcached cluster，以快取分數供 Web 應用程式顯示。  
- B. 建立 Amazon ElastiCache for Redis cluster，用來計算並快取分數供 Web 應用程式顯示。  
- C. 在 Web 應用程式前方放置 Amazon CloudFront distribution，以快取排行榜內容。  
- D. 在 Amazon RDS for MySQL 上建立 read replica，以執行排行榜計算查詢並提供讀取流量。  

**答案**  
B

## Question #432

**題目**  
某電商公司希望使用機器學習（ML）演算法來建立與訓練模型，並利用這些模型視覺化複雜情境，以及偵測客戶資料中的趨勢。架構團隊希望把 ML 模型整合到報表平台中，以分析增強後的資料，並直接在商業智慧儀表板中使用。哪一個解決方案可在最低營運負擔下滿足需求？

**選項**  
- A. 使用 AWS Glue 建立 ML transform 來建立與訓練模型，並使用 Amazon OpenSearch Service 視覺化資料。  
- B. 使用 Amazon SageMaker 建立與訓練模型，並使用 Amazon QuickSight 視覺化資料。  
- C. 使用 AWS Marketplace 上的預建 ML AMI 來建立與訓練模型，並使用 Amazon OpenSearch Service…  
- D. 使用 Amazon QuickSight 的 calculated fields 建立與訓練模型，並使用 Amazon QuickSight 視覺化。  

**答案**  
B

## Question #433

**題目**  
某公司在多個 AWS 帳號中執行正式與非正式環境工作負載，且這些帳號位於 AWS Organizations 中。公司需要設計一個可防止成本使用標籤（cost usage tags）被修改的方案。哪一個解決方案最符合需求？

**選項**  
- A. 建立自訂 AWS Config rule，除授權主體外防止標籤修改。  
- B. 在 AWS CloudTrail 建立自訂 trail 以防止標籤修改。  
- C. 建立 service control policy (SCP)，除授權主體外防止標籤修改。  
- D. 建立自訂 Amazon CloudWatch logs 以防止標籤修改。  

**答案**  
C

## Question #434

**題目**  
某公司在 AWS Cloud 上託管應用程式。該應用程式執行於 Auto Scaling group 中、位於 Elastic Load Balancer 後方的 Amazon EC2 執行個體上，並使用一個 Amazon DynamoDB table。公司希望在另一個 AWS Region 也能以最少停機時間提供此應用程式。Solutions architect 應如何以最少停機時間滿足需求？

**選項**  
- A. 在災難復原 Region 建立 Auto Scaling group 與 load balancer，並將 DynamoDB table 設定為 global table。  
- B. 建立 AWS CloudFormation template，在需要時建立 EC2、load balancer 與 DynamoDB tables。  
- C. 建立 AWS CloudFormation template，在需要時建立 EC2 與 load balancer，並設定 DynamoDB…  
- D. 在災難復原 Region 建立 Auto Scaling group 與 load balancer，並將 DynamoDB table 設定為 global table。  

**答案**  
A

## Question #435

**題目**  
某公司需要在 2 週內，將一個 20 TB 的 MySQL 資料庫從內部部署資料中心遷移到 AWS，且希望停機時間最短。哪一個解決方案能以最具成本效益的方式完成遷移？

**選項**  
- A. 訂購 AWS Snowball Edge Storage Optimized device，並使用 AWS DMS 搭配 AWS SCT…  
- B. 訂購 AWS Snowmobile，並使用 AWS DMS 搭配 AWS SCT…  
- C. 訂購 AWS Snowball Edge Compute Optimized with GPU device，並使用 AWS DMS 搭配 AWS SCT…  
- D. 訂購 1 GB 的專用 AWS Direct Connect 連線到資料中心，並使用 AWS DMS…  

**答案**  
D

## Question #436

**題目**  
某公司將其內部部署的 PostgreSQL 資料庫遷移到 Amazon RDS for PostgreSQL DB instance。新產品成功上市後，資料庫工作負載增加。公司希望在不新增基礎設施的前提下承載更大的工作負載，且要最具成本效益。哪一個解決方案最符合需求？

**選項**  
- A. 針對整體工作負載購買 Reserved DB instances，並將 Amazon RDS for PostgreSQL DB instance 升級為更大規格。  
- B. 將 Amazon RDS for PostgreSQL DB instance 改為 Multi-AZ DB instance。  
- C. 針對整體工作負載購買 Reserved DB instances，並新增另一個 Amazon RDS for PostgreSQL DB instance。  
- D. 將 Amazon RDS for PostgreSQL DB instance 改為 On-Demand DB instance。  

**答案**  
A

## Question #437

**題目**  
某公司在位於 Application Load Balancer (ALB) 後方、且屬於 Auto Scaling group 的 Amazon EC2 執行個體上營運電商網站。網站目前因來自不合法外部系統、且 IP 位址不斷變動的大量請求而發生效能問題。安全團隊擔心網站可能遭受 DDoS 攻擊。公司必須在盡量不影響合法使用者的前提下，阻擋這些不合法請求。Solutions architect 應推薦什麼？

**選項**  
- A. 部署 Amazon Inspector 並與 ALB 關聯。  
- B. 部署 AWS WAF、與 ALB 關聯，並設定 rate-limiting rule。  
- C. 在與 ALB 關聯的 network ACLs 中部署規則，封鎖傳入流量。  
- D. 部署 Amazon GuardDuty，並在 GuardDuty 中啟用 rate-limiting 保護。  

**答案**  
B

## Question #438

**題目**  
某公司想與外部稽核人員分享會計資料。資料存放於 private subnet 中的 Amazon RDS DB instance。該稽核人員擁有自己的 AWS 帳號，並且需要一份自己的資料庫副本。公司以最安全的方式分享資料庫，應該怎麼做？

**選項**  
- A. 建立資料庫 read replica，並設定 IAM 標準資料庫驗證授與稽核人員存取。  
- B. 將資料庫內容匯出為文字檔並存入 Amazon S3 bucket，再建立新的 IAM user 供稽核人員存取。  
- C. 複製資料庫 snapshot 到 Amazon S3 bucket，建立 IAM user，並分享金鑰給稽核人員。  
- D. 建立加密的資料庫 snapshot，將 snapshot 分享給稽核人員，並允許其存取對應的 AWS KMS key。  

**答案**  
D

## Question #439

**題目**  
某 solutions architect 設定了一個 VPC，但其 IP 位址範圍較小。由於 VPC 中的 Amazon EC2 執行個體數量持續增加，未來工作負載將面臨 IP 位址不足。哪一個解決方案能以最低營運負擔解決此問題？

**選項**  
- A. 新增額外的 IPv4 CIDR block 以增加 IP 位址數量，並在 VPC 中建立額外 subnets，再建立新的…  
- B. 建立第二個 VPC 與額外 subnets，透過 peering connection 連接兩個 VPC，並更新路由。  
- C. 使用 AWS Transit Gateway，新增一個 transit gateway 並連接第二個 VPC，再更新 transit gateway 路由。  
- D. 建立第二個 VPC，並透過自建 VPN 方案在兩個 VPC 間建立 Site-to-Site VPN。  

**答案**  
A

## Question #440

**題目**  
某公司在應用程式測試期間使用 Amazon RDS for MySQL DB instance。在測試週期結束、終止 DB instance 前，solutions architect 建立了兩份備份：第一份是使用 mysqldump 建立的資料庫 dump；第二份是在終止 RDS 時啟用 final DB snapshot。公司現在準備開始新的測試週期，並希望從最近的備份建立新的 DB instance。公司已選擇相容 MySQL 的 Amazon Aurora 作為新的資料庫服務。哪兩個方案可以建立新的 DB instance？（選二）

**選項**  
- A. 直接將 RDS snapshot 匯入 Aurora。  
- B. 將 RDS snapshot 上傳到 Amazon S3，再將該 snapshot 匯入 Aurora。  
- C. 將 database dump 上傳到 Amazon S3，再將該 dump 匯入 Aurora。  
- D. 使用 AWS DMS 將 RDS snapshot 匯入 Aurora。  
- E. 將 database dump 上傳到 Amazon S3，再使用 AWS DMS 將 dump 匯入 Aurora。  

**答案**  
AD

## Question #441

**題目**  
某公司在跨多個 Availability Zones 的 Auto Scaling group 中、位於 Application Load Balancer 後方的 Amazon Linux Amazon EC2 執行個體上託管多層 Web 應用程式。公司觀察到，當終端使用者大量存取靜態 Web 內容時，Auto Scaling group 會啟動更多 On-Demand Instances。公司希望最佳化成本。Solutions architect 應如何以最具成本效益的方式重新設計應用程式？

**選項**  
- A. 將 Auto Scaling group 改為使用 Reserved Instances，而非 On-Demand Instances。  
- B. 將 Auto Scaling group 改為透過 Spot Instances 擴展，而非 On-Demand Instances。  
- C. 建立 Amazon CloudFront distribution，並從 Amazon S3 bucket 提供靜態 Web 內容。  
- D. 建立位於 Amazon API Gateway API 後方的 AWS Lambda function 來託管靜態網站內容。  

**答案**  
C

## Question #442

**題目**  
某公司在多個 AWS 帳號中儲存數個 PB 等級的資料，並使用 AWS Lake Formation 管理資料湖。公司的 data science 團隊希望安全地把各帳號中的部分資料分享給工程團隊，用於分析。哪一個解決方案能以最低營運負擔滿足需求？

**選項**  
- A. 將所需資料複製到共用帳號，在該帳號建立 IAM access role，並透過 permission policy 授權。  
- B. 在各儲存資料的帳號中，使用 Lake Formation permissions Grant 指令授與工程團隊帳號所需存取權。  
- C. 使用 AWS Data Exchange 私下發布所需資料到工程團隊帳號。  
- D. 使用 Lake Formation tag-based access control，授權並授與跨帳號所需資料給工程團隊。  

**答案**  
D

## Question #443

**題目**  
某公司想在 AWS 上託管可擴展的 Web 應用程式。使用者來自全球不同地理區域，且可下載與上傳大小高達數 GB 的獨特資料。開發團隊希望以具成本效益的方式，降低上傳與下載延遲並提升效能。Solutions architect 應該怎麼做？

**選項**  
- A. 使用 Amazon S3 搭配 Transfer Acceleration 來託管應用程式。  
- B. 使用 Amazon S3 搭配 Cache-Control headers 來託管應用程式。  
- C. 使用 Amazon EC2 with Auto Scaling 與 Amazon CloudFront 來託管應用程式。  
- D. 使用 Amazon EC2 with Auto Scaling 與 Amazon ElastiCache 來託管應用程式。  

**答案**  
A

## Question #444

**題目**  
某公司聘請 solutions architect 為其應用程式設計可靠架構。此應用程式由一個 Amazon RDS DB instance 與兩台手動佈建、執行 web server 的 Amazon EC2 執行個體組成。這些 EC2 執行個體位於單一 Availability Zone。最近一名員工誤刪了 DB instance，導致應用程式中斷 24 小時。公司對整體環境可靠性感到擔憂。Solutions architect 應如何最大化基礎設施的可靠性？

**選項**  
- A. 刪除一台 EC2 執行個體，對另一台啟用 termination protection；將 DB instance 改為 Multi-AZ，並…  
- B. 將 DB instance 改為 Multi-AZ 並啟用 deletion protection；將 EC2 執行個體置於 Application Load Balancer 後方，並…  
- C. 建立額外的 DB instance，再搭配 Amazon API Gateway 與 AWS Lambda function，讓應用程式…  
- D. 將 EC2 執行個體放入跨多個 Availability Zones 的 EC2 Auto Scaling group，並使用 Spot…  

**答案**  
B

## Question #445

**題目**  
某公司在企業資料中心的大型 NAS 系統中儲存 700 TB 資料。公司有混合環境，並擁有一條 10 Gbps 的 AWS Direct Connect 連線。因監管稽核要求，公司必須在 90 天內將資料移到雲端，且需要有效率且不中斷地完成搬遷。在傳輸期間，公司仍需能存取與更新資料。哪一個解決方案最符合需求？

**選項**  
- A. 在企業資料中心建立 AWS DataSync agent，建立資料傳輸 task，開始傳輸到 Amazon S3 bucket。  
- B. 將資料備份到 AWS Snowball Edge Storage Optimized devices，寄回 AWS 資料中心，並掛載目標 Amazon S3…  
- C. 使用 rsync 經由 Direct Connect 連線，直接將資料從本地儲存複製到指定的 Amazon S3 bucket。  
- D. 將資料備份到磁帶，寄送到 AWS 資料中心，並在內部部署檔案系統上掛載目標 Amazon S3 bucket。  

**答案**  
A

## Question #446

**題目**  
某公司將 PDF 格式資料存放於 Amazon S3 bucket。公司必須符合法律要求，將所有新舊資料在 Amazon S3 中保留 7 年。哪一個解決方案能以最低營運負擔滿足需求？

**選項**  
- A. 在 S3 bucket 啟用 S3 Versioning，設定 S3 Lifecycle 在 7 年後刪除資料，並設定 MFA…  
- B. 在 S3 bucket 啟用 S3 Object Lock 的 governance retention mode，設定保留期限為 7 年，並重新複製所有…  
- C. 在 S3 bucket 啟用 S3 Object Lock 的 compliance retention mode，設定保留期限為 7 年，並重新複製所有…  
- D. 在 S3 bucket 啟用 S3 Object Lock 的 compliance retention mode，設定保留期限為 7 年，並使用 S3 Batch…  

**答案**  
C

## Question #447

**題目**  
某公司有一個無狀態 Web 應用程式，執行於由 Amazon API Gateway 觸發的 AWS Lambda functions 上。公司希望將此應用程式部署到多個 AWS Regions，以提供區域性容錯移轉能力。Solutions architect 應如何將流量導向多個 Regions？

**選項**  
- A. 為每個 Region 建立 Amazon Route 53 health checks，並使用 active-active failover 設定。  
- B. 建立一個 Amazon CloudFront distribution，為每個 Region 設定 origin，並使用 CloudFront health checks 導流。  
- C. 建立 transit gateway，將其附加到每個 Region 的 API Gateway endpoint，並設定 transit gateway 路由。  
- D. 在主要 Region 建立 Application Load Balancer，並讓 target group 指向各 Region 的 API Gateway endpoint hostnames。  

**答案**  
A

## Question #448

**題目**  
某公司有兩個 VPC：Management 與 Production。Management VPC 透過 customer gateway 的 VPN 連線到資料中心中的單一裝置；Production VPC 則透過 virtual private gateway 使用兩條 AWS Direct Connect 連線。Management 與 Production VPC 之間使用單一 VPC peering connection 互通。Solutions architect 應如何降低此架構中的單點故障風險？

**選項**  
- A. 在 Management 與 Production VPC 之間再新增一組 VPNs。  
- B. 新增第二個 virtual private gateway，並附加到 Management VPC。  
- C. 從第二個 customer gateway device 到 Management VPC 再新增一組 VPNs。  
- D. 在 Management 與 Production VPC 間新增第二個 VPC peering connection。  

**答案**  
C

## Question #449

**題目**  
某公司在 Oracle 資料庫上執行應用程式。由於資料庫資源、備份管理與資料中心維護能力有限，公司計畫快速遷移到 AWS。此應用程式使用需要特權存取的第三方資料庫功能。哪一個解決方案能以最具成本效益的方式協助公司遷移資料庫到 AWS？

**選項**  
- A. 將資料庫遷移到 Amazon RDS for Oracle，並用雲端服務取代第三方功能。  
- B. 將資料庫遷移到 Amazon RDS Custom for Oracle，並自訂資料庫設定以支援第三方功能。  
- C. 將資料庫遷移到 Amazon EC2 Oracle AMI，並自訂資料庫設定以支援第三方功能。  
- D. 將資料庫遷移到 Amazon RDS for PostgreSQL，並重寫應用程式碼以移除對 Oracle APEX 的依賴。  

**答案**  
C

## Question #450

**題目**  
某公司有一個部署在單一伺服器上的三層 Web 應用程式。公司希望將應用程式遷移到 AWS Cloud，並希望其符合 AWS Well-Architected Framework，以及 AWS 對安全性、可擴展性與韌性的最佳實務。哪三個解決方案組合可滿足需求？（選三）

**選項**  
- A. 建立跨兩個 Availability Zones 的 VPC，沿用應用程式現有架構，並以既有架構方式託管應用程式…  
- B. 設定 security groups 與 network ACLs 控制資料庫層存取，並設定單一 Amazon…  
- C. 建立跨兩個 Availability Zones 的 VPC，將應用程式重構為 web tier、application tier 與 database tier，並將各層分別託管…  
- D. 使用單一 Amazon RDS database，只允許來自 application tier security group 的資料庫存取。  
- E. 在 web tier 前方使用 Elastic Load Balancers，並透過包含各層參照的 security groups 控制存取。  
- F. 在 private subnets 中使用 Amazon RDS Multi-AZ cluster deployment，且只允許 application tier security group 存取資料庫。  

**答案**  
ACF
