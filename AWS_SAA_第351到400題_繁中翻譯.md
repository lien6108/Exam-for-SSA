# AWS SAA 第 351～400 題｜繁中翻譯

說明：
- 僅保留：題號、題目、選項、答案。
- 已翻譯為繁體中文，AWS 服務名稱保留英文。
- 少數選項在原始檔中本身就有截斷，以下以「…」保留原意。

## Question #351

**題目**
某公司正在將資料管理應用程式遷移到 AWS。公司希望轉向事件驅動架構。新架構需要更分散式，並在執行工作流程各階段時採用無伺服器概念。公司同時也希望將營運負擔降到最低。哪一個解決方案符合這些需求？

**選項**
- A. 在 AWS Glue 中建立工作流程，並使用 AWS Glue 來呼叫 AWS Lambda 函數處理各個工作流程步驟。
- B. 在 AWS Step Functions 中建立工作流程，將應用程式部署在 Amazon EC2 執行個體上，並使用 Step Functions 呼叫…
- C. 在 Amazon EventBridge 中建立工作流程，使用 EventBridge 依排程呼叫 AWS Lambda 函數來處理工作流程…
- D. 在 AWS Step Functions 中建立工作流程，使用 Step Functions 建立 state machine，並由 state machine 呼叫 AWS…

**答案**
D

## Question #352

**題目**
某公司正在為一款線上多人遊戲設計網路架構。該遊戲使用 UDP 網路通訊協定，並將部署於 8 個 AWS Region。網路架構必須將延遲與封包遺失降到最低，以提供終端使用者高品質的遊戲體驗。哪一個解決方案符合這些需求？

**選項**
- A. 在每個 Region 建立 transit gateway，並在各 transit gateway 之間建立跨區域 peering attachment。
- B. 設定 AWS Global Accelerator，並在每個 Region 建立 UDP listener 與 endpoint group。
- C. 設定 Amazon CloudFront 並啟用 UDP，於每個 Region 設定 origin。
- D. 在各 Region 之間建立 VPC peering mesh，並在每個 VPC 啟用 UDP。

**答案**
B

## Question #353

**題目**
某公司在單一 Availability Zone 的 Amazon EC2 執行個體上託管一個三層式 Web 應用程式。此應用程式使用自管的 MySQL 資料庫，該資料庫同樣執行在 EC2 上，並將資料儲存在 Amazon EBS 磁碟區。MySQL 目前使用 1 TB 的 Provisioned IOPS SSD (io2) EBS 磁碟區。公司預估尖峰流量下讀寫各需要 1,000 IOPS。公司希望在保留雙倍 IOPS 擴充能力的前提下，將中斷降到最低、穩定效能並降低成本，同時將資料庫層移轉為高可用且具容錯能力的全受管方案。哪一個解決方案最具成本效益？

**選項**
- A. 使用 Amazon RDS for MySQL 的 Multi-AZ 部署，並搭配 io2 Block Express EBS 磁碟區。
- B. 使用 Amazon RDS for MySQL 的 Multi-AZ 部署，並搭配 General Purpose SSD (gp2) EBS 磁碟區。
- C. 使用 Amazon S3 Intelligent-Tiering 存取分層。
- D. 使用兩台大型 EC2 執行個體，以主備模式託管資料庫。

**答案**
B

## Question #354

**題目**
某公司在 AWS 上託管一個無伺服器應用程式。該應用程式使用 Amazon API Gateway、AWS Lambda 與 Amazon RDS for PostgreSQL 資料庫。公司注意到在流量尖峰或不可預期流量期間，因資料庫連線逾時而導致應用程式錯誤增加。公司需要以最少的程式碼變更來降低應用程式失敗率。解決方案架構師應如何做？

**選項**
- A. 降低 Lambda 的並行數。
- B. 在 RDS DB instance 上啟用 RDS Proxy。
- C. 調大 RDS DB instance 類型以接受更多連線。
- D. 將資料庫遷移到啟用隨需擴展的 Amazon DynamoDB。

**答案**
B

## Question #355

**題目**
某公司正將舊有應用程式遷移到 AWS。該應用程式每小時執行一次批次工作，且屬於 CPU 密集型。此批次工作在內部部署伺服器上平均需 15 分鐘，該伺服器有 64 個 vCPU 與 512 GiB 記憶體。哪一個解決方案能在 15 分鐘內完成批次工作，且營運負擔最低？

**選項**
- A. 使用 AWS Lambda 搭配函數擴展。
- B. 使用 Amazon ECS 搭配 AWS Fargate。
- C. 使用 Amazon Lightsail 搭配 AWS Auto Scaling。
- D. 在 Amazon EC2 上使用 AWS Batch。

**答案**
A

## Question #356

**題目**
某公司將資料物件儲存在 Amazon S3 Standard。解決方案架構師發現其中 75% 的資料在 30 天後很少被存取。公司要求所有資料仍須立即可存取，且維持相同的高可用性與韌性，同時希望將儲存成本降到最低。哪一個儲存方案符合需求？

**選項**
- A. 在 30 天後將資料移到 S3 Glacier Deep Archive。
- B. 在 30 天後將資料移到 S3 Standard-Infrequent Access (S3 Standard-IA)。
- C. 在 30 天後將資料移到 S3 One Zone-Infrequent Access (S3 One Zone-IA)。
- D. 立即將資料移到 S3 One Zone-Infrequent Access (S3 One Zone-IA)。

**答案**
B

## Question #357

**題目**
某遊戲公司正將公開排行榜從資料中心遷移到 AWS Cloud。公司使用位於 Application Load Balancer 後方的 Amazon EC2 Windows Server 執行個體託管動態應用程式。此應用程式包含靜態檔案與動態的伺服器端程式碼。公司需要高可用的應用程式儲存解決方案。解決方案架構師應採取哪兩項步驟？

**選項**
- A. 將靜態檔案存放在 Amazon S3，並使用 Amazon CloudFront 在邊緣節點快取物件。
- B. 將靜態檔案存放在 Amazon S3，並使用 Amazon ElastiCache 在邊緣節點快取物件。
- C. 將伺服器端程式碼存放在 Amazon EFS，並將 EFS 掛載到每台 EC2 執行個體以共用檔案。
- D. 將伺服器端程式碼存放在 Amazon FSx for Windows File Server，並將 FSx 掛載到每台 EC2 執行個體…
- E. 將伺服器端程式碼存放在 General Purpose SSD (gp2) Amazon EBS 磁碟區，並將 EBS 掛載到…

**答案**
AD

## Question #358

**題目**
某社群媒體公司在 Application Load Balancer (ALB) 後方的 Amazon EC2 執行個體上執行其應用程式。該 ALB 是 Amazon CloudFront distribution 的 origin。應用程式在 Amazon S3 bucket 中儲存超過 10 億張圖片，且每秒處理數千張圖片。公司希望能動態調整圖片大小，並依據用戶端提供適當的格式。哪一個解決方案能以最低營運負擔滿足需求？

**選項**
- A. 在 EC2 執行個體安裝外部影像管理函式庫，並用該函式庫處理圖片。
- B. 建立 CloudFront origin request policy，利用該 policy 自動調整圖片大小並依據…提供適當格式。
- C. 使用帶有外部影像管理函式庫的 Lambda@Edge 函數，並將該函數關聯到 CloudFront…
- D. 建立 CloudFront response headers policy，利用該 policy 自動調整圖片大小並依據…提供適當格式。

**答案**
D

## Question #359

**題目**
某醫院需要將病患紀錄儲存在 Amazon S3 bucket。醫院的法遵團隊必須確保所有受保護健康資訊（PHI）在傳輸中與靜態儲存時都受到加密保護，且法遵團隊必須自行管理靜態資料加密金鑰。哪一個解決方案符合需求？

**選項**
- A. 在 AWS Certificate Manager (ACM) 建立公開 SSL/TLS 憑證，並將該憑證關聯到 Amazon S3，再設定預設…
- B. 在 S3 bucket policy 使用 aws:SecureTransport 條件，只允許透過 HTTPS (TLS) 的加密連線，並設定預設…
- C. 在 S3 bucket policy 使用 aws:SecureTransport 條件，只允許透過 HTTPS (TLS) 的加密連線，並設定預設…
- D. 在 S3 bucket policy 使用 aws:SecureTransport 條件，只允許透過 HTTPS (TLS) 的加密連線，並使用 Amazon Macie…

**答案**
C

## Question #360

**題目**
某公司在同一個 VPC 中使用 Amazon API Gateway 執行一個 private gateway，並提供兩個 REST API。BuyStock RESTful Web 服務會呼叫 CheckStock RESTful Web 服務。公司希望以最低營運負擔提供 API 間的授權。哪一個解決方案符合需求？

**選項**
- A. 在 HTTP header 中加入 X-API-Key 作為授權標頭。
- B. 使用 interface endpoint。
- C. 使用 gateway endpoint。
- D. 在兩個 REST API 之間加入 Amazon SQS queue。

**答案**
A

## Question #361

**題目**
某公司在 AWS 上託管多人遊戲應用程式。公司希望以低於毫秒等級的延遲讀取資料，並將超過 1 年未存取的資料封存 10 年。哪一個解決方案最具成本效益？

**選項**
- A. 將常用資料放在 Amazon RDS，並定期以自訂腳本匯出到 Amazon S3 bucket。
- B. 直接將資料存放於 Amazon S3 bucket，並透過 S3 Lifecycle policy 將舊資料移至 S3 Glacier Deep Archive…
- C. 將常用資料存放於 Amazon DynamoDB 並搭配 DynamoDB Accelerator (DAX)，再將資料匯出到 Amazon S3…
- D. 將常用資料存放於 Amazon DynamoDB，啟用 streaming 至 Amazon Kinesis Data Streams，再使用 Amazon Kinesis…

**答案**
B

## Question #362

**題目**
某公司使用支付處理系統，要求相同 payment ID 的訊息必須依寫入順序被接收。哪一些做法可以符合此需求？（選兩項）

**選項**
- A. 將訊息寫入 Amazon DynamoDB table，並以 payment ID 作為 partition key。
- B. 將訊息寫入 Amazon Kinesis data stream，並以 payment ID 作為 partition key。
- C. 將訊息寫入 Amazon ElastiCache for Memcached 叢集，並以 payment ID 作為 key。
- D. 將訊息寫入 Amazon SQS queue，並將 message attribute 設為 payment ID。
- E. 將訊息寫入 Amazon SQS FIFO queue，並將 message group 設為 payment ID。

**答案**
BD

## Question #363

**題目**
某公司正在建置遊戲系統，需要將唯一事件同時傳送給 leaderboard、matchmaking 與 authentication 等不同服務。哪一個服務最適合？

**選項**
- A. Amazon EventBridge event bus
- B. Amazon SNS FIFO topics
- C. Amazon SNS standard topics
- D. Amazon SQS FIFO queues

**答案**
B

## Question #364

**題目**
某醫院正在設計一個新應用程式以蒐集病患症狀。醫院決定使用 Amazon SQS 與 Amazon SNS 元件。醫院要求資料在傳輸過程與靜態儲存時都必須加密，且加密金鑰僅能由授權主體使用。哪兩項做法符合需求？

**選項**
- A. 對 SQS 元件啟用 server-side encryption，並更新預設 key policy 以限制僅授權主體可使用金鑰。
- B. 對 SNS 元件啟用 server-side encryption，並使用 AWS KMS customer managed key。
- C. 對 SNS 元件啟用加密，更新預設 key policy 以限制僅授權主體可使用金鑰，並設定…
- D. 對 SQS 元件啟用 server-side encryption，並使用 AWS KMS customer managed key。
- E. 對 SQS 元件啟用 server-side encryption，並使用 AWS KMS customer managed key…

**答案**
CD

## Question #365

**題目**
某公司執行由 Amazon RDS 支援的 Web 應用程式。新任資料庫管理員誤編資料導致資料遺失。公司需要找出最能保護資料的 RDS 功能。哪一個選項最適合？

**選項**
- A. Read replicas
- B. Manual snapshots
- C. Automated backups
- D. Multi-AZ deployments

**答案**
C

## Question #366

**題目**
某公司的 Web 應用程式由 Amazon API Gateway、AWS Lambda 與 Amazon DynamoDB 組成。Lambda 會從 DynamoDB 擷取付費內容，但公司只希望有訂閱的使用者能存取 premium content。哪一個解決方案最合適？

**選項**
- A. 在 API Gateway API 上啟用 API caching 與 throttling。
- B. 在 API Gateway API 上設定 AWS WAF，建立規則過濾有訂閱的使用者。
- C. 對 DynamoDB table 中的 premium content 套用細粒度 IAM 權限。
- D. 實作 API usage plans 與 API keys，限制未訂閱使用者的存取。

**答案**
C

## Question #367

**題目**
某公司使用 Amazon Route 53 latency-based routing，將全球使用者請求導向其使用 UDP 的應用程式。該應用程式端點位於內部部署環境，且分布於 3 個 AWS Region 對應的入口。公司希望改善可用性與效能。哪一個解決方案最適合？

**選項**
- A. 在 3 個 AWS Region 中各配置一個 Network Load Balancer (NLB) 來對應內部部署端點，並建立一個 accelerator…
- B. 在 3 個 AWS Region 中各配置一個 Application Load Balancer (ALB) 來對應內部部署端點，並建立一個…
- C. 在 3 個 AWS Region 中各配置一個 NLB 來對應內部部署端點，並在 Route 53 建立…
- D. 在 3 個 AWS Region 中各配置一個 ALB 來對應內部部署端點，並在 Route 53 建立…

**答案**
A

## Question #368

**題目**
解決方案架構師希望所有新使用者都必須符合特定密碼複雜度要求，且必須定期輪換 IAM 使用者密碼。應採取什麼做法？

**選項**
- A. 對整個 AWS 帳號設定整體密碼政策。
- B. 為 AWS 帳號中的每一位 IAM 使用者分別設定密碼政策。
- C. 使用第三方軟體設定密碼要求。
- D. 將 Amazon CloudWatch 規則掛到 Create_newuser 事件，在建立新使用者時設定密碼。

**答案**
A

## Question #369

**題目**
某公司已將應用程式遷移到 Amazon EC2 Linux 執行個體。其中一台 EC2 執行個體會依排程執行多個 1 小時的工作。公司希望改用受管服務並降低營運負擔。哪一個解決方案最適合？

**選項**
- A. 使用 AWS Batch 以 job 形式執行這些工作，並使用 Amazon EventBridge 排程這些 job。
- B. 將 EC2 執行個體轉為容器，並使用 AWS App Runner 依需求建立容器執行工作。
- C. 將這些工作複製成 AWS Lambda 函數，並使用 Amazon EventBridge 排程 Lambda 函數。
- D. 建立執行工作之 EC2 的 Amazon Machine Image (AMI)，再以 Auto Scaling group 執行…

**答案**
A

## Question #370

**題目**
某公司在 VPC 中執行一個公開的三層式 Web 應用程式。應用程式跨多個 Availability Zone 的 Amazon EC2 執行個體上執行。公司需要讓私有子網中的執行個體可對外連線下載套件，同時維持正確架構。哪一個做法正確？

**選項**
- A. 在公有子網建立 NAT instance，並在每個私有子網的 route table 加入指向 NAT instance 的預設路由。
- B. 在私有子網建立 NAT instance，並在每個私有子網的 route table 加入指向 NAT instance 的預設路由。
- C. 在公有子網建立 NAT gateway，並在每個私有子網的 route table 加入指向 NAT gateway 的預設路由。
- D. 在私有子網建立 NAT gateway，並在每個私有子網的 route table 加入指向 NAT gateway 的預設路由。

**答案**
C

## Question #371

**題目**
某公司需要建立 Amazon EKS 叢集來託管數位媒體串流應用程式。EKS 叢集中的資料會儲存在 Amazon EBS，且公司要求所有 EBS 磁碟區都必須使用 customer managed key 加密。哪兩個步驟符合需求？

**選項**
- A. 使用 Kubernetes 外掛，透過 customer managed key 進行資料加密。
- B. 建立 EKS 叢集後，找出 EBS 磁碟區，再使用 customer managed key 啟用加密。
- C. 在建立 EKS 叢集前，於該 AWS Region 啟用 EBS 預設加密，並選擇 customer managed key 作為預設金鑰。
- D. 建立 EKS 叢集後，建立一個具有 customer managed key 存取權限的 IAM role，並將其關聯…
- E. 將 customer managed key 儲存為 EKS 叢集中的 Kubernetes secret，並用來加密 EBS 磁碟區。

**答案**
AE

## Question #372

**題目**
某公司想將 Oracle 資料庫遷移到 AWS。該資料庫包含單一資料表，內含數百萬筆地理資訊影像。公司需要高擴展性與高效能查詢，且希望降低成本。哪一個解決方案最適合？

**選項**
- A. 將影像與地理代碼存入資料表，並使用 Amazon RDS Multi-AZ 上的 Oracle。
- B. 將影像存入 Amazon S3 bucket，並使用 Amazon DynamoDB，以 geographic code 為 key、image S3 URL 為值。
- C. 將影像與地理代碼都存入 Amazon DynamoDB table，並在高流量時配置 DynamoDB Accelerator (DAX)。
- D. 將影像存入 Amazon S3 bucket，並把地理代碼與影像 S3 URL 存入資料表，再使用 Oracle…

**答案**
B

## Question #373

**題目**
某公司有一個應用程式，會從汽車上的 IoT 感測器蒐集資料。資料透過 Amazon Kinesis Data Firehose 串流並儲存在 Amazon S3。公司需要將資料保留 1 年，並在之後以最低成本長期封存。哪一個做法最適合？

**選項**
- A. 使用 S3 Intelligent-Tiering，並建立 Lifecycle policy 於 1 年後轉到 S3 Glacier Deep Archive。
- B. 使用 S3 Intelligent-Tiering，並設定其自動將物件移至 S3 Glacier Deep Archive…
- C. 使用 S3 Standard-IA，並建立 Lifecycle policy 將物件移至 S3 Glacier…
- D. 使用 S3 Standard，並建立 Lifecycle policy 將物件移至 S3 Standard-IA，之後再…

**答案**
D

## Question #374

**題目**
某公司在 us-east-1 Region 的三個獨立 VPC 中執行多個商業應用程式。這些應用程式彼此必須能通訊，且未來可能還會增加更多 VPC。公司希望採用可擴展且管理負擔低的連線方式。哪一個解決方案最適合？

**選項**
- A. 在各 VPC 之間建立完整 VPC peering mesh。
- B. 使用 AWS PrivateLink 逐一連線每個服務。
- C. 建立 site-to-site VPN 連線各 VPC。
- D. 使用 AWS Transit Gateway 將三個 VPC 連接起來。

**答案**
D

## Question #375

**題目**
某電商公司正在建置分散式應用程式，流程中包含多個 serverless 函數與 AWS 服務，用來完成訂單處理流程。公司希望集中管理工作流程、追蹤步驟狀態，並在服務之間協調執行順序。哪一個解決方案最合適？

**選項**
- A. 使用 Amazon EventBridge 來協調所有服務。
- B. 使用 AWS Step Functions 建立工作流程。
- C. 使用 Amazon SNS 與 Lambda 進行串接。
- D. 使用 Amazon SQS 搭配多個 Lambda consumer。

**答案**
B

## Question #376

**題目**
某公司剛建立一個 Amazon RDS for MySQL DB instance。大多數資料庫連線來自 serverless 應用程式，且尖峰時會出現大量短連線，導致應用程式可用性下降。哪一個解決方案最適合？

**選項**
- A. 為該 RDS for MySQL DB instance 啟用 RDS Proxy。
- B. 啟用 read replica 來處理連線。
- C. 調高 Lambda timeout。
- D. 將資料庫移到 Amazon Aurora Serverless。

**答案**
A

## Question #377

**題目**
某公司最近部署新的稽核系統，用來集中管理作業系統版本、修補狀態與已安裝軟體等資訊。公司希望以最低營運負擔持續蒐集這些跨 EC2 執行個體的資訊。哪一個解決方案最適合？

**選項**
- A. 使用 Amazon Inspector 蒐集作業系統與軟體資訊。
- B. 使用 AWS Systems Manager Inventory。
- C. 使用 AWS Config 蒐集作業系統資訊。
- D. 使用 CloudTrail 記錄修補狀態。

**答案**
B

## Question #378

**題目**
某公司正在開發即時多人遊戲，客戶端與 Auto Scaling group 中的伺服器之間使用 UDP 通訊。公司希望提供最低延遲並快速將流量導向最近的健康端點。哪一個解決方案最適合？

**選項**
- A. 使用 Application Load Balancer 搭配 Route 53 latency-based routing。
- B. 使用 AWS Global Accelerator 搭配 Network Load Balancer。
- C. 使用 CloudFront 搭配 UDP origin。
- D. 使用 Transit Gateway 連接各區域遊戲伺服器。

**答案**
B

## Question #379

**題目**
某公司託管一個前端應用程式，其後端為與 AWS Lambda 整合的 Amazon API Gateway API。當 API 收到請求時，Lambda 會查詢資料庫。公司發現相同資料集會被頻繁重複查詢，導致效能問題。哪一個解決方案最適合？

**選項**
- A. 讓前端直接連資料庫，略過 API 以加快查詢。
- B. 對處理請求的 Lambda 函數設定 provisioned concurrency。
- C. 將查詢結果快取在 Amazon S3，以加速類似資料集的後續存取。
- D. 調大資料庫規模，增加 Lambda 可同時建立的連線數。

**答案**
C

## Question #380

**題目**
某公司正將內部部署工作負載遷移到 AWS Cloud。公司已使用多台 Amazon EC2 執行個體與 Amazon RDS DB instance。這些資源僅於上班時間使用，公司希望自動在非上班時間關閉資源以降低成本。哪一個解決方案最適合？

**選項**
- A. 使用 elastic resize 擴縮 EC2，並在下班後將 DB instance 縮到 0。
- B. 到 AWS Marketplace 尋找可排程啟停 EC2 與 DB instance 的合作夥伴方案。
- C. 再建立一台 EC2，透過 crontab 執行 shell script 啟停既有的 EC2 與 DB instance。
- D. 建立 AWS Lambda 函數來啟停 EC2 與 DB instance，並使用 Amazon EventBridge 依排程呼叫。

**答案**
A

## Question #381

**題目**
某公司託管一個三層式 Web 應用程式，其中包含 PostgreSQL 資料庫。該資料庫儲存文件的中繼資料，而公司想要大量保存文件內容並執行報表分析，同時避免影響主交易型資料庫效能。哪一個解決方案最適合？

**選項**
- A. 建立新的 Amazon DocumentDB（相容 MongoDB）叢集，包含 read replica，並將報表查詢導向 replica。
- B. 建立新的 Amazon Aurora PostgreSQL DB cluster，包含 Aurora Replica，並將報表查詢導向 Replica。
- C. 建立新的 Amazon RDS for PostgreSQL Multi-AZ DB instance，並讓報表模組查詢次要節點。
- D. 建立新的 Amazon DynamoDB table 儲存文件，並以固定 write capacity 支援新增。

**答案**
D

## Question #382

**題目**
某公司在 AWS 上有一個三層式應用程式，會從使用者裝置接收感測器資料。流量先經過 Network Load Balancer，再進入 Amazon EC2 執行個體。公司需要保護傳輸中的資料免於竊聽，並維持架構簡潔。哪一個解決方案最適合？

**選項**
- A. 設定 TLS listener，並在 NLB 部署伺服器憑證。
- B. 啟用 AWS Shield Advanced，並在 NLB 上啟用 AWS WAF。
- C. 將負載平衡器改成 Application Load Balancer (ALB)，並在 ALB 上啟用 AWS WAF。
- D. 使用 AWS KMS 加密 EC2 執行個體上的 Amazon EBS 磁碟區。

**答案**
A

## Question #383

**題目**
某公司打算將一套商用套裝軟體從內部部署資料中心遷移到 AWS。該軟體的授權條款要求必須使用專屬實體主機。哪一種購買選項最適合？

**選項**
- A. Dedicated Reserved Hosts
- B. Dedicated On-Demand Hosts
- C. Dedicated Reserved Instances
- D. Dedicated On-Demand Instances

**答案**
A

## Question #384

**題目**
某公司在跨多個 Availability Zone 的 Amazon EC2 Linux 執行個體上執行應用程式。應用程式需要一個共享儲存層，且資料最初存取頻繁、之後轉為低頻存取。公司希望在維持高可用的前提下降低成本。哪一個儲存解決方案最適合？

**選項**
- A. 使用 Amazon S3 Standard，並透過 Lifecycle policy 將低頻資料移到 S3 Glacier。
- B. 使用 Amazon S3 Standard，並透過 Lifecycle policy 將低頻資料移到 S3 Standard-Infrequent Access。
- C. 使用 Amazon EFS Standard，並透過 lifecycle management 將低頻資料移至較低成本層級。
- D. 使用 Amazon EFS One Zone，並透過 lifecycle management 將低頻資料移至較低成本層級。

**答案**
B

## Question #385

**題目**
解決方案架構師正在建立新的 VPC 設計。架構包含兩個供 load balancer 使用的公有子網、兩個供 Web 伺服器使用的私有子網，以及兩個供 MySQL 使用的私有子網。Web 伺服器只使用 HTTPS。架構師已經建立一個允許來自 0.0.0.0/0 之 443 連線的 load balancer security group。公司政策要求每項資源都僅具備完成任務所需的最低存取權限。應採用哪一種額外設定策略？

**選項**
- A. 為 Web 伺服器建立 security group，允許來自 0.0.0.0/0 的 443；為 MySQL 伺服器建立 security group，允許…
- B. 為 Web 伺服器建立 network ACL，允許來自 0.0.0.0/0 的 443；為 MySQL 伺服器建立 network ACL，允許…
- C. 為 Web 伺服器建立 security group，允許來自 load balancer 的 443；為 MySQL 伺服器建立 security group，允許來自 Web 伺服器。
- D. 為 Web 伺服器建立 network ACL，允許來自 load balancer 的 443；為 MySQL 伺服器建立 network ACL，允許…

**答案**
C

## Question #386

**題目**
某電商公司在 AWS 上執行多層式應用程式。前端與後端層都在 Amazon EC2 上執行，資料庫則使用 Amazon RDS for MySQL。後端層會與 RDS instance 通訊。由於經常重複呼叫相同資料集，導致資料庫效能下降。應採取什麼動作改善後端效能？

**選項**
- A. 使用 Amazon SNS 儲存資料庫呼叫。
- B. 使用 Amazon ElastiCache 快取大型資料集。
- C. 使用 RDS for MySQL read replica 快取資料庫呼叫。
- D. 使用 Amazon Kinesis Data Firehose 串流資料庫呼叫。

**答案**
B

## Question #387

**題目**
某新進員工加入公司擔任部署工程師，將使用 AWS CloudFormation 範本建立多個 AWS 資源。解決方案架構師希望該工程師在執行工作時遵循最小權限原則。應採取哪兩項做法？

**選項**
- A. 讓部署工程師使用 AWS 帳號 root user 憑證執行 CloudFormation stack 操作。
- B. 建立新的 IAM user，並加入附有 PowerUsers IAM policy 的群組。
- C. 建立新的 IAM user，並加入附有 AdministratorAccess IAM policy 的群組。
- D. 建立新的 IAM user，並加入一個具有允許 AWS CloudFormation 所需權限之 IAM policy 的群組。
- E. 為部署工程師建立 IAM role，明確定義特定 CloudFormation stack 所需的權限，並讓其使用該 role。

**答案**
DE

## Question #388

**題目**
某公司在 VPC 中部署兩層式 Web 應用程式。Web 層使用位於公有子網的 Amazon EC2 Auto Scaling group，資料庫層則為 Amazon RDS。公司需要讓 Web 層可以存取資料庫層。哪一個解決方案最適合？

**選項**
- A. 在私有子網的 network ACL 加入明確規則，允許來自 Web 層 EC2 的流量。
- B. 在 VPC route table 加入路由，允許 Web 層 EC2 與資料庫層間的流量。
- C. 將 Web 層 EC2 與資料庫層 RDS 分別部署在兩個 VPC，並設定 VPC peering。
- D. 在資料庫層 RDS 的 security group 加入 inbound 規則，允許來自 Web 層 security group 的流量。

**答案**
D

## Question #389

**題目**
某公司在單一 Availability Zone 的 Amazon RDS for MySQL DB instance 中，儲存線上廣告業務的大型資料集。公司需要提升商業報表查詢效能，同時不影響交易寫入。哪一個解決方案最適合？

**選項**
- A. 部署 RDS read replica 以處理商業報表查詢。
- B. 將 DB instance 水平擴展並放在 Elastic Load Balancer 後方。
- C. 將 DB instance 升級為更大規格以同時處理寫入與查詢。
- D. 將 DB instance 部署為 Multi-AZ，用於處理商業報表查詢。

**答案**
D

## Question #390

**題目**
某公司在一組 Amazon EC2 執行個體上託管三層式電商應用程式。這些執行個體位於 Application Load Balancer 後方，並在 Auto Scaling group 中執行。公司需要確保使用者工作階段在擴展後仍可持續。哪兩個解決方案可行？

**選項**
- A. 在 ALB 啟用 sticky sessions（session affinity）。
- B. 使用 Amazon DynamoDB table 儲存使用者 session 資訊。
- C. 部署 Amazon Cognito user pool 管理使用者 session。
- D. 部署 Amazon ElastiCache for Redis 叢集儲存使用者 session 資訊。
- E. 在應用程式中使用 AWS Systems Manager Application Manager 管理使用者 session。

**答案**
BD

## Question #391

**題目**
某公司需要為三層式無狀態 Web 應用程式建立備份策略。該應用程式在 Auto Scaling group 中的 Amazon EC2 執行個體上執行，資料庫層使用 Amazon RDS。公司需要同時備份應用程式層與資料庫層。哪一個解決方案最適合？

**選項**
- A. 每 2 小時對 EC2 與資料庫的 Amazon EBS 磁碟區做 snapshot，以符合…
- B. 設定 snapshot lifecycle policy 來建立 Amazon EBS snapshot，並在 Amazon RDS 啟用自動備份。
- C. 保留 Web 與應用程式層的最新 Amazon Machine Images (AMI)，在 Amazon RDS 啟用自動備份，並使用…
- D. 每 2 小時對 EC2 的 Amazon EBS 磁碟區做 snapshot，並在 Amazon RDS 啟用自動備份。

**答案**
D

## Question #392

**題目**
某公司要在 AWS 部署新的公開 Web 應用程式。應用程式包含使用 Amazon EC2 的 Web 伺服器層，並使用資料庫層。公司需要正確設定安全性群組，確保公開入口與後端隔離。哪一個做法最適合？

**選項**
- A. 將 Web 伺服器 security group 設為允許來自 0.0.0.0/0 的 443 入站流量，並將資料庫 security group 僅允許來自 Web 層的必要連線。
- B. 將 Web 伺服器 security group 設為僅允許客戶 IP 的 443，並設定…
- C. 將 Web 伺服器 security group 設為僅允許客戶 IP 的 443，並設定…
- D. 將 Web 伺服器 security group 設為允許來自 0.0.0.0/0 的 443，並設定…

**答案**
A

## Question #393

**題目**
某支付處理公司會錄下與客戶的所有語音通話，並將音訊檔儲存在 Amazon S3 bucket。公司需要從通話中偵測並遮罩 PII。哪一個解決方案最適合？

**選項**
- A. 使用 Amazon Kinesis Video Streams 處理音訊檔，再以 AWS Lambda 掃描已知 PII 模式。
- B. 當音訊檔上傳到 S3 bucket 時，觸發 AWS Lambda 啟動 Amazon Textract 任務分析通話內容。
- C. 設定 Amazon Transcribe transcription job 並啟用 PII redaction。當音訊檔上傳到 S3 bucket 時，觸發…
- D. 建立 Amazon Connect contact flow 匯入音訊檔並啟用轉錄，再內嵌 AWS Lambda…

**答案**
C

## Question #394

**題目**
某公司在 AWS Cloud 上執行多層式電商 Web 應用程式。應用程式執行於 Amazon EC2 執行個體，資料庫使用 Amazon RDS。最近發現儲存層 IOPS 成為瓶頸，需要提升磁碟效能。哪一個做法最適合？

**選項**
- A. 將磁碟區改成磁性磁碟。
- B. 增加 gp3 磁碟區的 IOPS 數量。
- C. 將磁碟區改成 Provisioned IOPS SSD (io2) 磁碟區。
- D. 將 2,000 GB gp3 磁碟區改成兩個 1,000 GB gp3 磁碟區。

**答案**
C

## Question #395

**題目**
某 IAM 使用者在上週正式環境部署期間，對公司 AWS 帳號中的資源做了多項設定變更。解決方案架構師需要找出是哪位使用者對哪些資源做了變更。應使用哪一項服務？

**選項**
- A. Amazon GuardDuty
- B. Amazon Inspector
- C. AWS CloudTrail
- D. AWS Config

**答案**
B

## Question #396

**題目**
某公司在 AWS 上實作自管 DNS 服務，架構由不同 Availability Zone 的 Amazon EC2 執行個體組成，並搭配 accelerator。公司需要保護此服務避免遭受 DDoS 攻擊。哪一個解決方案最合適？

**選項**
- A. 訂閱 AWS Shield Advanced，並將 accelerator 加入受保護資源。
- B. 訂閱 AWS Shield Advanced，並將 EC2 執行個體加入受保護資源。
- C. 建立含有 rate-based rule 的 AWS WAF web ACL，並將其關聯到 accelerator。
- D. 建立含有 rate-based rule 的 AWS WAF web ACL，並將其關聯到 EC2 執行個體。

**答案**
A

## Question #397

**題目**
某電商公司需要執行每日排程工作，彙總並過濾銷售紀錄以供分析。公司將銷售紀錄儲存在 Amazon S3，並希望以最低營運負擔執行每日工作。哪一個解決方案最適合？

**選項**
- A. 建立 AWS Lambda 函數，並以 Amazon EventBridge 排程每天執行一次。
- B. 建立 AWS Lambda 函數，再建立 Amazon API Gateway HTTP API 與函數整合，並透過 Amazon…
- C. 建立 Amazon ECS 叢集並使用 AWS Fargate launch type，再建立 Amazon EventBridge 排程。
- D. 建立 Amazon ECS 叢集並使用 Amazon EC2 launch type 與 Auto Scaling group…

**答案**
C

## Question #398

**題目**
某公司需要將 600 TB 資料從內部部署的 NAS 系統傳輸到 AWS Cloud。資料傳輸必須儘快完成，且不應大幅依賴現有網路頻寬。哪一個解決方案最適合？

**選項**
- A. 使用 Amazon S3 multipart upload，透過 HTTPS 傳輸檔案。
- B. 在內部部署 NAS 與最近的 AWS Region 之間建立 VPN，並透過 VPN 傳輸資料。
- C. 透過 AWS Snow Family 主控台訂購多台 AWS Snowball Edge Storage Optimized 裝置，並使用這些裝置傳輸資料。
- D. 建立 10 Gbps AWS Direct Connect 連線到最近的 AWS Region，再透過該連線傳輸資料。

**答案**
B

## Question #399

**題目**
某金融公司在 AWS 上託管 Web 應用程式。該應用程式使用 Amazon API Gateway Regional API endpoint，讓使用者查詢即時股價。公司安全團隊注意到 API 請求數量增加，並擔心 HTTP flood 攻擊會讓應用程式離線。解決方案架構師需要以最低營運負擔保護應用程式。哪一個解決方案符合需求？

**選項**
- A. 在 API Gateway Regional API endpoint 前建立 Amazon CloudFront distribution，並將最大 TTL 設為 24 小時。
- B. 建立 Regional AWS WAF web ACL，加入 rate-based rule，並將 web ACL 關聯到 API Gateway stage。
- C. 使用 Amazon CloudWatch metrics 監控 Count 指標，當達到預先定義速率時通知安全團隊。
- D. 在 API Gateway Regional API endpoint 前建立 Amazon CloudFront distribution 並搭配 Lambda@Edge，再建立 AWS…

**答案**
B

## Question #400

**題目**
某氣象新創公司有一套自製 Web 應用程式，用來在線上銷售氣象資料。公司使用 Amazon DynamoDB 儲存資料，並想建立一項新服務：每當有新的氣象事件被記錄時，就通知 4 個內部團隊的管理者。公司不希望這項新服務影響現有應用程式效能。哪一個解決方案能以最低營運負擔符合需求？

**選項**
- A. 使用 DynamoDB transactions 將新事件資料寫入資料表，並在 transaction 中設定通知內部團隊。
- B. 讓現有應用程式將訊息發佈到 4 個 Amazon SNS topic，並由各團隊…
- C. 在資料表上啟用 Amazon DynamoDB Streams，並使用 trigger 寫入單一 Amazon SNS topic。
- D. 在每筆記錄增加一個自訂屬性標記新項目，再以 cron job 每分鐘掃描一次新資料並…

**答案**
C
