# AWS SAA 第 151～200 題繁中翻譯（跳過第 165 題）

說明：
- 依據你上傳的原始整理檔進行翻譯與整理。
- 依你的要求，直接跳過 Question #165。
- AWS 服務名稱保留英文，以避免用詞失真。
- 因原始檔可擷取內容有限，無法完整還原的題目已明確標註。

---

## Question #151

**題目**  
某公司想將其內部部署資料中心遷移至 AWS。根據公司的合規要求，公司只能使用 ap-northeast-3 區域。公司管理員不得將 VPC 連接到網際網路。下列哪些解決方案可滿足這些需求？（選兩項）

**選項**
- A. 使用 AWS Control Tower 實作資料駐留防護機制，拒絕網際網路存取，並拒絕除 ap-northeast-3 以外所有 AWS 區域的存取。
- B. 使用 AWS WAF 規則防止網際網路存取，並在 AWS 帳戶設定中拒絕除 ap-northeast-3 以外所有 AWS 區域的存取。
- C. 使用 AWS Organizations 設定服務控制政策（SCP），防止 VPC 取得網際網路連線，並拒絕除 ap-northeast-3 以外所有 AWS 區域的存取。
- D. 在每個 VPC 的網路 ACL 中建立輸出規則，拒絕所有來自 0.0.0.0/0 的流量；再為每位使用者建立 IAM 政策以限制區域存取。
- E. 使用 AWS Config 啟用受管規則，以偵測並警示 Internet Gateway，以及偵測並警示部署於非允許區域的新資源。

**答案**  
AC

---

## Question #152

**題目**  
某公司使用三層式 Web 應用程式為新進員工提供訓練。此應用程式每天只會被存取 12 小時。公司目前使用 Amazon RDS for MySQL DB instance 來儲存資訊，並希望將成本降到最低。解決方案架構師應該怎麼做？

**選項**
- A. 為 AWS Systems Manager Session Manager 設定 IAM 政策，建立 IAM 角色並更新信任關係。
- B. 建立 Amazon ElastiCache for Redis 叢集，讓使用者在 DB 停止時仍可從快取讀取資料。
- C. 啟動一台 Amazon EC2 執行個體，建立可存取 Amazon RDS 的 IAM 角色並掛載於該執行個體，再設定排程控制資料庫。
- D. 建立 AWS Lambda 函數來啟動與停止 DB instance，並建立 Amazon EventBridge（Amazon CloudWatch Events）排程規則。

**答案**  
D

---

## Question #153

**題目**  
某公司銷售由熱門歌曲片段製成的手機鈴聲。這些檔案儲存在 Amazon S3 Standard，且每個檔案至少 128 KB。公司擁有數百萬個檔案，但超過 90 天的鈴聲下載次數很低。公司希望在節省儲存成本的同時，讓最常被存取的檔案仍能快速提供給使用者。公司應採取哪個最具成本效益的做法？

**選項**
- A. 將物件的初始儲存層級設定為 S3 Standard-Infrequent Access（S3 Standard-IA）。
- B. 將檔案移至 S3 Intelligent-Tiering，並設定在 90 天後移動到較便宜的儲存層級。
- C. 啟用 S3 Inventory 來管理物件，並在 90 天後將物件移至 S3 Standard-IA。
- D. 實作 S3 Lifecycle policy，在 90 天後將物件從 S3 Standard 移至 S3 Standard-IA。

**答案**  
D

---

## Question #154

**題目**  
某公司需要將醫療試驗結果儲存在 Amazon S3 儲存庫中。此儲存庫必須允許少數科學家新增檔案，並限制其他所有使用者僅能唯讀。沒有任何使用者可以修改或刪除儲存庫中的檔案。公司還必須將每個檔案自建立日起至少保留 1 年。哪個解決方案可滿足這些需求？

**選項**
- A. 使用 S3 Object Lock 的 governance mode，並設定 1 年的 legal hold。
- B. 使用 S3 Object Lock 的 compliance mode，並設定 365 天的保留期間。
- C. 使用 IAM role 限制所有使用者不得刪除或修改 S3 bucket 中的物件，並搭配 S3 bucket policy 僅允許特定 IAM 寫入。
- D. 設定 S3 bucket 在每次新增物件時觸發 AWS Lambda 函數，由函數追蹤物件雜湊值以防更動。

**答案**  
B

---

## Question #155

**題目**  
某大型媒體公司在 AWS 上託管一個 Web 應用程式。公司希望開始快取機密的媒體檔案，讓全球使用者都能穩定存取。內容儲存在 Amazon S3 buckets 中。公司必須不論請求來自哪個地理位置，都能快速交付內容。哪個解決方案可滿足這些需求？

**選項**
- A. 使用 AWS DataSync 將 S3 buckets 與 Web 應用程式連接。
- B. 部署 AWS Global Accelerator 以連接 S3 buckets 與 Web 應用程式。
- C. 部署 Amazon CloudFront，讓 S3 buckets 與 CloudFront edge servers 連接。
- D. 使用 Amazon SQS 將 S3 buckets 與 Web 應用程式連接。

**答案**  
C

---

## Question #156

**題目**  
某公司會產出來自不同資料庫的批次資料，也會產出來自網路感測器與應用程式 API 的即時串流資料。公司需要將所有資料整合到同一處，以供商業分析使用。公司需要先處理進來的資料，再將資料暫存到不同的 Amazon S3 buckets 中。後續團隊將執行一次性查詢，並把資料匯入商業智慧工具以呈現 KPI。哪一組步驟可用最低營運負擔滿足需求？（選兩項）

**選項**
- A. 使用 Amazon Athena 執行一次性查詢，並使用 Amazon QuickSight 建立 KPI 儀表板。
- B. 使用 Amazon Kinesis Data Analytics 執行一次性查詢，並使用 Amazon QuickSight 建立 KPI 儀表板。
- C. 建立自訂 AWS Lambda 函數，將各資料庫中的個別紀錄搬移到 Amazon Redshift 叢集。
- D. 使用 AWS Glue ETL job 將資料轉換為 JSON 格式，並載入多個 Amazon S3 buckets。
- E. 使用 AWS Lake Formation 的 blueprints 識別可匯入資料湖的資料，並用 AWS Glue 爬取來源。

**答案**  
AC

---

## Question #157

**題目**  
某公司將資料儲存在 Amazon Aurora PostgreSQL DB cluster 中。公司必須將所有資料保留 5 年，且 5 年後必須刪除所有資料。同時，公司也必須無限期保留資料庫中執行動作的稽核日誌。目前公司已為 Aurora 設定 automated backups。解決方案架構師應採取哪兩個步驟來滿足需求？（選兩項）

**選項**
- A. 為 DB cluster 建立手動快照。
- B. 為 automated backups 建立 lifecycle policy。
- C. 將 automated backup retention 設為 5 年。
- D. 為 DB cluster 設定 Amazon CloudWatch Logs 匯出。
- E. 使用 AWS Backup 建立備份並將備份保留 5 年。

**答案**  
BE

---

## Question #158

**題目**  
某解決方案架構師正在為即將到來的音樂活動最佳化網站。演出影片將會即時串流播放，之後也會提供隨選觀看。此活動預期會吸引全球線上觀眾。哪個服務可以同時提升即時串流與隨選串流的效能？

**選項**
- A. Amazon CloudFront
- B. AWS Global Accelerator
- C. Amazon Route 53
- D. Amazon S3 Transfer Acceleration

**答案**  
A

---

## Question #159

**題目**  
某公司正在執行一個對外公開的無伺服器應用程式，使用 Amazon API Gateway 與 AWS Lambda。近期因來自 botnet 的詐騙請求暴增，應用程式流量大幅上升。解決方案架構師應採取哪些步驟來封鎖未授權使用者的請求？（選兩項）

**選項**
- A. 建立 usage plan，並只將 API key 分享給真正的使用者。
- B. 在 Lambda 函數中加入邏輯，忽略來自詐騙 IP 的請求。
- C. 實作 AWS WAF 規則，鎖定惡意請求並加以過濾。
- D. 將現有的 public API 改為 private API，並更新 DNS 記錄重新導向使用者至新端點。
- E. 為每位呼叫 API 的使用者建立 IAM role，讓使用者在呼叫前先 Assume Role。

**答案**  
CD

---

## Question #160

**題目**  
某電商公司在 AWS Cloud 上執行其分析應用程式。該應用程式每月產生約 300 MB 的 JSON 格式資料。公司正在評估災難復原方案來備份這些資料。若需要使用資料，必須能以毫秒等級存取，且資料需保留 30 天。哪個方案最具成本效益？

**選項**
- A. Amazon OpenSearch Service（Amazon Elasticsearch Service）
- B. Amazon S3 Glacier
- C. Amazon S3 Standard
- D. Amazon RDS for PostgreSQL

**答案**  
C

---

## Question #161

**題目**  
某公司有一個小型 Python 應用程式，會處理 JSON 文件，並將結果輸出到內部部署的 SQL 資料庫。該應用程式每天會執行數千次。公司想將此應用程式移轉到 AWS Cloud，並需要一個高可用、可高度擴展且營運負擔最低的解決方案。哪個解決方案可滿足需求？

**選項**
- A. 將 JSON 文件放入 Amazon S3 bucket，並在多台 Amazon EC2 instances 上執行 Python 程式進行處理。
- B. 將 JSON 文件放入 Amazon S3 bucket，建立 AWS Lambda function 執行 Python 程式進行處理。
- C. 將 JSON 文件放入 Amazon EBS volume，並使用 EBS Multi-Attach 將該磁碟掛載至多台執行個體。
- D. 將 JSON 文件放入 Amazon SQS queue 作為訊息，並將 Python 程式部署為可處理佇列訊息的服務。

**答案**  
D

---

## Question #162

**題目**  
某公司想在 AWS 上使用高效能運算（HPC）基礎設施來進行金融風險建模。公司的 HPC 工作負載執行於 Linux 上。每個 HPC 工作流程都會使用數百台 Amazon EC2 Spot Instances、執行時間短，並產生數千個輸出檔，最終會儲存在持久性儲存中以供分析與長期使用。公司需要一種雲端儲存解決方案，可以將內部部署資料複製到長期持久儲存，讓所有 EC2 執行個體都能用於處理，同時也需要一個與持久儲存整合的高效能檔案系統來讀寫資料集與輸出檔案。哪一組 AWS 服務可滿足需求？

**選項**
- A. Amazon FSx for Lustre 與 Amazon S3 整合
- B. Amazon FSx for Windows File Server 與 Amazon S3 整合
- C. Amazon S3 Glacier 與 Amazon EBS 整合
- D. Amazon S3 bucket 搭配 VPC endpoint，並整合 Amazon EBS General Purpose SSD（gp2）

**答案**  
A

---

## Question #163

**題目**  
某公司正在內部部署環境中建置容器化應用程式，並決定將該應用程式移轉到 AWS。應用程式在部署後不久就會有數千名使用者。公司不確定要如何大規模管理容器部署，並需要一個高可用且營運負擔最低的方案。哪個解決方案可滿足這些需求？

**選項**
- A. 將容器映像儲存在 Amazon Elastic Container Registry（Amazon ECR），並使用 Amazon Elastic Container Service（Amazon ECS）。
- B. 將容器映像儲存在 Amazon Elastic Container Registry（Amazon ECR），並使用另一種 Amazon ECS 部署模式。
- C. 將容器映像儲存在執行於 Amazon EC2 執行個體上的映像儲存庫中，並讓容器執行於分散部署的 EC2 執行個體上。
- D. 建立包含容器映像的 Amazon EC2 AMI，並以 Auto Scaling 方式啟動 EC2 執行個體。

**答案**  
C

---

## Question #164

**題目**  
某公司有兩個應用程式：一個傳送端應用程式會傳送待處理的訊息與負載內容；另一個處理端應用程式則負責接收這些訊息與負載。公司希望導入 AWS 服務來處理兩個應用程式之間的訊息傳遞。傳送端每小時大約會送出 1,000 則訊息，這些訊息可能需要最多 2 天才能處理完成；若處理失敗，訊息必須被保留，且不得影響其他剩餘訊息的處理。哪個解決方案最具營運效率且可滿足需求？

**選項**
- A. 架設一台執行 Redis 資料庫的 Amazon EC2 instance，讓兩個應用程式都使用它來儲存、處理與刪除訊息。
- B. 使用 Amazon Kinesis Data Streams 接收傳送端送出的訊息，並讓處理端整合 Kinesis Client Library（KCL）。
- C. 讓傳送端與處理端整合 Amazon Simple Queue Service（Amazon SQS）queue，並設定 dead-letter queue 來收集處理失敗的訊息。
- D. 讓處理端訂閱 Amazon Simple Notification Service（Amazon SNS）topic 以接收通知處理訊息，並讓傳送端寫入 SNS topic。

**答案**  
C

---

## Question #165

依你的要求，已跳過此題。

---

## Question #166

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #167

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #168

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #169

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #170

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #171

**題目**  
某公司提供一個 API 給使用者，自動依商品價格計算稅額。公司只有在假期期間才會出現大量查詢，並因此造成回應變慢。解決方案架構師需要設計一個可擴展且具彈性的解決方案。應該怎麼做？

**選項**
- A. 提供一個託管於 Amazon EC2 instance 上的 API，由 EC2 instance 在接收到 API 請求時執行所需運算。
- B. 使用 Amazon API Gateway 設計 REST API 接收商品名稱，並由 API Gateway 將商品名稱傳給 AWS Lambda 進行稅額計算。
- C. 建立一個 Application Load Balancer，後方掛兩台 Amazon EC2 instances，讓它們計算稅額。
- D. 使用 Amazon API Gateway 設計 REST API，並將其連接到一個託管於 Amazon EC2 instance 上的 API，由 API Gateway 接收並轉送請求。

**答案**  
D

---

## Question #172

**題目**  
某解決方案架構師正在為一個應用程式建立新的 Amazon CloudFront distribution。使用者提交的部分資訊屬於敏感資料。該應用程式已使用 HTTPS，但還需要額外一層安全保護。敏感資訊應在整個應用程式堆疊中受到保護，且只有特定應用程式可存取這些資訊。解決方案架構師應採取哪個動作？

**選項**
- A. 設定 CloudFront signed URL。
- B. 設定 CloudFront signed cookie。
- C. 設定 CloudFront field-level encryption profile。
- D. 設定 CloudFront，並將 Origin Protocol Policy 設為 HTTPS Only，同時調整 Viewer Protocol Policy。

**答案**  
A

---

## Question #173

**題目**  
某遊戲公司在 AWS 上託管一個瀏覽器型應用程式。使用者會大量存取儲存在 Amazon S3 中的影片與圖片，而且這些內容對所有使用者都相同。隨著應用程式越來越受歡迎，全球已有數百萬使用者存取這些媒體檔案。公司希望在降低來源端負載的同時，將檔案提供給使用者。哪個解決方案最具成本效益？

**選項**
- A. 在 Web 伺服器前部署 AWS Global Accelerator。
- B. 在 S3 bucket 前部署 Amazon CloudFront Web distribution。
- C. 在 Web 伺服器前部署 Amazon ElastiCache for Redis。
- D. 在 Web 伺服器前部署 Amazon ElastiCache for Memcached。

**答案**  
B

---

## Question #174

**題目**  
某公司有一個多層式應用程式，前端 Web 層由六台執行於單一 Availability Zone 的 Amazon EC2 Web 伺服器組成，並位於一個 Auto Scaling group 中，前方有 Application Load Balancer（ALB）。解決方案架構師需要在不修改應用程式的前提下，將基礎設施改為高可用。應選擇哪一種架構？

**選項**
- A. 建立一個 Auto Scaling group，在兩個 Region 中各放三台執行個體。
- B. 將 Auto Scaling group 修改為橫跨兩個 Availability Zone，各放三台執行個體。
- C. 建立一個 Auto Scaling 範本，以便在另一個 Region 中快速建立更多執行個體。
- D. 將前方的 ALB 改為 round-robin 組態，以平衡傳送到 Web 層的流量。

**答案**  
B

---

## Question #175

**題目**  
某電商公司有一個訂單處理應用程式，使用 Amazon API Gateway 與 AWS Lambda function。該應用程式將資料儲存在 Amazon Aurora PostgreSQL 資料庫中。在最近一次銷售活動中，客戶訂單突然暴增，部分客戶遭遇逾時，且其訂單未被處理。解決方案架構師發現，由於開啟了大量資料庫連線，導致資料庫的 CPU 與記憶體使用率都很高。架構師需要在對應用程式做最少修改的前提下，避免逾時錯誤。哪個解決方案可滿足需求？

**選項**
- A. 為 Lambda function 設定 provisioned concurrency，並將資料庫改為跨多個 AWS Region 的 global database。
- B. 使用 Amazon RDS Proxy 為資料庫建立 proxy，並修改 Lambda function 讓它使用 RDS Proxy endpoint，而不是直接連線到資料庫。
- C. 在不同 AWS Region 建立資料庫 read replica，並使用 API Gateway 的 query string 參數將流量導向 read replica。
- D. 使用 AWS Database Migration Service（AWS DMS）將 Aurora PostgreSQL 的資料移轉到 Amazon DynamoDB，並修改應用程式。

**答案**  
B

---

## Question #176

**題目**  
某應用程式執行於 private subnets 中的 Amazon EC2 instances 上，且需要存取 Amazon DynamoDB table。要在確保流量不離開 AWS 網路的前提下，以最安全的方式存取該資料表，應該怎麼做？

**選項**
- A. 為 DynamoDB 使用 VPC endpoint。
- B. 在 public subnet 中使用 NAT gateway。
- C. 在 private subnet 中使用 NAT instance。
- D. 使用附加在 VPC 上的 internet gateway。

**答案**  
D

---

## Question #177

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #178

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #179

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #180

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #181

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #182

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #183

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #184

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #185

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #186

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #187

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #188

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #189

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #190

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #191

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #192

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #193

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #194

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #195

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。

---

## Question #196

**題目**  
某公司在大量 Amazon EC2 instances 上執行應用程式。該應用程式會讀寫 Amazon DynamoDB table 中的資料項目。DynamoDB table 的大小持續成長，但應用程式只需要最近 30 天的資料。公司需要一個可將成本與開發工作量降到最低的解決方案。哪個方案可滿足需求？

**選項**
- A. 使用 AWS CloudFormation 範本部署完整解決方案，每 30 天重新部署一次堆疊，並刪除舊資料。
- B. 使用一台執行來自 AWS Marketplace 的監控應用程式的 EC2 instance，並將其設定為搭配 Amazon DynamoDB 使用。
- C. 設定 Amazon DynamoDB Streams，在建立新項目時觸發 AWS Lambda function，再由 Lambda 執行後續資料刪除控制。
- D. 擴充應用程式，在每次建立新項目時額外新增一個屬性，其值為目前時間戳記加上 30 天。

**答案**  
D

---

## Question #197

**題目**  
某公司有一個執行在內部部署 Windows Server 上的 Microsoft .NET 應用程式。該應用程式使用 Oracle Database Standard Edition 儲存資料。公司計畫遷移到 AWS，並希望在遷移過程中盡量減少開發修改。同時，AWS 上的應用程式環境必須具備高可用性。公司應採取哪兩項行動來滿足需求？（選兩項）

**選項**
- A. 將應用程式重構為使用 .NET Core 的 AWS Lambda 無伺服器架構。
- B. 使用 AWS Elastic Beanstalk 的 .NET 平台重新託管應用程式，並採用 Multi-AZ 部署。
- C. 重新平台化應用程式，使其執行在使用 Amazon Linux AMI 的 Amazon EC2 上。
- D. 使用 AWS Database Migration Service（AWS DMS）將 Oracle 資料庫遷移到採用 Multi-AZ 的 Amazon DynamoDB。
- E. 使用 AWS Database Migration Service（AWS DMS）將 Oracle 資料庫遷移到採用 Multi-AZ 的 Oracle on Amazon RDS。

**答案**  
BD

---

## Question #198

**題目**  
某公司在內部部署資料中心的 Kubernetes 叢集上執行一個容器化應用程式，並使用 MongoDB 作為資料儲存。公司想把其中部分環境遷移到 AWS，但目前無法修改程式碼，也不能改變部署方式。公司需要一個可將營運負擔降到最低的解決方案。哪個解決方案可滿足需求？

**選項**
- A. 使用 Amazon ECS 搭配 Amazon EC2 worker nodes 作為運算環境，並在 EC2 上執行 MongoDB 作為資料儲存。
- B. 使用 Amazon ECS 搭配 AWS Fargate 作為運算環境，並使用 Amazon DynamoDB 作為資料儲存。
- C. 使用 Amazon EKS 搭配 Amazon EC2 worker nodes 作為運算環境，並使用 Amazon DynamoDB 作為資料儲存。
- D. 使用 Amazon EKS 搭配 AWS Fargate 作為運算環境，並使用 Amazon DocumentDB（with MongoDB compatibility）作為資料儲存。

**答案**  
D

---

## Question #199

**題目**  
某電話行銷公司正在 AWS 上設計其客戶聯絡中心功能。公司需要一個可支援多說話者辨識並產生逐字稿檔案的解決方案。公司也希望能查詢這些逐字稿檔案，以分析業務模式。逐字稿檔案必須保存 7 年以供稽核使用。哪個解決方案可滿足這些需求？

**內容狀態**  
原始檔於目前可擷取內容中未成功還原完整選項與答案，因此暫無法完整提供。

---

## Question #200

**內容狀態**  
原始檔於目前可擷取內容中未成功還原此題，暫無法提供完整翻譯。
